name: Cross-Model Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: {}  # least-privilege; the job below grants only the inverted-privilege ceiling

jobs:
  review:
    permissions:
      pull-requests: write
      contents: read
    uses: francisco-perez-sorrosal/praxion/.github/workflows/reusable-cross-model-review.yml@664c68461605527752c85f649af1e605b813cdaa
    with:
      policy_path: .github/autofix-policy.yml
    secrets:
      CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
