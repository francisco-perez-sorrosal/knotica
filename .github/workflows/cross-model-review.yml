name: Cross-Model Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: {}  # least-privilege; the job below grants only the inverted-privilege ceiling

jobs:
  review:
    permissions:
      pull-requests: write
      contents: read
    uses: francisco-perez-sorrosal/praxion/.github/workflows/reusable-cross-model-review.yml@778bd3cc28df66adf6bc7f0e84dda6076fbb726a
    with:
      policy_path: .github/autofix-policy.yml
    secrets:
      CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
