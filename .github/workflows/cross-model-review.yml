name: Cross-Model Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: {}  # least-privilege; the job below grants only the inverted-privilege ceiling

jobs:
  review:
    permissions:
      pull-requests: write
      contents: read
    uses: francisco-perez-sorrosal/praxion/.github/workflows/reusable-cross-model-review.yml@79f9f6b1831dfeb3a58fb045d856d000886a11f9
    with:
      policy_path: .github/autofix-policy.yml
    secrets:
      CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
