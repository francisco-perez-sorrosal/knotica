# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/

# uv
# (uv.lock IS committed)

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
tmp/

# Dashboard author dependencies (the generated single-file artifact is tracked).
dashboard/node_modules/

# Ephemeral agent pipeline documents (never commit; .ai-state/ IS committed)
.ai-work/

# Vercel Skills CLI — third-party agent skills (restore via skills-lock.json locally)
.agents/
skills-lock.json

# Claude Code local settings
.claude/settings.local.json
.claude/*.local.md
.claude/*.local.json

# OS
.DS_Store

# Local credentials (discovery API keys) — never commit
.env
