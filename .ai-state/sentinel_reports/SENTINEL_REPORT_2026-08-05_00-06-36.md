# Sentinel Report

**Generated**: 2026-08-05 00:06:36
**Project**: knotica (`/Users/fperez/dev/knotica`), branch `feat/test-topology` @ `1427fd4` (17 commits ahead of `main`, unpushed)
**Run**: second sentinel run. Diffed against `SENTINEL_REPORT_2026-08-04_15-48-39.md`.
**Plugin**: `i-am@bit-agora` v0.21.1 (`~/.claude/plugins/cache/bit-agora/i-am/0.21.1/`)
**Weighting**: Ecosystem Coherence and Consistency, per the run brief.

---

## Scope and Interpretation (surfaced assumptions)

**Interpretation.** knotica is a Praxion-*consumer* project. Praxion's `agents/` and `rules/` trees
are the governing specification, not artifacts under audit. Same scope basis as the baseline.

**Gap-filling assumptions** (each load-bearing, none blocking):

1. **N/A-by-scope set carried forward unchanged** from the baseline: `BC01/BC03/BC04`, `X01/X05/X06/X08`,
   `EC01/EC03/EC04/EC06`, `GL01/GL03`, `V01–V04`, `S02/S03`, `C03/C05`, `N02`, `F04`. Marking these FAIL
   would manufacture ~20 phantom findings.
2. **Every count in this report was measured mechanically**, and the method is stated at the point of
   use. This is a direct response to calibration feedback on the baseline run (see
   [Calibration Against the Prior Run](#calibration-against-the-prior-run)). No count is asserted from reading.
3. **All YAML was parsed with `yaml.safe_load` under `.venv/bin/python`**, never regex. System `python3`
   lacks `pyyaml`.
4. **Baseline closures were re-verified independently**, from the artifacts, not from the ledger's
   resolution notes. Where the ledger's account and my measurement agree, I say so; where the ledger
   corrected *me*, I say that too.
5. **Duplication detection used three lenses this run**, not one — see [Code Health](#code-health-ch).
   The second lens (name-level, cross-module) is what found this run's CH01 finding; the AST lens alone
   would have missed it, exactly as it did last run.

**Objections registered** against three framings in the run brief — see
[Objections to the Run Brief](#objections-to-the-run-brief). Two of the brief's eight named risks are
**clean**, four are **confirmed**, one is **half-right**, one is **overstated**.

---

## Ecosystem Health: **B**

### Summary

**Every one of the five baseline Important findings is genuinely closed, and I verified each one from the
artifacts rather than from its resolution note.** The decision corpus now parses cleanly end-to-end
(72/72 under `yaml.safe_load`, 0 dangling pointers, 0 non-reciprocal links — measured, not read), the
archived spec carries a real 42-row traceability matrix covering all 42 declared REQ ids, the calibration
gate parses its log for the first time, and `DESIGN.md` §3 grew from 18 to 23 Built rows so that all 12
top-level packages are modelled in **both** architecture documents. Three of the closures are backed by a
new committed gate (`scripts/check_adr_health.py`, 9 tests including golden bad-cases, wired into
`make verify`) rather than by a one-time fix — which is the difference between a repair and a remedy.

Grade stays **B**, and the reason is worth stating precisely: **the remediation wave was excellent, and
the same wave introduced three new coherence defects of exactly the class it was closing.** Three new
`core/` modules landed *after* the architecture-doc pass and appear zero times in either architecture
document. `TEST_TOPOLOGY.md` — edited by five actors in one day — now contains two internal
contradictions where an early Note says "deferred to the test-engineer" and a later section says "done".
And a duplicate-consolidation pass that gave topic identity one declaration left one of the seven
consumers on its own inline re-implementation.

None of these blocks correct behaviour. All three are the *residue of speed*: work that was correct at
the moment it was written and was not re-read after the next commit landed. That is a different failure
mode from the baseline's ("created reliably, completed unevenly") and it wants a different remedy — a
closing re-read, not a new gate.

**Trend: improving.** Findings 0/5/5 → 0/3/5, and the three Importants are all newly-introduced rather
than carried forward. Zero baseline findings regressed.

---

### Ecosystem Coherence: **B**

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | N/A (scope) | Pipeline diagram lives in Praxion's `agents/README.md` |
| EC02 | PASS | Every in-repo artifact referenced: 14/14 `commands/*.md` carry `description:` and are claimed by `.claude-plugin/`; `skills/wiki-maintenance/` by the SessionStart nudge; `hooks/hooks.json` by `.claude/settings.json` |
| EC04 | N/A (scope) | Handoff coverage is a Praxion pipeline property |
| EC05 | **PASS** (was WARN) | The narrative/structural disagreement is closed. `okf`, `guillotine`, `service`, `dashboard` now appear in **both** `.ai-state/DESIGN.md` (10/9/10/12 mentions) and `docs/architecture.md` (5/6/8/14). Measured by `grep -c` per package per document |
| EC06 | N/A (scope) | Validates Praxion's own `claude/config/CLAUDE.md` |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` → empty |

**Cross-dimension anomalies:**

1. **The dominant signature has changed, and the change is informative.** The baseline's signature was
   *present container, absent substance* — artifacts created reliably, completed unevenly. That
   signature is **gone**: every container audited this run holds real substance (42 matrix rows, 22
   parseable calibration rows, 23 §3 rows, a 9-test ADR gate). The new signature is **write-forward
   without re-read**: an artifact is correct when written, a later commit invalidates one sentence in
   it, and nothing re-reads the whole. All three Important findings are instances. It is a *faster*
   project's failure mode, and it is cheaper to fix than the baseline's.
2. **The architecture documents are now the slowest-moving surface in the repo, and nothing signals
   that.** `.ai-state/DESIGN.md` and `docs/architecture.md` were last touched at `31b7483`; three
   commits later (`fe10b7a`, `05d1c89`) added three `core/` modules, and the final commit `1427fd4`
   reconciled `TEST_TOPOLOGY.md` to them. The topology tracked the code; the architecture docs did not.
   Measured by `git log --oneline -1 -- <file>` per artifact against `git log --oneline -1 -- <module>`.
   `make verify` gates the topology (`scripts/test_group.py --check`) and the ADR corpus
   (`check_adr_health.py`) but has no architecture-doc check — so the one artifact pair with no gate is
   the one that lagged.
3. **`.ai-state/metrics_reports/` still holds only `index.html`.** TD01–TD04, RD01, and TT04 remain
   dark; `check_readiness_feedback.py` returns skip-with-INFO. Unchanged from baseline. All TD findings
   below are LLM-judgment from direct inspection, not threshold breaches.

**Artifact coherence distribution:** 9 A, 4 B, 2 C (see scorecard). Baseline was 5 A, 6 B, 3 C.

**Synthesis.** As a system, knotica is more coherent than it was 8 hours ago, and it is coherent in the
way that matters most: an agent dropped into this repo today gets accurate orientation from
`CLAUDE.md` → `docs/PRE_PLAN.md` → `docs/architecture.md` → `.ai-state/DESIGN.md`, and every
cross-reference in that chain resolves (0 broken markdown links across all three; 56/56 `dec-NNN`
references resolve). The friction is concentrated in a single seam: **the boundary between "code
changed" and "the architecture record was told".** Three new modules crossed it unrecorded, and the
one artifact that *did* track them — the topology — absorbed architectural content it does not own.
That inversion is the most consequential thing in this report, because it means the next agent that
reads `DESIGN.md` for a `core/` change will get a component model that is three modules and one
enumeration behind, and will not know it.

---

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Commands | 14 (14/14 with `description:`) | stable |
| Skills (in-repo) | 1 (`wiki-maintenance`) | stable |
| ADRs — finalized | 68 | stable |
| ADRs — drafts | **4** (was 1) | +3 |
| ADR frontmatter parsing cleanly (`yaml.safe_load`) | **72 / 72** (was 67/69) | **improving** |
| ADR non-reciprocal cross-refs | **0** (baseline reported 22; true figure was 8) | **improving** |
| ADR dangling pointers | **0** (was 1) | improving |
| ADR index rows | 68 = finalized count; drafts correctly excluded | stable |
| `category: architectural` ADRs carrying `dissent:` | 100% (0 violations, 72 files) | stable |
| Archived specs | 1 | stable |
| — traceability matrix rows | **42** (was 0); 42/42 declared REQ ids covered | **improving** |
| `DESIGN.md` §3 Built component rows | **23** (was 18) | improving |
| Top-level packages modelled in both arch docs | **12 / 12** (was 8/12) | **improving** |
| `core/` modules named in `DESIGN.md`'s residual row vs on disk | 56 claimed / **59 actual** | **degrading** |
| Test-topology groups | **14** (was 11) | improving |
| — `subsystems` entries resolving to §3 Built | **23 / 23**, 1:1, 0 uncovered | stable (PASS) |
| Test files / tests | 175 / **2520** (topology claims 175 / 2520) | verified exact |
| Calibration log rows | 22 (was 21); gate now parses all 22 | improving |
| Tech-debt rows — `LEDGER.md` | 13 (8 non-terminal, 5 `resolved` awaiting post-merge migration) | +2 net |
| Tech-debt rows — `RESOLVED.md` | 24; **0 missing `resolved-by`** (was 3) | improving |
| Always-loaded surface | 85,127 chars / **23,646 tokens** (÷3.6) — 5.4% headroom | flat |
| — of which knotica's own `CLAUDE.md` | **7,875 chars** (was 10,548) | improving |
| `src/knotica/` modules | 192 `.py` | +2 |

**Note on the token line.** knotica shed 2,673 chars from its own `CLAUDE.md` (the `## Current status`
changelog moved to `docs/PRE_PLAN.md`), and the always-loaded total still rose by 100 chars — Praxion-side
rules grew ~2,773 in the same window. knotica's lever worked; it was absorbed upstream. Method:
`cat ~/.claude/CLAUDE.md ./CLAUDE.md` + every `praxion/rules/**/*.md` lacking `paths:` frontmatter,
excluding catalog `README.md` and `CLAUDE.md` index files, `wc -c`, ÷3.6.

---

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|----------------|-----------|----------|---------------|------------|----------------|---------|
| `.ai-state/decisions/` (72) | state | A | B | A | A | A | A | A | **A** ↑ from C |
| `.ai-state/specs/` (1) | state | A | A | A | A | A | A | A | **A** ↑ from C |
| `.ai-state/calibration_log.md` | state | A | B | A | B | A | A | A | **B** ↑ from C |
| `.ai-state/DESIGN.md` | state | B | B | **C** | A | A | A | B | **B** ↑ from C |
| `docs/architecture.md` | doc | B | A | **C** | A | A | A | B | **B** = |
| `.ai-state/TEST_TOPOLOGY.md` | state | A | **C** | A | A | A | B | A | **C** ↓ from A |
| `.ai-state/TECH_DEBT_LEDGER.md` + `RESOLVED` | state | A | A | A | A | A | A | A | **A** ↑ from B |
| `CLAUDE.md` | config | A | A | A | A | A | A | A | **A** ↑ from B |
| `docs/PRE_PLAN.md` | doc | A | A | A | A | A | A | A | **A** = |
| `commands/` (14) | command | A | A | A | A | A | A | A | **A** = |
| `skills/wiki-maintenance/` | skill | A | A | A | A | A | A | A | **A** = |
| `hooks/` + `.claude/settings.json` | hook | A | A | A | A | A | A | A | **A** ↑ from B |
| `.claude-plugin/plugin.json` | config | A | A | A | A | A | A | A | **A** = |
| `.ai-state/idea_ledgers/` (2) | state | A | A | A | A | A | A | A | **A** = |
| `.ai-work/` pipeline docs | ephemeral | **C** | B | A | **C** | A | A | B | **C** new |

`TEST_TOPOLOGY.md` drops A→C on **Consistent** alone; its schema, cross-references, and arithmetic are
flawless (see [Test Topology](#test-topology-tt)). The drop is two internal prose contradictions, and it
is the honest grade — a file that says two different things about the same fact in two places is not
consistent, however good the rest is.

---

## Findings

### Critical (blocks correct behavior)

*None.*

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I1 | AC01 / AC02 / P04 | Architecture Completeness | `.ai-state/DESIGN.md:54`, `docs/architecture.md`, `src/knotica/core/{topics,jsonl,loop_attempt}.py` | **Three `core/` modules created today appear zero times in either architecture document.** Measured by `grep -c -F "core/topics.py"` (and the two siblings) against both docs: `DESIGN.md` = 0, `architecture.md` = 0 for all three; bare-name greps (`topics.py`, `jsonl.py`, `loop_attempt.py`) also return 0/0. `TEST_TOPOLOGY.md` names all three. Three concrete consequences: **(a)** §3's `core/` residual row asserts "`core/` holds **56 top-level modules**" — `git ls-tree` at `31b7483` (the docs' last touch) gives 55 excl. `__init__`/56 incl.; at HEAD it is 58/59. The count is stale by exactly the three new modules. **(b)** `loop_attempt.py` is missing from §3's loop row, which is an *explicit fifteen-module enumeration*, not a residual — so this is an omission from a closed list, not an under-covered catch-all. **(c)** `docs/architecture.md`'s header claims "Last verified against code — 2026-08-04 … Every path in the table below was re-checked on disk", which is true of the 42 paths present and silent about the three absent. **Mechanism, from the pipeline's own log:** `.ai-work/sentinel-remediation/PROGRESS.md` records `[implementer] Phase 7/10 … DESIGN.md/architecture.md deliberately NOT edited (verbatim-mirrored into the forbidden TEST_TOPOLOGY.md)`. Adding an enumerated `core/` sibling is structural; the implementer delegation checklist requires `(if structural) .ai-state/DESIGN.md + docs/architecture.md`. The content went into the topology file instead — an ownership inversion in which the test-topology artifact became the de-facto architecture record | Add `topics.py` / `jsonl.py` to §3's `core/` residual enumeration and `loop_attempt.py` to the loop row's Component cell; correct "56" to 59; mirror into `docs/architecture.md`; add `loop_attempt.py` to Note 2's `loop-runtime` subtraction row. Filed as **`td-038`** | systems-architect |
| I2 | N04 / EC02 | Consistency | `.ai-state/TEST_TOPOLOGY.md:121-131` vs `:1069-1072`; `:169-182` vs `:988-991` | **`TEST_TOPOLOGY.md` states two facts twice, differently.** **(a) The guillotine reconciliation.** Note 2 (line ~129) says: *"The claim is recorded here; reconciling `vault-semantics`' `file_dependencies` against it is a selector change and **belongs to the test-engineer**"* — i.e. pending. The Open-divergence section (line ~1070) says: *"`operations/guillotine.py` did need one and **got it**: it left `vault-semantics`' `file_dependencies` for `guillotine-audit`'s in the same edit that created that group"* — i.e. done. Machine check (`yaml.safe_load` over all 14 fenced blocks): `guillotine-audit.file_dependencies == ['src/knotica/guillotine/**', 'src/knotica/core/operations/guillotine.py']`; `vault-semantics.file_dependencies` contains no guillotine entry. **The later section is right; Note 2's deferral sentence is stale.** **(b) The fitness files.** Note 3 names seven files and says they *"are deliberately un-grouped"*. Expanding every group's `arg` list (directory args walked, glob args expanded) shows **6 of the 7 are in group `arg` lists** — `test_file_size_ratchet.py` in 14 groups, `test_architecture_boundaries.py` in 5, and `test_server_tool_surface.py` / `test_tool_description_guards.py` / `test_template.py` / `test_vault_targeting.py` in one each. Only `test_spine.py` is truly un-grouped. The Partition check (*"6 pinned fitness files + 3 un-grouped"*) and the runtime prose (*"A pinned fitness file is shared by construction"*) both describe pinning as done. **Note 3 is the stale one.** Both instances share a shape: an architect-owned Note written early, a test-engineer-owned section written later, and no closing re-read | Rewrite Note 2's final sentence to past tense and Note 3's closing paragraph to name `test_spine.py` as the sole un-grouped fitness file. Two sentences. Filed as **`td-039`** | test-engineer |
| I3 | CH01 | Code Health | `src/knotica/core/lint.py:222` vs `core/status.py:514` vs `core/vault_metadata_tree.py:129` | **The topic-identity consolidation is one consumer short, and the survivor is a behavioural fork.** `fe10b7a` created `core/topics.py` (`__all__ = ["is_topic", "require_topic"]`) and six modules adopted it (`datasets_inventory`, `branch_scoreboard`, `golden_review`, `status`, `vault_metadata_tree`, `service/manager`, `mcp_server/tools_read`). But `_topic_directories` is still defined in **three** modules with **two** variants: `status.py` and `vault_metadata_tree.py` are byte-identical (`[name for name in sorted(store.list_dir("")) if is_topic(store, name)]`); `lint.py:222` is an independent inline re-implementation that never calls `is_topic` — it composes `not name.startswith(".")`, `name not in RESERVED_TOP_LEVEL_NAMES`, and a local `_is_directory` (`lint.py:742`). Two divergences: **(i) ordering** — `lint.py` returns `store.list_dir("")` order, the other two return `sorted(…)`, and `lint.py:160` feeds it straight into the lint report, so report ordering is filesystem-dependent where every other consumer is stable; **(ii) exception surface** — `_is_directory` catches only `NotADirectoryError` where `is_topic` also catches `FileNotFoundError`. *(ii) is latent, not live:* all names come from `list_dir("")`, so they exist. **This is the second lens finding.** Whole-function AST comparison sees three distinct bodies and reports nothing; name-level cross-module comparison sees one name, three modules, two variants. `DESIGN.md`'s `search/` row states the invariant this violates — *"a search result can never disagree with the rest of the codebase about what a path holds"* — and `core/vault_layout.py:17` explicitly routes reserved-name checks *"through `knotica.core.topics.is_topic`"* | Replace `lint.py:222`'s body with the two-line `is_topic` form; delete the now-unused `_is_directory`. ~4 lines. Filed as **`td-040`** | implementer |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S1 | CA01 / N03 | Calibration | `.ai-state/calibration_log.md` rows dated 2026-07-19, 2026-07-22, 2026-07-30 | **The schema migration is structurally sound and semantically lossy in three places.** The gate is alive — `check_calibration_coverage.py` now returns `covered: true, newest_calibration: 2026-08-05, uncalibrated_commits: 0`, and I cross-checked its row count against `grep -cE '^\| 20[0-9]{2}-'` = 22, so it is reading real rows, not failing open. Three cells are wrong rather than sparse: **(a)** the 2026-07-22 `deprecated-alias-removal` row carries `Recommended Tier` = `Actual Tier` = `Standard (folded into in-flight pipeline)`; the calibration spec is explicit that execution-mode prose *"belongs in this [Source] cell — never conflate it"*. **(b)** the 2026-07-30 row's `Source` = `escalated at intake — Partly — Full was the right shape and was run as such after registering the objection at intake`; `git show deff595` confirms this is the old `Right call?` verdict relocated into `Source`, where the spec allows only `recommended` / `user-override` plus execution-mode prose. **(c)** the 2026-07-19 row's tier is `Lightweight+`, not a 5-tier enum member. Separately, **0 of 22** `Retrospective` cells lead with the spec's enum (`correct` / `under-calibrated` / `over-calibrated` / `[pending]`) — all begin `Shape: …`. Empty `Signals` on all 22 is **not** a defect: the pre-migration schema had no such column, so nothing was lost. One row is out of chronological order (2026-07-30 precedes 2026-07-29) | Fix the three cells; optionally prefix Retrospectives with the enum going forward. No ledger row — this is a 3-cell edit, not carried work | user |
| S2 | N05 | Consistency | `CLAUDE.md:10`, `.ai-state/DESIGN.md:21` and `:297`, `docs/PRE_PLAN.md:50` | **`.knotica/locks/` is a third state location that all three canonical statements of the stateless invariant still exclude by their own wording.** All three say the vault (git) and `config.toml` are the **only** state. `dec-draft-7f4c1a92` moves the retry clock to a gitignored marker there and names the cost itself: *"Costs a second, gitignored state location for the attempt clock"*. **The brief's suspicion is half-right and I want to be precise about which half.** There is **no behavioural contradiction**: the invariant is scoped to the *MCP server* ("Stateless server: no session state"), the retry clock belongs to the *loop daemon* (a separate process), and `PRE_PLAN.md:105` already draws that line for `dec-044` — *"the service manager controls the daemon, not the MCP server"* — while listing "heartbeat liveness" among preserved invariants. And the precedent is **not** undocumented drift: `CLAUDE.md:21` names `.knotica/locks/` heartbeats in always-loaded text. What is missing is that the carve-out lives only inside one draft ADR's Consequences prose. A reader of any of the three "only state" sentences gets a claim the codebase no longer satisfies literally | Add a half-sentence carve-out to the `CLAUDE.md:10` bullet — e.g. *"…the only **server** state; the loop daemon additionally keeps gitignored runtime markers under `.knotica/locks/`"* — and mirror into `DESIGN.md:297` / `PRE_PLAN.md:50` | user |
| S3 | P06 / GL03 | Pipeline Discipline | `.ai-work/sentinel-remediation/` | **A Standard-tier pipeline ran with no `TASK_BRIEF.md`, and P06 passed anyway because its trigger is `SYSTEMS_PLAN.md`.** The slug holds only `LEARNINGS.md`, `TEST_RESULTS.md`, `PROGRESS.md` — no `TASK_BRIEF.md`, `SYSTEMS_PLAN.md`, `IMPLEMENTATION_PLAN.md`, or `WIP.md` — while `PROGRESS.md` records a 10-phase systems-architect run, an 8-phase test-engineer run, and 7 implementer phases, and two draft ADRs carry `pipeline_tier: standard`. `check_p06_task_brief.py --json` → `[]`, because it keys on `SYSTEMS_PLAN.md` presence: a slug missing *both* the plan and the brief is invisible to it. This is a **GL03 instance in Praxion's own gate** — the check asserts a conditional where the unconditional Standard/Full brief floor is the real contract | Local: none required, the work is done. **Upstream (Praxion)**: P06's trigger should be "Standard/Full tier implied by *any* of `SYSTEMS_PLAN.md`, `IMPLEMENTATION_PLAN.md`, or ≥2 distinct agents in `PROGRESS.md`" | user |
| S4 | Pipeline Isolation | Pipeline Discipline | `git worktree list` | **A Standard-tier pipeline ran in the main checkout.** `git worktree list` shows `/Users/fperez/dev/knotica  1427fd4 [feat/test-topology]` — the pipeline branch is checked out in the canonical working tree, not a worktree, where the protocol requires `EnterWorktree` for Standard/Full. Mitigating and worth stating: only one pipeline was in flight, the branch *is* isolated from `main`, seven worktrees exist and are used routinely, and nothing collided. Low practical risk; recorded because the deviation is silent | Note only | user |
| S5 | CH01 | Code Health | `src/knotica/evals/{golden,runner}.py`; `core/gapfill.py:884` | **Second-lens residue below the I3 bar.** `_strip_code_fence` is **AST-identical** in `evals/golden.py` and `evals/runner.py` (lens C). Name-level clusters with divergent bodies: `_config_error` × 4 modules, `_optional_str` × 4, `_load_note` × 3, `_load_json_object` × 2, `_citation_validity` × 2 — each plausibly independent, none contradicting a stated invariant. Separately, lens A found **63 cross-module imports of `_private` names**; 57 are the deliberate `mcp_server` dispatcher↔implementation seam documented in §3, but two cross a *package* boundary: `core/gapfill.py:884` imports `_normalize_doi` / `_normalize_url` from `knotica.discovery.service`, in a codebase that maintains an explicit `mcp_server ⊬ discovery` import-boundary test | Promote the two `discovery.service` helpers to public, or move them to a shared leaf. Not ledgered — below the bar and not invariant-contradicting | implementer |

---

## Baseline Diff — 2026-08-04 15:48:39 → 2026-08-05 00:06:36

**Every closure below was verified from the artifact, independently of the ledger's resolution note.**
The measurement method is given for each.

### Closed (5 Important, 2 Suggested — 7 of 10)

| Baseline | Verdict | Independent verification |
|----------|---------|--------------------------|
| **I1** — 2 ADRs with YAML `safe_load` rejects | **CLOSED** | Parsed all 72 ADRs with `yaml.safe_load`: **72/72 succeed**, 0 missing required fields, 0 duplicate ids, 68/68 finalized ids match `dec-\d{3}`, 4/4 draft ids match `dec-draft-[0-9a-f]{8}` with `status: proposed`. Root cause closed too: `scripts/check_adr_health.py` is wired into `make verify` and carries 9 tests including `test_rejects_frontmatter_that_strict_yaml_cannot_parse` — a live gate with a golden bad-case, not a one-time fix |
| **I2** — non-reciprocal `re_affirms` | **CLOSED** | Built an id→frontmatter map and compared every pointer against its target's back-link: **35 `re_affirms` pointers, 0 non-reciprocal; 3 `supersedes`, 0 non-reciprocal; 0 reverse orphans** (`re_affirmed_by` entries with no matching `re_affirms`). `check_adr_health.py` gates it. **The ledger's note corrects my baseline count and the correction is right** — see [Calibration](#calibration-against-the-prior-run) |
| **I3** — §3 omits 4 shipped packages | **CLOSED** | §3 Built rows 18 → **23**. `grep -c` per package across both docs: `okf` 10/5, `guillotine` 9/6, `service` 10/8, `dashboard` 12/14 — all non-zero in **both**, where the baseline measured zero in both for three of them. All 42 `src/knotica/…` paths in `docs/architecture.md` resolve on disk; 24/24 in `DESIGN.md` resolve except `src/knotica/agent/`, which is `Status: Planned` and correct |
| **I4** — spec traceability is a pointer, not a matrix | **CLOSED** | `## Traceability` now holds **42 rows**, and the 42 distinct REQ ids in the matrix equal the 42 in `## Requirements` — 1:1, measured by `grep -oE 'REQ-[A-Z]+-[0-9]+' \| sort -u \| wc -l` over each section. 4 rows carry `*none*` for tests and are honestly annotated as drill/unrecoverable. The section leads with a `RE-DERIVED, NOT CONTEMPORANEOUS — 2026-08-04` banner naming its own limits. **This is a better artifact than a contemporaneous matrix would have been**, because it tells the reader what it cannot prove |
| **I5** — calibration gate dead (fails open) | **CLOSED** | `check_calibration_coverage.py --json` → `covered: true, newest_calibration: 2026-08-05, uncalibrated_commits: 0`. Cross-checked against `grep -cE '^\| 20[0-9]{2}-'` = 22 rows — the gate is reading, not skipping. Header is now the canonical 7 columns. Three residual cell-level defects → **S1** |
| **S1** — `dec-062` dangling `dec-draft-c4b81d90` | **CLOSED** | 0 dangling pointers across all 72 ADRs; `grep 'supersedes\|superseded_by' 062-*.md` returns nothing. `test_adr_health.py::test_rejects_a_finalized_record_that_points_at_a_draft_id` is the regression guard |
| **S2** — 3 resolved rows missing `resolved-by` | **CLOSED** | Parsed both ledger files by header index: **0 terminal rows missing `resolved-by`** in either `TECH_DEBT_LEDGER.md` or `TECH_DEBT_RESOLVED.md` |

### Dispositioned, not closed (1)

| Baseline | Verdict | Evidence |
|----------|---------|----------|
| **S4** — `re_affirms` used as informal "builds on" | **ACCEPTED BY DECISION** | Still true mechanically: **27 of 31** ADRs carrying `re_affirms` have `status != 're-affirmation'`. But `dec-draft-df837e3b` now *decides* this: *"Accept the broadened meaning for this project… the referring ADR does not need `status: re-affirmation`"*, paired with mandatory reciprocity enforced in `make verify`. That is the correct disposition of the finding — the convention was broadened deliberately and the resulting failure mode was gated rather than left to author discipline. **One residual worth naming, which the ADR's own `dissent:` already names**: `df837e3b` is a *draft*, so it is absent from `DECISIONS_INDEX.md` by design, and a Praxion-side consumer assuming the narrow protocol will read 27 informal pointers here as formal re-affirmations. No action while the branch is unmerged; finalize resolves it |

### Regressed / newly introduced (3)

All three are **new**, all three are in artifacts the remediation wave touched, and all three are the
same shape — see I1, I2, I3 above. No baseline finding regressed.

### Unchanged (1)

| Baseline | Verdict | Evidence |
|----------|---------|----------|
| **S5** — always-loaded token headroom | **FLAT** | 85,027 → 85,127 chars; 5.5% → 5.4% headroom. knotica did its part (`CLAUDE.md` 10,548 → 7,875, the exact lever the baseline recommended); Praxion-side growth absorbed it. Not knotica's to fix |
| **S3** — 3 duplicate function bodies | **PARTIALLY CLOSED, RESCOPED** | `_is_topic` consolidated into `core/topics.py` (6 adopters) — genuinely closed. `_read_jsonl_dicts`/`_parse_jsonl` consolidated into `core/jsonl.py` (2 adopters). `_require_topic` consolidated. **But the second lens shows the consolidation was one consumer short** → I3 |

---

## Per-Dimension Results

### Test Topology (TT)

The structural checks are **immaculate**, and the arithmetic is verified against the live tree rather
than taken on trust. The one downgrade is prose consistency (I2).

| Check | Result | Evidence |
|-------|--------|----------|
| **TT01** | **PASS** | All **23 `subsystems` entries across 14 groups** resolve to `Status: Built` rows in `DESIGN.md` §3, after normalizing backticks and whitespace (the topology renders §3 cells with backticks stripped; a naive string compare produces 6 false positives — stated so the method is reproducible). Distinct subsystems = 23; §3 Built rows = 23; **`Built` rows with no owning group = 0**. Exactly 1:1, and it grew from 18:18 to 23:23 without losing the property |
| **TT02** | **PASS** | All 14 groups use `strategy: pytest-globs` (registered, Registry 1, Python leaf). **Every selector `arg` path resolves**: 0 misses across all groups |
| **TT03** | **PASS** | 0 `class = topology-drift` rows in the ledger (threshold ≥3) |
| **TT04** | **SKIP** (policy) | 0 `METRICS_REPORT_*.json` with per-group data (threshold 7). `expected_runtime_envelope` absent from all 14 — correct and explicitly reasoned: *"one sample per group is a measurement, not a p50/p95 distribution"* |
| **TT05** | **PASS** | 14 valid kebab-case ids; no collision with the reserved set (`unit`/`integration`/`contract`/`e2e`) raw or snake_cased. Marker clause N/A (`pytest-globs` requires no marker registration) |
| **TT06** | **SKIP** | Topology exists |

**Schema completeness beyond the checks.** All 14 groups carry every required field. **All
`file_dependencies` globs resolve** (0 misses). No dangling `integration_boundaries`. Tier
distribution: 11 `integration`, 1 each `unit`/`contract`/`e2e`. `parallel_safe: true` on all 14;
`shared_fixture_scope` 13 `per-suite` / 1 `per-test`.

**The arithmetic is verified, not asserted — and this is the part that most impressed me.** The file
makes four independent numeric claims; I checked all four against the live tree:

| Claim | Method | Result |
|---|---|---|
| "Files — 175 total" | `os.walk('tests')` for `test_*.py` | **175** ✓ |
| "166 own-membership + 6 pinned fitness + 3 un-grouped = 175" | Expanded every `arg` (directory args walked, globs expanded), classified by membership count | **166 + 6 + 3** ✓ — and the 3 un-grouped are exactly `test_adr_health.py`, `test_spine.py`, `test_topology_runner.py` |
| "Tests — 2520 total" | full `pytest` run | **2520 passed**, 0 failed, 310.56 s ✓ |
| "column sums to 2606 against 2480 unique; the 126 excess is 13×6 + 4×12" | Arithmetic on the runtime table | **2606 − 126 = 2480** ✓ |

`scripts/test_group.py --check` → `topology check OK — 14 groups, every selector and dependency resolves`.

**On the brief's disclosure about crossed ownership boundaries** (five actors editing one file, including
direct edits into architect- and test-engineer-owned sections): **the numbers survived; two sentences of
prose did not.** Every derived figure in the file is correct against the live tree, including the
non-obvious column-sum identity that exists precisely to catch a stale row. What broke is what
incremental cross-boundary editing typically breaks — a Note written early and not re-read after the
section it defers *to* was completed (I2, both instances). My judgment: editing derived figures directly
was proportionate and demonstrably harmless; the missing discipline was not routing, it was a closing
read-through of the whole file after the last section landed. That is cheaper than routing and would
have caught both.

### Decision Log (DL)

| Check | Result | Evidence |
|-------|--------|----------|
| DL01 | PASS | 68 finalized matching `^\d{3}-.+\.md$` + 4 drafts matching `^\d{8}-\d{4}-[a-z0-9-]+-[a-z0-9-]+-[a-z0-9-]+\.md$` |
| DL02 | **PASS** (was FAIL) | **72/72 parse under `yaml.safe_load`**; all 8 required fields present on all 72; 0 duplicate ids; 100% of `category: architectural` carry `dissent:` |
| DL03 | PASS | 68 index rows = 68 finalized; ids match exactly in both directions (0 in-index-not-on-disk, 0 on-disk-not-in-index). 4 drafts correctly absent |
| DL04 | **PASS** (was WARN) | **0 dangling** across all `supersedes`/`superseded_by`/`re_affirms`/`re_affirmed_by` |
| DL05 | PASS | 1 archived spec, 72 ADRs |
| DL06 | **PASS** (was FAIL) | **0 non-reciprocal** in either direction, gated by `check_adr_health.py` |

**The four drafts cohere — I checked specifically for the contradiction the brief asked about.**

- `dec-draft-4b91f4f7` (topology binds to §3 granularity) ⇄ `dec-draft-c20759d6` (§3 granularity for the
  four packages) — **reciprocal pair**, `re_affirms` / `re_affirmed_by` both set. `c20759d6` states
  explicitly: *"Realizes reversal trigger (a) of `dec-draft-4b91f4f7` … that decision is re-affirmed,
  not superseded: the binding rule was right and this is the §3 pass it named as the unblock."*
  **That is the correct call, not an evasion of supersession.** `4b91f4f7`'s *rule* ("where §3 is
  silent, no group is created") is unchanged and still holds; only its then-current *application*
  ("`okf/`, `guillotine/`, `service/` … fall through to pipeline-tier") is now historical. A rule whose
  triggering condition stopped obtaining has not been superseded.
- `dec-draft-df837e3b` (broadened `re_affirms` + reciprocity gate) and `dec-draft-7f4c1a92` (suppress
  no-information loop writes) carry no cross-references and conflict with nothing.
- **No draft supersedes another without saying so**, and no draft contradicts any of the 68 finalized
  records. `dec-draft-7f4c1a92`'s relationship to the finalized `dec-068` is stated in its own
  Considered Options: *"The runner-up, and the option `dec-068` already partly took."* Complementary,
  not contradictory.
- `DESIGN.md` contains **2 `dec-draft-*` id citations** (`4b91f4f7`, `c20759d6`) where the baseline
  measured zero. **This is correct, not a regression** — `adr-conventions.md § Linking to ADRs` sanctions
  citing an in-flight ADR *by id* (never by path) in persistent files, and `DESIGN.md:481` says so:
  *"draft id finalizes at merge"*. 0 `dec-draft-*` in `docs/architecture.md`.

### Architecture Completeness (AC)

| Check | Result | Evidence |
|-------|--------|----------|
| AC01 | **WARN** | Both docs now model all 12 top-level packages (baseline gap closed), but three `core/` modules are unrecorded — **I1** |
| AC02 | PASS | Component names internally consistent (§3 ↔ Data Flow ↔ the vault-write table) |
| AC03 | PASS | 24 `src/knotica/…` paths in `DESIGN.md`; 1 unresolved (`src/knotica/agent/`, `Status: Planned` — correct) |
| AC04 | PASS | **43 `dec-NNN` in `DESIGN.md` + 13 in `docs/architecture.md` = 56, all resolve** to finalized files |
| AC05 | PASS | `docs/architecture.md` exists |
| AC06 | PASS | Component names match real modules |
| AC07 | PASS | **All 42 `src/knotica/…` paths in `docs/architecture.md` resolve** — the `Path (verified on disk)` column is accurate for every row present (I1 is about rows absent) |
| AC08 | PASS | Component tables carry no Status column; "Planned" appears only in scoping prose that excludes those items |
| AC09 | **WARN** | Same three-module residue as I1, in the same direction in both docs — a *shared* omission, which is what the brief asked about. Not double-counted |
| AC10 | PASS | `aac_fence_validator.py` → `Verdict: PASS` on both documents |
| AC11 / AC12 | SKIP | LikeC4 model substrate absent (`components.svg` still `TODO(diagram): not yet rendered`); bidirectional traceability convention not populated |

### Spec Health (SH)

| Check | Result | Evidence |
|-------|--------|----------|
| SH01 | **PASS** (was FAIL) | All three `.ai-work/wiki-mvp-core/` references are now annotated in place as deleted-at-cleanup rather than presented as live paths |
| SH02 | **PASS** (was FAIL) | 42 matrix rows. **The GL03 substance clause I recommended last run would now pass honestly**, not vacuously |
| SH03 | PASS | Spot-checked REQ-MUT-01 (single-commit transaction) and REQ-MUT-05 (import-boundary discipline) against §3's dependency rule and `test_architecture_boundaries.py` — still accurate |
| SH04 | **WARN** (was VACUOUS) | The matrix now has entries, so SH04 can finally say something: **8 genuine gaps** (1 `UNTESTED`, 7 `PARTIAL`). Already ledgered as `td-037` — correctly filed as one row with a shared cause, not eight |
| SH05 | PASS | `## Key Decisions` substantive |
| SH06 | N/A | Single spec, no predecessor |
| SH07 | PASS | `check_spec_drift.py --json` → `{"findings": [], "count": 0}` (run under `.venv/bin/python`) |
| SH08 | PASS | `check_spec_archival_gap.py` → `gap: false` (33-day-old spec, no qualifying ≥3 ADR cluster) |

### Calibration (CA)

- **CA01 — PASS** (was FAIL). Canonical 7-column header; 22 data rows. Three cell-level defects → **S1**.
- **CA02 — PASS.** 22 rows clears the 5-entry threshold. **Recommendation-vs-actual match: 21/22 (95%)**
  — the single divergence is 2026-07-30 (`Standard` recommended, `Full` actual), and the row explains
  why. Well above the 60% floor; **no override pattern exceeds 40%** (one override in 22 = 4.5%).
- **CA03 — PASS** (was FAILS OPEN). `covered: true, newest_calibration: 2026-08-05, uncalibrated_commits: 0`,
  cross-checked against a mechanical row count so I know it is reading rather than skipping.

### Code Health (CH)

**CH01 was run with three lenses this run, in direct response to the calibration feedback.**

| Lens | Method | Yield |
|---|---|---|
| **A — cross-module private imports** | AST walk of every `ImportFrom` in `src/knotica`, flagging imported names beginning `_` | 63 hits; 57 are the documented `mcp_server` dispatcher↔implementation seam; 2 cross a package boundary (S5) |
| **B — name-level, cross-module** | Collect every module-level and class-level `def` name; report names defined in ≥2 modules; classify bodies as IDENTICAL or DIVERGENT | **This is the lens that found I3.** `_topic_directories` × 3 modules / 2 variants. Also `_config_error` × 4, `_optional_str` × 4, `_load_note` × 3 |
| **C — AST-normalized bodies (≥8 statements)** | `ast.dump` of the function body, md5, group by hash | 1 hit: `_strip_code_fence` identical in `evals/golden.py` and `evals/runner.py` |

**The lens question the feedback raised, answered.** Lens C alone — the baseline's only lens — reports
`_topic_directories` as three unrelated functions, because three different bodies hash three different
ways. Lens B reports one name, three modules, two variants, and points straight at the divergence. The
failure mode named in the feedback (*"whole-function AST comparison could not see it because two copies
were a different shape and one was an import"*) is structural, and lens A + lens B together are the fix:
A catches the import case, B catches the different-shape case. **I have added both to my standing
method.**

### Pipeline Discipline (P)

- **P01–P05** SKIP — Task Chronograph MCP unavailable.
- **P04 — WARN.** `PROGRESS.md` records the implementer declining a deliverable its delegation checklist
  makes conditional-mandatory (`(if structural) DESIGN.md + docs/architecture.md`) and writing the
  content into `TEST_TOPOLOGY.md` instead. Boundary inversion → **I1**.
- **P06 — PASS (fails open).** `check_p06_task_brief.py --json` → `[]`, but the brief floor was in fact
  missed → **S3**.
- **P07 SKIP** — no `INTERFACE_DESIGN.md` / `TRANSACTIONS_DESIGN.md` / `CONSULT_*.md`.
- **P08 — no advisory.** `clean_work_safety.py` → `{total: 6, block: 0, warn: 3, safe: 3, stale_safe: 0}`.
  Threshold `stale_safe ≥ 3`; actual 0.

**On the brief's question about three truncated subagents (risk 8): I found no partially-applied work.**
Six independent checks, all green: `uv run ruff check .` → `All checks passed!`; `uv run mypy src/knotica`
→ `no issues found in 192 source files`; `scripts/check_adr_health.py` → `ADR health OK — 68 finalized +
4 draft record(s)`; `scripts/test_group.py --check` → `topology check OK`; **`uv run --extra evals pytest` → `2520 passed, 20 warnings in 310.56s`, exit code 0** — the full
suite, green, at exactly the count `TEST_TOPOLOGY.md` claims; `git status --porcelain` → only
`.ai-state/observations.jsonl` (a runtime append-log) plus two untracked non-code paths
(`.claude/praxion-rules.yaml.example`, `designs/memory-guillotine-pitch/` — the latter dated 2026-07-07,
predating this session). Working tree is otherwise clean; no `.orig`/`.rej`/stray fragments. **The
ground-truth re-derivation held.** The one thing a truncation *did* leave behind is not code — it is I1,
and `PROGRESS.md` shows that was a deliberate scope call rather than an interruption artifact.

### Freshness (F)

- **F01/F02 PASS** — 0 broken markdown links across `CLAUDE.md`, `docs/PRE_PLAN.md`, `docs/architecture.md`
  (resolved relative to each file's directory, anchors stripped).
- **F03 — the `CLAUDE.md` → `PRE_PLAN.md` relocation is clean in both directions.** `CLAUDE.md § Current
  status` is now a 9-line summary with three outbound links, all resolving. `docs/PRE_PLAN.md:162` carries
  `### Implementation status (as of 2026-08-05)` with an explicit relocation note. No document anywhere
  in the repo still points at `CLAUDE.md § Current status` (the only grep hit is a frozen
  `observations.jsonl` telemetry record from 2026-07-19, not a live reference). **Risk 6: clean.**
- **F07/F08/F09 N/A** — `skills/wiki-maintenance/SKILL.md` declares no `staleness_sensitive_sections`.
- **F10 PASS** — unchanged from baseline; four git hooks live, finalize hook byte-identical to the
  versioned copy.
- **F11 SKIP** — `.ai-state/doc_manifest.yaml` absent.

### Gate Liveness (GL)

- **GL02 PASS** — `check_gate_liveness.py --json` → `{"findings": [], "count": 0}`.
- **GL03 lens applied locally.** The baseline surfaced two dead gates; **both are now alive** (CA03 parses
  its log; SH02 would pass on substance). One new instance found: **P06 asserts a conditional where the
  contract is unconditional** (S3). Recommended substance clause given there.
- **A gate the project built itself deserves naming.** `scripts/check_adr_health.py` + `tests/test_adr_health.py`
  is a textbook live gate: 9 tests, each a golden bad-case (`test_rejects_frontmatter_that_strict_yaml_cannot_parse`,
  `test_rejects_a_one_directional_re_affirms`, `test_rejects_a_back_reference_with_no_forward_pointer`,
  `test_rejects_a_finalized_record_that_points_at_a_draft_id`) plus two no-false-positive controls. It is
  wired into `make verify`. This is the correct response to a class of defect, and it is why I1/I2 are
  the *only* Decision-Log-adjacent findings this run.

### Behavioral Contract (BC)

- **BC01/BC03/BC04** N/A-by-scope.
- **BC02 PASS** — `CLAUDE.md § Behavioral Contract` names all four behaviors in canonical order with
  identical spelling: **Surface Assumptions → Register Objection → Stay Surgical → Simplicity First**.

### Skipped / Not Applicable

| Dimension | Reason |
|-----------|--------|
| TD01–TD04, RD01, TT04 | `.ai-state/metrics_reports/` holds only `index.html` — unchanged from baseline. `check_readiness_feedback.py` → skip-with-INFO |
| C09 / F05 / F06 | No `compose.yaml`/`Dockerfile`; `SYSTEM_DEPLOYMENT.md` correctly absent |
| HK01 | `PRAXION_HACKATHON_MODE` unset — check inert |
| PR01 | No `PRE_REFACTOR_PLAN.md` under any slug |
| X03 | `CLAUDE.md` has no `## Structure` section — not required |

---

## Tech-Debt Findings

**Filed this run: 3 rows** (`td-038`–`td-040`), all `source = sentinel`, all `status = open`,
`first-seen`/`last-seen` `2026-08-05`.

**Staleness WARN.** `.ai-state/metrics_reports/` contains no `METRICS_REPORT_*` and no `METRICS_LOG.md`.
Per policy this is a TD-dimension WARN, not a block. All three rows are LLM-judgment from direct
inspection; TD01–TD04 had no data and filed nothing.

**Why each row was warranted** (the gating policy's required judgment trace):

- **`td-038` — three `core/` modules unrecorded in both architecture docs** (`important`, `drift`,
  `goals-to-code`, owner `systems-architect`). Warranted on three grounds. (1) *Grounded and exact*:
  0 mentions of each of three paths in each of two documents, plus a numeric claim ("56 top-level
  modules") that `git ls-tree` shows is stale by exactly those three. (2) *Not a residual-coverage
  question*: `loop_attempt.py` is missing from an **explicitly enumerated fifteen-module** §3 row, which
  is a closed list. (3) *It is carried work with a named owner*, not a fix-now typo — the §3 loop row
  and the residual row both need judgment about where the modules belong, which is the same judgment
  `dec-draft-c20759d6` exercised. `direction = goals-to-code` because the goals artifact lags the code.
- **`td-039` — `TEST_TOPOLOGY.md` internal contradictions** (`suggested`, `drift`, `goals-to-code`, owner
  `test-engineer`). Filed as **one** row, not two, because both instances share a single cause: a Note
  written early, a section completed later, no closing re-read. Severity `suggested` rather than
  `important` because the *machine-readable* half of the file is correct in both cases — the YAML, the
  selectors, and the arithmetic all agree with the live tree; it is only the prose that misleads. But it
  is ledgered rather than fix-now because the file is 1,000+ lines with five recent authors, and "read
  the whole thing once and reconcile the Notes" is a real task, not a two-line edit.
- **`td-040` — topic-identity consolidation is one consumer short** (`suggested`, `correctness`,
  `code-to-goals`, owner `implementer`). **This row exists because the previous run's judgment call was
  overturned and the overturn was right.** The baseline saw the same class of defect, measured it at
  "~30 lines, below the bar", and deliberately did not file it; the true shape was a behavioural fork in
  a predicate `DESIGN.md` declares single-source. I am filing the successor rather than repeating the
  call. Severity `suggested` rather than `important` because the two live implementations agree at
  today's only call site — the observable difference is ordering, and the predicate divergence is latent.
  Class is `correctness` rather than `complexity` because the defect is a behavioural difference, not
  a size or duplication metric.

**Deliberately NOT filed:** S1 (three calibration cells — a 3-cell edit, no carried work), S2 (one
half-sentence in three files), S3/S4 (pipeline-process observations, and S3's real fix is upstream in
Praxion), S5 (below the bar, invariant-neutral, and the baseline's mistake was filing too *little* of
the *right* thing, not too little of everything).

**TD05 — ledger discipline audit** (reads only, writes no rows):

| Sub-check | Result |
|-----------|--------|
| (a) terminal rows missing `resolved-by` | **PASS** (was WARN, 3 rows) — 0 across both ledger files, parsed by header index |
| (b) `in-flight` rows older than 30 days | **PASS** — `td-008` (`last-seen 2026-07-22`, 14 days), `td-032` (1 day) |
| (c) `owner-role = unassigned` older than 7 days | **PASS** — 0 unassigned |

**Ledger quality note (unprompted, second run running).** The ledger's resolution notes are the most
useful artifact in this repo for an auditor, because they **argue with the finding they close**.
`td-033`'s note reads: *"8 one-directional `re_affirms` backfilled across 6 files (NOT the 22 the finding
claimed; sentinel over-counted ~3x and its cited example dec-025 already listed both back-refs)."* That
is a resolution note doing something most never do — recording that the *finding* was wrong while
closing it anyway. `td-032` remains `in-flight` with an explicit half-done accounting rather than being
flipped to `resolved` on the architect half. `td-036` records that the fix was *deletion* because the
code had zero references, and that the fitness test was widened from a package name-list to the literal
`DESIGN.md` rule — closing the gate's own blind spot along with the defect. Forward-pointer:
`.ai-state/TECH_DEBT_LEDGER.md`.

---

## Calibration Against the Prior Run

The brief flagged two measurement failures in the baseline. Both are accepted, and both changed how this
run was executed.

**1. I2 was over-counted ~3× (reported 22, actual 8), and the cited example was wrong.** Accepted
without reservation. `dec-025` did list both `dec-030` and `dec-032` in its `re_affirmed_by`; I asserted
the opposite. The mechanism I named was correct and is what `dec-draft-df837e3b` acted on — but a correct
mechanism attached to a 3×-inflated count is a finding a reader cannot trust.

*What changed:* **every count in this report was produced by a script and the method is stated inline.**
Concretely — ADR reciprocity by building an id→frontmatter map with `yaml.safe_load` and comparing each
pointer against its target's back-link in both directions; topology membership by expanding every `arg`
(walking directory args, globbing the rest) and classifying by membership count; module counts by
`git ls-tree` at two revisions and diffing; test counts by `pytest --collect-only`. Where a naive
measurement would have produced a false positive, I say so and give the normalization (TT01's backtick
stripping; the topology's directory-form `arg` values, which made my first membership pass report 24
un-grouped files where the true figure is 3).

**2. CH01 was under-counted — five consumers in two divergent variants, one of them a cross-module
private import, reported as "3 exact-duplicate bodies … below the bar".** Accepted. The framing "below
the bar" was wrong on its own terms: a behavioural fork in a predicate `DESIGN.md` declares single-source
is not a line-count question.

*What changed:* **three lenses instead of one** (see [Code Health](#code-health-ch)). Lens B — name-level,
cross-module, with an IDENTICAL/DIVERGENT classification — found this run's I3, which lens C alone reports
as three unrelated functions. Lens A — cross-module imports of `_private` names — is the answer to the
"one was an import" half. Both are now standing method, not a one-off.

**A note on what the correction cost and what it bought.** The baseline's over-count did not cause wasted
work: `td-033` closed the real 8 and then built the gate, which is what mattered. The under-count *did* —
the `_is_topic` fork shipped one more day and the consolidation that eventually fixed it left `lint.py`
behind. The asymmetry is worth naming: **an over-count is embarrassing, an under-count is expensive.**
Where this run was uncertain I measured rather than estimated, and where I could not measure I said so.

---

## Objections to the Run Brief

The brief invited correction, "mine especially". Three framings warrant it.

**1. "Four unfinalized ADR drafts … any contradiction, or any that supersedes another without saying so?"
— NO CONTRADICTION FOUND, and the one relationship that *looks* like an unstated supersession is
correctly a re-affirmation.**

`dec-draft-c20759d6` reverses the *practical outcome* of `dec-draft-4b91f4f7` — the earlier draft says
`okf/`, `guillotine/`, `service/`, `dashboard/` tests "fall through to pipeline-tier", and after
`c20759d6` they have groups. A reader could reasonably call that supersession. It isn't, and the
distinction is load-bearing: `4b91f4f7`'s decision is a **rule** ("where §3 is silent, no group is
created"), not a **state** ("these four have no group"). The rule is unchanged; §3 stopped being silent.
`c20759d6` says exactly this and points at the reversal trigger `4b91f4f7` itself specified. The pointers
are reciprocal and machine-verified. **This is the protocol working as designed**, and it is worth
recording that the brief's suspicion was reasonable and the artifact survives it.

**2. "`td-031` moved the loop's retry clock into a gitignored runtime marker … Is that consistent with
the stated invariant, or does it widen it? … the precedent may itself be undocumented drift." —
HALF-RIGHT, and the wrong half is the one you were least sure about.**

The precedent is **documented**, in always-loaded context: `CLAUDE.md:21` states the loop *"heartbeats to
`.knotica/locks/`"*, and `PRE_PLAN.md:105` records for `dec-044` that *"the service manager controls the
daemon, not the MCP server"* while listing "heartbeat liveness" among preserved invariants. So this is
not undocumented drift, and there is no behavioural inconsistency — the invariant is scoped to the MCP
server's *session* state, and the retry clock belongs to a different process.

What *is* true, and is the real residual: the sentence "the vault (git) and `config.toml` are the **only**
state" appears in three places (`CLAUDE.md:10`, `DESIGN.md:21`/`:297`, `PRE_PLAN.md:50`) and **none was
amended**, even though `dec-draft-7f4c1a92` itself calls the change *"a second, gitignored state
location"*. The carve-out exists only inside a draft ADR's Consequences prose. **The invariant is not
violated; it is unrestated** (S2). That is a documentation gap, not an architecture problem — a materially
smaller thing than the brief feared, and worth fixing anyway because the literal reading is now false.

**3. "`DESIGN.md` §3 went 18 → 23 rows and `docs/architecture.md` was updated in the same pass. Do they
agree in *detail* … A shared omission is still an omission." — CONFIRMED, and sharper than the framing
allows for.**

They agree in detail on everything they cover: all 12 top-level packages appear in both, 56/56 `dec-NNN`
references resolve, 42/42 `architecture.md` source paths resolve, and `aac_fence_validator.py` passes both.
The shared omission is real and it is **not** the four packages — it is three modules that did not exist
when the pass ran. The pass was correct when written. What the brief's framing does not anticipate is
the *timing*: `git log -1 --` shows both docs last touched at `31b7483`, then `fe10b7a` and `05d1c89`
added the modules, then `1427fd4` reconciled the topology to them and left the architecture docs behind.

**This is the finding I most want on the record, because it generalizes.** `make verify` gates the
topology and the ADR corpus. It does not gate the architecture documents. In a repo that lands 17 commits
in a day, the ungated artifact is the one that lags — and this time the *gated* artifact (the topology)
absorbed the content the *ungated* one should have carried, which makes the lag harder to see, not
easier. The remedy is not another finding; it is a `make verify` step that fails when a `src/knotica/**`
module has no mention in either architecture doc. That is a ~20-line check and it would have caught I1
at commit time.

**One framing I decline to correct.** The brief's disclosure about crossing `TEST_TOPOLOGY.md`'s ownership
boundaries invited disagreement. Having checked all four of that file's numeric claims against the live
tree and found them exact, I do not think the boundary crossing caused harm — the derived figures are
right, including the self-checking column-sum identity. The two defects I did find (I2) are not
attributable to *who* edited, but to *nobody re-reading the whole file at the end*. I would not route
those edits differently; I would add a final read.

---

## Recommended Actions (prioritized)

1. **Record the three new `core/` modules in both architecture documents** (I1 / `td-038`) — add
   `topics.py` + `jsonl.py` to §3's `core/` residual enumeration, `loop_attempt.py` to the loop row's
   fifteen-module cell and to Note 2's `loop-runtime` subtraction row, and correct "56 top-level modules"
   to 59. *Owner: systems-architect. Effort: one focused pass.*
2. **Add an architecture-doc freshness check to `make verify`.** A ~20-line script asserting every
   `src/knotica/**/*.py` module is named in `DESIGN.md` or `docs/architecture.md` (or in a declared
   residual row) would have caught I1 at commit time, and it is the only artifact pair in the repo with
   no gate. Model it on `check_adr_health.py`, which is the right shape. *Owner: user. Highest
   leverage item in this report.*
3. **Reconcile the two stale sentences in `TEST_TOPOLOGY.md`** (I2 / `td-039`) — Note 2's guillotine
   deferral and Note 3's "deliberately un-grouped". *Owner: test-engineer. Effort: two sentences plus a
   full read-through.*
4. **Fold `lint.py:_topic_directories` onto `core.topics.is_topic`** (I3 / `td-040`) — ~4 lines, and it
   completes a consolidation that is otherwise done. *Owner: implementer.*
5. **Fix three calibration cells** (S1) — the `Standard (folded into…)` tier values, the `Partly —…`
   in `Source`, and `Lightweight+`. *Owner: user. Effort: minutes.*
6. **Restate the stateless invariant** to carve out daemon runtime markers (S2), in all three places it
   is stated. *Owner: user.*
7. **Run `/project-metrics`** — still the lowest-effort/highest-information-gain action available; would
   reactivate TD01–TD04, RD01, and eventually TT04. Unchanged recommendation from the baseline.
   *Owner: user.*
8. **Upstream feedback to Praxion** — P06's trigger keys on `SYSTEMS_PLAN.md` where the brief floor is
   unconditional at Standard/Full (S3). *Owner: user.*

---

*Read-only audit. No artifact under audit was modified. Writes this run: this report,
`.ai-state/sentinel_reports/SENTINEL_LOG.md`, and 3 appended rows in `.ai-state/TECH_DEBT_LEDGER.md`.*
