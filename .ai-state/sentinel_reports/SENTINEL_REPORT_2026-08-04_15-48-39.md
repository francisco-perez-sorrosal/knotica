# Sentinel Report

**Generated**: 2026-08-04 15:48:39
**Project**: knotica (`/Users/fperez/dev/knotica`)
**Run**: **first sentinel run — baseline establishment.** No prior `SENTINEL_REPORT_*.md` and no `SENTINEL_LOG.md` existed; all trend columns read `baseline`.
**Plugin**: `i-am@bit-agora` v0.21.1 (checks executed against `~/.claude/plugins/cache/bit-agora/i-am/0.21.1/`, the versioned path the project actually resolves)

---

## Scope and Interpretation (surfaced assumptions)

**Interpretation.** knotica is a *Praxion-onboarded consumer project*, not a Praxion ecosystem checkout. Its context-artifact surface is `.ai-state/`, `CLAUDE.md`, `docs/`, `.claude/`, `.claude-plugin/`, `commands/`, `hooks/`, `skills/`, plus the git-hook installation. Praxion's own `agents/`, `rules/`, and skill catalog live outside this repo and are treated as the **governing specification**, not as artifacts under audit.

**Gap-filling assumptions made** (each load-bearing, none blocking):

1. **Checks presupposing an in-repo `agents/` + `rules/` tree are N/A-by-scope, not failures.** BC01/BC03/BC04, X01/X05/X06/X08, EC01/EC03/EC04/EC06, GL01/GL03, V01–V04, S02/S03, C03/C05, N02, F04 audit Praxion's own artifact tree. Marking them FAIL here would manufacture ~20 phantom findings against a project that correctly does not vendor them. They are recorded as N/A with a reason.
2. **The versioned plugin path is the authority for script-backed checks.** Where a check invokes a Praxion script, I ran `~/.claude/plugins/cache/.../0.21.1/scripts/`, not `/Users/fperez/dev/praxion/scripts/`, because `installed_plugins.json` resolves to the former and that is what the project's own hooks use.
3. **The always-loaded token budget uses the ÷3.6 basis** documented as authoritative in `rules/CLAUDE.md § Token Budget`, excluding catalog `README.md` files. Stated explicitly because three mutually inconsistent bases have historically coexisted and every PASS is basis-dependent.
4. **Test-topology counts were taken as given.** Per the run brief I audited the topology's *structure and consistency* (TT01–TT05), not its measured partition. I did not run `pytest --collect-only`. All TT results below are structural.

**Objections registered** — see [Objections to the Run Brief](#objections-to-the-run-brief). Two of the three pre-labelled framings survive scrutiny; one is materially understated.

---

## Ecosystem Health: **B**

### Summary

knotica's context-artifact ecosystem is **well above typical first-audit condition**. The decision log is dense and disciplined (69 ADRs, 100% of `architectural` ADRs carry a `dissent:` field — a compliance rate I have not seen in any prior audit), the tech-debt ledger carries substantive, honestly-remeasured prose rather than stub rows, and `.ai-state/TEST_TOPOLOGY.md` passes every structural check on its first day of existence.

Grade **B** rather than **A** is driven by five Important findings, all of the same species: **artifacts whose containers are present and correct but whose substance is missing or unverified.** Two ADRs have frontmatter that no strict YAML parser accepts; 22 of ~30 ADR re-affirmation pointers are one-directional; the archived spec's traceability section is a pointer to a deleted file rather than a matrix; the calibration-coverage gate silently passes because it cannot parse the log it is checking; and `DESIGN.md` §3 omits four shipped packages entirely.

No Critical findings. Nothing in the ecosystem blocks correct agent behavior today. Every Important finding is a **latent** failure — tooling that will break or has already silently stopped biting.

**Trend**: baseline. Establishes the reference point for future runs.

---

### Ecosystem Coherence: **B**

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | N/A (scope) | Pipeline diagram lives in Praxion's `agents/README.md`, not this repo |
| EC02 | PASS | Every in-repo artifact is referenced: `skills/wiki-maintenance/` by `.claude-plugin/` + SessionStart nudge; all 14 `commands/*.md` by the plugin manifest; `hooks/hooks.json` + `scripts/session_state_guard.sh` by `.claude/settings.json` |
| EC04 | N/A (scope) | Handoff coverage is a Praxion pipeline property |
| EC05 | **WARN** | Structural gap for stated purpose: `CLAUDE.md § Current status` describes the guillotine and service-lifecycle subsystems at length, and `docs/okf.md` documents `okf/` — yet **none of `okf/`, `guillotine/`, `service/` appears even once in either architecture document.** The narrative surface and the component model disagree about what the system contains |
| EC06 | N/A (scope) | Validates Praxion's `claude/config/CLAUDE.md` against its own reference |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` → `[]` (no important-severity findings) |

**Cross-dimension anomalies:**

1. **The "present container, absent substance" pattern spans four dimensions.** SH02 (traceability section with no matrix), CA03 (log present, unparseable), DL02 (frontmatter present, invalid), AC01/EC05 (§3 table present, four packages missing). This is a single systemic signature, not four coincidences: knotica's artifacts are *created* reliably and *completed* unevenly. It is exactly the failure class Praxion's own GL03 check exists to name, and it is the strongest cross-cutting signal in this audit.
2. **No project-local gate validates any `.ai-state/` metadata.** `scripts/` holds 13 project scripts, of which only `check_id_citation_discipline.py` and `regenerate_adr_index.py` touch `.ai-state/`, and neither validates frontmatter or cross-references. Every DL/SH/CA finding below is a direct consequence: nothing would have caught them but this audit.
3. **`.ai-state/metrics_reports/` holds only `index.html`** — no `METRICS_REPORT_*.json`, no `METRICS_LOG.md`. This deactivates six checks at once (TD01–TD04, RD01, TT04). Not a defect, but it means the entire quantitative-signal half of this audit is dark; all TD findings below are LLM-judgment from direct inspection.

**Artifact coherence distribution:** 5 A, 6 B, 3 C (see scorecard).

**Synthesis.** The system works as a whole. Agent-facing artifacts (`CLAUDE.md`, the topology, the ledger, the ADR corpus) are coherent, cross-referenced, and current — an agent dropped into this repo gets accurate orientation. The friction is concentrated at **machine-readable metadata boundaries**: places where a human reading the artifact sees correct content but a parser sees something broken or absent. That is a benign failure mode for human collaborators and a corrosive one for the tooling that is supposed to keep the ecosystem honest.

---

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills (in-repo) | 1 (`wiki-maintenance`, 182 lines) | baseline |
| Commands | 14 (all with `description:`) | baseline |
| Hooks | 2 (`hooks.json`, `session_start.sh`) + 4 git hooks | baseline |
| ADRs — finalized | 68 | baseline |
| ADRs — drafts | 1 (`dec-draft-4b91f4f7`) | baseline |
| ADR index rows | 68 (= finalized count, drafts correctly excluded) | baseline |
| Archived specs | 1 (`SPEC_wiki-mvp-core_2026-07-03.md`, 42 REQ ids) | baseline |
| Tech-debt rows — active | 7 → **11** (4 filed this run) | baseline |
| Tech-debt rows — resolved | 24 | baseline |
| Calibration log rows | 21 | baseline |
| Test-topology groups | 11 (18/18 §3 Built components mapped 1:1) | baseline |
| Idea ledgers | 2 | baseline |
| Always-loaded surface | 85,027 chars / **23,619 tokens** (÷3.6) — 5.5% headroom | baseline |
| — of which knotica's own `CLAUDE.md` | 10,548 chars (12.4%) | baseline |
| `src/knotica/` modules | ~190 `.py` | baseline |
| — modelled in `DESIGN.md` §3 | 18 component rows (24 modules unmodelled) | baseline |

---

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|----------------|-----------|----------|---------------|------------|----------------|---------|
| `.ai-state/TEST_TOPOLOGY.md` | state | A | A | A | A | A | A | A | **A** |
| `.ai-state/TECH_DEBT_LEDGER.md` + `RESOLVED` | state | A | A | A | A | A | A | B | **B** |
| `CLAUDE.md` | config | A | A | A | A | A | B | B | **B** |
| `commands/` (14) | command | A | A | A | A | A | A | A | **A** |
| `skills/wiki-maintenance/` | skill | A | A | A | A | A | A | A | **A** |
| `hooks/` + `.claude/settings.json` | hook | A | A | B | A | A | A | A | **B** |
| `.claude-plugin/plugin.json` | config | A | A | A | A | A | A | A | **A** |
| `docs/architecture.md` | doc | B | A | A | A | A | A | B | **B** |
| `.ai-state/DESIGN.md` | state | **C** | A | B | A | A | A | **C** | **C** |
| `.ai-state/decisions/` (69) | state | **C** | B | A | **C** | **C** | A | B | **C** |
| `.ai-state/specs/` (1) | state | **C** | A | **C** | B | **C** | A | B | **C** |
| `.ai-state/calibration_log.md` | state | A | **C** | A | **C** | A | A | B | **C** |
| `.ai-state/idea_ledgers/` (2) | state | A | A | A | A | A | A | A | **A** |
| `docs/PRE_PLAN.md` | doc | A | A | A | A | A | A | A | **A** |

Dimensions not appearing as scorecard columns — Pipeline Discipline (P), Code Health (CH), Tech Debt (TD), Test Topology (TT), Decision Log (DL), Architecture Completeness (AC), Spec Health (SH), Calibration (CA), Gate Liveness (GL) — are reported in Findings and the per-dimension sections below.

---

## Findings

### Critical (blocks correct behavior)

*None.*

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I1 | DL02 | Decision Log | `.ai-state/decisions/018-golden-set-split-and-bootstrap.md:7`, `.ai-state/decisions/068-loop-blocked-failure-backoff.md:13` | **Two ADRs have YAML frontmatter that `yaml.safe_load` rejects.** Both are unquoted scalars containing a colon-space: `018`'s `summary:` ends `...Frozen records use source: curate_example (no enum change).`; `068`'s `dissent:` contains `...it is the wrong shape of fix: the loop still writes two commits...`. YAML reads the embedded `: ` as a nested mapping indicator and errors. **Currently latent** — the project's own `regenerate_adr_index.py` uses a tolerant line-regex parser, so `DECISIONS_INDEX.md` is intact (both ADRs present with correct summaries). But every strict-YAML consumer breaks: Praxion's `finalize_adrs.py`, sentinel DL02/DL04/DL06, the verifier's `affected_reqs` cross-reference, and the systems-architect's brownfield ADR discovery | Wrap both values in double quotes (2-line fix). Then add a frontmatter-validity gate — see `td-033` | user |
| I2 | DL06 | Decision Log | `.ai-state/decisions/` (22 ADRs) | **22 of ~30 `re_affirms` pointers are one-directional** — the target ADR's `re_affirmed_by` list omits the referring ADR. Examples: `dec-020 → dec-003/dec-007/dec-013` (none back-link), `dec-030` and `dec-032 → dec-025` (whose `re_affirmed_by` is empty), `dec-040 → dec-001`, `dec-046 → dec-008`, `dec-051 → dec-048`, `dec-064 → dec-062`, `dec-066 → dec-059`. Only the `dec-006`, `dec-007`, and `dec-058` clusters are reciprocal. Consequence: "what later decisions rest on this one?" is unanswerable from the target ADR — the exact query supersession metadata exists to serve | Backfill the 22 missing `re_affirmed_by` entries; gate reciprocity thereafter (`td-033`) | user |
| I3 | AC01 / AC02 / EC05 | Architecture Completeness | `.ai-state/DESIGN.md § 3. Components`, `docs/architecture.md` | **`DESIGN.md` §3 omits four shipped top-level packages entirely — and so does the developer guide.** `src/knotica/okf/` (11 modules), `guillotine/` (9), `service/` (3), `dashboard/` (1) = **24 modules with no component row**. Verified by direct grep: `okf`, `guillotine`, and `service` appear **zero times** in *both* `.ai-state/DESIGN.md` and `docs/architecture.md`; `dashboard` appears once in the guide. Separately, `src/knotica/core/` is one row standing for 56 top-level modules. Consequences: (a) 11 test files / 128 tests belong to no topology group, so scoped runs silently skip them; (b) the systems-architect's brownfield baseline for any future `guillotine/` or `okf/` work starts from a document that does not know those packages exist; (c) `vault-semantics` is a 550-test residual group whose size is unfixable from inside the topology | §3 refinement pass: add rows for the four packages; consider splitting the `core/` row. Then `/refresh-topology` to gain the corresponding groups. Filed as **`td-032`** | systems-architect |
| I4 | SH02 / SH04 / SH01 | Spec Health | `.ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md:225-231` | **The archived spec's `## Traceability` section is a pointer, not a matrix.** It reads: *"Live map: `.ai-work/wiki-mvp-core/traceability.yml` … The rendered matrix (Requirement \| Test(s) \| Implementation \| Status) is baked into this section at feature-end archival, before `.ai-work/` cleanup."* The bake-in never happened, and `.ai-work/wiki-mvp-core/` no longer exists (present slugs: `kb-dashboard-switcher`, `loop-consolidation`, `notes-overlay`, `retrieval-scorer-fix`, `test-topology-init`). All three referenced `.ai-work/` paths dangle (SH01). **42 REQ ids in the spec have no test or implementation mapping**, and SH04 (no UNTESTED entries) passes only vacuously — there are no entries at all. The evidence is unrecoverable without re-derivation | Re-derive the REQ→test→implementation matrix from the current suite and bake it into the archived spec. Filed as **`td-034`** | test-engineer |
| I5 | CA01 / CA03 / GL03 | Calibration | `.ai-state/calibration_log.md:5` | **The calibration-coverage gate is dead — it passes by failing to parse.** `check_calibration_coverage.py` hard-codes `_TIMESTAMP_COL = "Timestamp"` and locates the header by `if _TIMESTAMP_COL in line`. knotica's header is `\| Date \| Task \| Tier \| Right call? \| Actual shape \| Retrospective \|` — no `Timestamp` column — so `header_index` stays `None`, the function returns `None`, and the check reports `covered: true, newest_calibration: null` over **21 real data rows it never read**. Canonical schema (`skills/spec-driven-development/references/calibration-procedure.md:104`) is `\| Timestamp \| Task \| Signals \| Recommended Tier \| Actual Tier \| Source \| Retrospective \|`. Note the *practice* is healthy — 21 substantive rows with real retrospectives; it is the *gate* that is inert | Reconcile the header to the canonical schema (mapping is lossy: `Tier`/`Right call?`/`Actual shape` → `Recommended Tier`/`Actual Tier`/`Source`), or record an explicit ADR accepting the deviation and the unmonitored coverage. Filed as **`td-035`**. Secondary: the script fails open rather than warning on an unparseable log — worth upstream feedback | user |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S1 | DL04 | Decision Log | `.ai-state/decisions/062-notes-anchor-recovery-window-geometry.md` | **Mixed-stage pointer**: a *finalized* ADR carries `supersedes: dec-draft-c4b81d90`. Per `adr-conventions.md § Finalize Protocol`, finalize rewrites `dec-draft-<hash>` cross-references to `dec-NNN`; this one was missed. The referenced draft no longer exists on disk (the only draft is `dec-draft-4b91f4f7`), so the pointer dangles | Resolve which finalized `dec-NNN` `c4b81d90` became and rewrite; or drop the field if it superseded nothing that shipped | user |
| S2 | TD05(a) | Tech Debt | `.ai-state/TECH_DEBT_RESOLVED.md` | **3 of 24 resolved rows have an empty `resolved-by`**: `td-007`, `td-010`, `td-011`. Schema requires an ADR id, commit SHA, or PR URL when `status = resolved` — without it the resolution is unauditable | Backfill the three commit SHAs | user |
| S3 | CH01 | Code Health | `src/knotica/core/` | **3 exact-duplicate function bodies** (AST-normalized, ≥9 statement-lines): `datasets_inventory._read_jsonl_dicts` ≡ `ingest_activity._parse_jsonl` (11 lines); `datasets_inventory._require_topic` ≡ `golden_review._require_topic` (10); `vault_metadata_tree._is_topic` ≡ `status._is_topic` (9). The third is the notable one: `DESIGN.md` names `core/vault_layout.py` as *"the single declaration"* of path/topic classification precisely so *"a search result can never disagree with the rest of the codebase about what a path holds"* — two independent `_is_topic` implementations mildly contradict that stated invariant | Fold `_is_topic` into `core/vault_layout.py`; consider a shared `_require_topic` and JSONL reader. **Deliberately not ledgered** — 30 lines total is below the bar, and filing it would be the "mechanical dump" the TD gating policy warns against. Re-evaluate if the cluster grows | implementer |
| S4 | N04 / DL | Consistency | `.ai-state/decisions/` | **`re_affirms` is used as an informal "builds on / consistent with" annotation.** 26 of ~30 uses carry `status: accepted` rather than `status: re-affirmation`. `adr-conventions.md § Re-affirmation Protocol` reserves the field for *"a prior decision challenged, re-examined, and found still correct — not routine acknowledgment."* This is very likely the **mechanism behind I2**: an author adding a lightweight "related to" pointer has no reason to go update the target's back-link | Either narrow `re_affirms` to the formal protocol and introduce a separate `relates_to` field, or accept the broadened usage and document it — but then the reciprocity obligation must be automated, since it will not be met by hand | user |
| S5 | T02 | Token Efficiency | always-loaded surface | **85,027 chars / 23,619 tokens against a 25,000 budget — 5.5% headroom** (÷3.6, the basis `rules/CLAUDE.md` declares authoritative; the stricter ÷3.5 basis yields 2.8%). Largest contributors are Praxion-side: `swe-agent-coordination-protocol.md` (26,216), global `CLAUDE.md` (13,253), `adr-conventions.md` (12,220). **knotica's own controllable share is 10,548 chars (12.4%)** | Mostly a Praxion-side concern, not knotica's. knotica's lever if headroom tightens: `CLAUDE.md § Current status` is a ~4,000-char prose block that reads as changelog and could move to `docs/PRE_PLAN.md` with a pointer | user |

---

## Per-Dimension Results

### Test Topology (TT) — **the focus of this run**

`.ai-state/TEST_TOPOLOGY.md` (created 2026-08-04) **passes every applicable structural check.** This is an unusually clean first-day result.

| Check | Result | Evidence |
|-------|--------|----------|
| **TT01** | **PASS** | All **18 `subsystems` entries across 11 groups resolve verbatim** to `Status: Built` rows in `DESIGN.md` §3. Zero unresolved. Coverage is exactly 1:1 — 18 distinct subsystems vs 18 Built components, **zero §3 Built components without an owning group**. `src/knotica/agent/` (the sole `Planned` row) is correctly absent |
| **TT02** | **PASS** | All 11 groups use `strategy: pytest-globs`, a registered Registry 1 identifier (Python leaf). Argument shape conforms (list of path strings, all non-empty). Additionally verified beyond the check's requirement: **every one of the selector `arg` paths exists on disk** |
| **TT03** | **PASS** | Zero `class = topology-drift` rows in `TECH_DEBT_LEDGER.md` (threshold is ≥3) |
| **TT04** | **SKIP** (policy) | Zero `METRICS_REPORT_*.json` files with per-group data (threshold: 7). `expected_runtime_envelope` is absent from all 11 groups — **correct**, the field is explicitly *optional at M1 and M2* per the leaf's `§ expected_runtime_envelope Policy`. Runtimes are instead recorded in prose under `### Verified runtimes (2026-08-04, single sample each)` |
| **TT05** | **PASS** | All 11 ids are valid kebab-case; **no collision with the trunk reserved set** (`unit`/`integration`/`contract`/`e2e`) under either raw or snake_case form. The marker-registration clause is **N/A**: no group declares a marker-selector entry (all are `pytest-globs`, which the registry notes requires *"No marker registration"*) |
| **TT06** | **SKIP** | `.ai-state/TEST_TOPOLOGY.md` exists — TT06 is the adoption nudge for topology-less projects |

**Schema completeness beyond the checks.** All 11 groups carry every required field (`id`, `title`, `subsystems`, `tier`, `selectors`, `file_dependencies`, `parallel_safe`, `shared_fixture_scope`) plus the optional `integration_boundaries` and `notes`. Every `file_dependencies` glob resolves. **No dangling `integration_boundaries`** — every referenced group id exists. Tier distribution: 8 `integration`, 1 each `unit`/`contract`/`e2e`. `parallel_safe: true` on all 11; `shared_fixture_scope` 10 `per-suite` / 1 `per-test`.

**Assessment.** The topology is not merely schema-valid — it is unusually well-reasoned. Notes 1–3 name the `DESIGN.md` §3 gap *at the point of use* rather than leaving it inferable from absence; Note 2's residual definition of `vault-semantics` pre-empts the most likely downstream error (a `core/**` glob swallowing four other groups); and the `### Un-grouped tests` section enumerates the shortfall explicitly. **TT01 passes honestly, not by construction** — the file earns its green by declining to invent synthetic subsystem names, which the draft ADR `dec-draft-4b91f4f7` records as a considered and rejected option.

The one thing TT01 *cannot* see is I3: the topology is correctly bound to a component model that is itself incomplete. TT01 is green and the coverage gap is real, simultaneously and without contradiction.

### Decision Log (DL)

| Check | Result | Evidence |
|-------|--------|----------|
| DL01 | PASS | 68 finalized (`^\d{3}-.+\.md$`) + 1 draft (`^\d{8}-\d{4}-[a-z0-9-]+…$`), both filename shapes conform |
| DL02 | **FAIL** | 2/69 unparseable frontmatter (**I1**). Otherwise: all 8 required fields present on all 69; all finalized ids match `dec-\d{3}`; the draft matches `dec-draft-[0-9a-f]{8}` with `status: proposed`; **no duplicate ids**; **100% of `category: architectural` ADRs carry `dissent:`** (0 violations) |
| DL03 | PASS | 68 index rows = 68 finalized ADRs; ids match. The draft is correctly **absent** from the index — by design, not a gap |
| DL04 | WARN | 1 dangling mixed-stage pointer (**S1**). All other `supersedes`/`superseded_by`/`re_affirms`/`re_affirmed_by` targets resolve |
| DL05 | PASS | 1 archived spec, 69 ADRs — ADR frequency far exceeds the floor |
| DL06 | **FAIL** | 22 non-reciprocal links (**I2**). Supersession chains, by contrast, are **fully reciprocal**: `dec-013↔055`, `dec-045↔050`, `dec-055↔067` all correct, and all 3 superseded ADRs carry `status: superseded` |

### Architecture Completeness (AC)

| Check | Result | Evidence |
|-------|--------|----------|
| AC01 | **WARN** | `DESIGN.md` exists, but under-models — see **I3** |
| AC02 | WARN | Component names are internally consistent (§3 ↔ Data Flow); the failure is omission, not inconsistency |
| AC03 | PASS | 18 §3 paths, 1 unresolved — `src/knotica/agent/`, which is `Status: Planned`. Correct by design |
| AC04 | PASS | **42 `dec-NNN` references in `DESIGN.md` and 13 in `docs/architecture.md` — all 55 resolve** to finalized ADR files. Zero `dec-draft-*` references in either persistent doc (correct per `adr-conventions.md § Linking to ADRs`) |
| AC05 | PASS | `docs/architecture.md` exists alongside `DESIGN.md` |
| AC06 | PASS | Component names in the guide match real modules |
| AC07 | PASS | **All 32 `src/knotica/...` paths in `docs/architecture.md` resolve on disk.** The guide's `Path (verified on disk)` column is accurate |
| AC08 | PASS (with note) | Component tables at lines 63 and 172 are `\| Component \| Responsibility \| Path (verified on disk) \|` — **no Status column**. "Planned" appears only in scoping prose (lines 17, 49) that explicitly excludes those items from the guide, which is the correct as-built posture. The `\| Status \| Meaning \|` table at line 412 is a *note-anchor resolution* legend, not component status — a false positive, excluded |
| AC09 | WARN | A naive backticked-path diff shows 16 modules in the guide but not `DESIGN.md`. **Most are measurement artifact** — `arena_resolve.py`, `loop_factory.py`, `candidate_gate.py`, `best_effort.py`, `branch_namespaces.py`, `records.py`, `status.py` are all named in §3 *prose* within the `core/loop.py` and `core/` rows. The genuine residue is `src/knotica/dashboard/`, already covered by **I3** — not double-counted |
| AC10 | PASS | `aac_fence_validator.py` → `Verdict: PASS` on both `.ai-state/DESIGN.md` and `docs/architecture.md` |
| AC11 | SKIP | LikeC4 model substrate absent — `docs/diagrams/architecture/src/architecture.c4` is referenced in the §3 comment but the rendered/queryable model is not present (`components.svg` is marked `TODO(diagram): not yet rendered`) |
| AC12 | SKIP | Bidirectional traceability convention not populated (no `metadata.req_ids`, no `architectural_elements:` frontmatter) |

### Spec Health (SH)

| Check | Result | Evidence |
|-------|--------|----------|
| SH01 | **FAIL** | All 3 `.ai-work/wiki-mvp-core/` references dangle (**I4**) |
| SH02 | **FAIL** | `## Traceability` present but contains no matrix (**I4**). Note: the check's own pass condition — *"section present and non-empty"* — technically passes, since the section holds prose. I am flagging it anyway per GL03 (substance over structure) and recommending the check gain a `≥1 matrix row` clause |
| SH03 | PASS | Spot-checked behavioral claims (REQ-MUT-01 single-commit transaction, REQ-MUT-05 import-boundary discipline) against `DESIGN.md` §3's dependency rule and the `test_architecture_boundaries.py` selector — still accurate |
| SH04 | VACUOUS | No UNTESTED entries because there are no entries |
| SH05 | PASS | `## Key Decisions` (line 216) is substantive |
| SH06 | N/A | Single spec, no predecessor |
| SH07 | PASS | `check_spec_drift.py --json` → `{"check":"SH07","findings":[],"count":0}` (run under `.venv/bin/python`; system `python3` lacks `pyyaml`) |
| SH08 | PASS | `check_spec_archival_gap.py` → `gap: false` (newest spec 32 days old, no qualifying ≥3 ADR cluster) |

### Freshness (F)

- **F01/F02** PASS — no broken file references in in-repo artifacts.
- **F07/F08/F09** N/A — `skills/wiki-maintenance/SKILL.md` declares no `staleness_sensitive_sections`; no `last-verified` markers anywhere. The protocol is opt-in and not adopted. Not a defect.
- **F10 — PASS, with a residual-risk note.** All four git hooks are live. `pre-commit` is a regular file resolving `PLUGIN_ROOT` dynamically from `installed_plugins.json` (→ the versioned `0.21.1` path). `post-commit`, `post-merge`, `post-checkout` symlink to `/Users/fperez/dev/praxion/scripts/git-finalize-hook.sh`. **I verified this file is byte-identical to the versioned `0.21.1/scripts/git-finalize-hook.sh` (`diff -q` clean), so there is no active drift.** See [Objections](#objections-to-the-run-brief) for the residual risk.
- **F11** SKIP — `.ai-state/doc_manifest.yaml` absent.

### Calibration (CA)

- **CA01 — FAIL.** Header deviates from canonical schema (**I5**). The file is otherwise well-formed with 21 data rows.
- **CA02 — SKIP.** Requires the coverage parser, which cannot read this log. Note that 21 rows *would* clear the 5-entry threshold — the trend analysis is blocked by I5, not by insufficient data.
- **CA03 — FAILS OPEN.** Reports `covered: true` having read zero rows (**I5**).

### Pipeline Discipline (P)

- **P01–P05** SKIP — Task Chronograph MCP tools unavailable in this session.
- **P06 PASS** — `check_p06_task_brief.py --json` → `[]`. No Standard/Full slug has a `SYSTEMS_PLAN.md` without a `TASK_BRIEF.md`.
- **P07 SKIP** — no `INTERFACE_DESIGN.md`, `TRANSACTIONS_DESIGN.md`, or `CONSULT_*.md` under any `.ai-work/` slug.
- **P08 — no advisory.** `clean_work_safety.py` → `{total: 5, block: 0, warn: 2, safe: 3, stale_safe: 0}`. Threshold is `stale_safe ≥ 3`; actual is 0.

### Gate Liveness (GL)

- **GL01/GL03** N/A-by-scope for Praxion's agent corpus — but GL03's *lens* was applied to knotica and is the organizing insight of this report (see Cross-dimension anomaly 1). Two concrete GL03 instances surfaced: **CA03 passes on an unparseable log** (I5) and **SH02's pass condition asserts section presence where a populated matrix is the real contract** (I4). For SH02 I recommend the substance clause: *"`## Traceability` present **and** contains ≥1 requirement row."*
- **GL02 PASS** — `check_gate_liveness.py --json` → `{"findings": [], "count": 0}`.

### Behavioral Contract (BC)

- **BC01/BC03/BC04** N/A-by-scope (Praxion's rule + agent corpus).
- **BC02 PASS** — knotica's `CLAUDE.md § Behavioral Contract` names all four behaviors in canonical order with identical spelling: **Surface Assumptions → Register Objection → Stay Surgical → Simplicity First**. Matches the global `~/.claude/CLAUDE.md` anchor.

### Skipped / Not Applicable

| Dimension | Reason |
|-----------|--------|
| TD01–TD04 | `.ai-state/metrics_reports/` holds only `index.html` — no `METRICS_REPORT_*.md/json`, no `METRICS_LOG.md`. Threshold-driven debt detection is dark; TD findings below are LLM-judgment from direct inspection |
| RD01 | `check_readiness_feedback.py` → skip-with-INFO (no `METRICS_REPORT_*.json`) |
| C09 / F05 / F06 | No `compose.yaml`/`Dockerfile`; `.ai-state/SYSTEM_DEPLOYMENT.md` correctly absent |
| HK01 | `PRAXION_HACKATHON_MODE` unset in `.claude/settings.json` — check is inert, emits nothing |
| PR01 | No `PRE_REFACTOR_PLAN.md` under any `.ai-work/` slug |
| X03 | knotica's `CLAUDE.md` has no `## Structure` section (8 sections, none structural) — not required |
| F11 | `.ai-state/doc_manifest.yaml` absent |
| **Skill-genesis** | `.ai-state/skill_genesis_reports/` absent — confirmed expected, on-demand-only agent, **not a finding** |

---

## Tech-Debt Findings

**Filed this run: 4 rows** (`td-032`–`td-035`), all `class = drift`, all `source = sentinel`, all `status = open`, `first-seen`/`last-seen` `2026-08-04`.

**Staleness WARN.** `.ai-state/metrics_reports/` contains no `METRICS_REPORT_*.md` and no `METRICS_LOG.md`. Per policy this is a TD-dimension WARN, not a block. **Consequence for this run: all four rows are LLM-judgment findings from direct artifact inspection, not threshold breaches.** TD01 (hotspots), TD02 (cyclic SCCs), TD03 (coverage floor), and TD04 (p95 complexity) had no data and filed nothing. Running `/project-metrics` would light up the quantitative half of this dimension.

**Why each row was warranted** (the LLM-judgment trace the gating policy requires):

- **`td-032` — `DESIGN.md` §3 under-models the codebase** (`important`, `goals-to-code`, owner `systems-architect`). Warranted on four grounds. (1) *Grounded*: 24 modules exist on disk with no component row; `okf`/`guillotine`/`service` appear zero times in **both** architecture documents — verified by grep, and this is materially stronger than the "§3 has no row" framing in the draft ADR. (2) *Goal-referenced*: AC01 architecture-doc completeness, plus the topology's scoped-selection goal. (3) *Deferred with a named trigger*: `dec-draft-4b91f4f7` deliberately defers the §3 refinement and specifies the reversal trigger; a ledger row is where a deliberately-deferred, triggered obligation belongs. (4) **An ADR records a decision; it does not track work.** Consumer agents read the ledger for pending obligations, not the ADR corpus — and this ADR is an *unfinalized draft*, therefore absent from `DECISIONS_INDEX.md` by design, so without this row the gap has no discoverable home outside the topology file's prose. `direction = goals-to-code` because the goals artifact lags the code, inverting the project's other seven rows.
- **`td-033` — no gate validates ADR frontmatter or cross-reference reciprocity** (`suggested`, `goals-to-code`, owner `doc-engineer`). Filed as **one** row, not two, because I1 (2 malformed frontmatters) and I2 (22 broken back-links) share a single root cause: nothing in this project validates ADR metadata. Filing the symptoms separately would treat a missing gate as two typos. The immediate 24 defects are cheap to fix; the row exists for the gate, which is the part that prevents recurrence.
- **`td-034` — archived spec lost its traceability matrix** (`important`, `goals-to-code`, owner `test-engineer`). Warranted because the evidence is **unrecoverable**, not merely missing: `.ai-work/wiki-mvp-core/traceability.yml` was deleted at cleanup before the matrix was baked in, so 42 REQ ids must be re-derived from the current suite. That is real work with a real owner — the definition of debt rather than a fix-now defect.
- **`td-035` — calibration log schema deviation disables CA03** (`suggested`, `goals-to-code`, owner `implementer`). Warranted because a **silently dead gate is precisely what a ledger should surface** — left alone it passes green forever. Severity is `suggested` rather than `important` because the *practice* is healthy (21 substantive rows); only the automated check is inert. The column mapping is genuinely lossy (`Tier`/`Right call?`/`Actual shape` do not map 1:1 onto `Signals`/`Recommended Tier`/`Actual Tier`/`Source`), so this is a judgment call, not a rename.

**Deliberately NOT filed:** S3 (3 small duplicate-function clusters, ~30 lines) — below the bar; filing it would be the mechanical dump the gating policy warns against.

**TD05 — ledger discipline audit** (reads only, writes no rows):

| Sub-check | Result |
|-----------|--------|
| (a) `resolved` rows missing `resolved-by` | **WARN** — 3 of 24: `td-007`, `td-010`, `td-011` (**S2**) |
| (b) `in-flight` rows older than 30 days | PASS — only `td-008` is `in-flight`; `last-seen 2026-07-22`, 13 days |
| (c) `owner-role = unassigned` older than 7 days | PASS — zero unassigned rows |

**Ledger quality note (unprompted).** knotica's ledger is the strongest I have audited. Rows carry remeasurement history (`REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above`), explicit bounding mechanisms (`BOUNDED 2026-07-29 by tests/test_file_size_ratchet.py`), and honest partial-resolution accounting (`td-008`: *"PARTIAL RESOLUTION ONLY — research found the ledger's own prior estimate was optimistic"*). `td-031` distinguishes the fixed *instance* from the unfixed *family* — a distinction most ledgers collapse. Forward-pointer: `.ai-state/TECH_DEBT_LEDGER.md`.

---

## Objections to the Run Brief

The brief invited correction of its pre-labelled characterizations. Three were offered; **two survive, one is understated.**

**1. "The git-hook symlink deviation is deliberate dogfooding" — ACCEPTED, with a residual-risk note.**
Verified: `diff -q /Users/fperez/dev/praxion/scripts/git-finalize-hook.sh <versioned 0.21.1 copy>` is clean. There is **no active drift** and no defect today. The characterization holds. The residual risk worth stating: the symlink targets a **mutable working tree**, so the hook body can change under an uncommitted edit or a branch switch in the Praxion checkout with no version bump and no signal here. Note the asymmetry — `pre-commit` resolves `PLUGIN_ROOT` dynamically from `installed_plugins.json` (versioned), while the three finalize hooks are pinned to the dev path. The two halves of the hook installation therefore track different sources. Not a finding; worth knowing.

**2. "`skill_genesis_reports/` absent is expected, not a finding" — ACCEPTED.**
`skill-genesis` is on-demand-only (`/skill-genesis`), never pipeline-spawned. Absence carries no signal. Not reported.

**3. "The `DESIGN.md` §3 gap is `core/` too coarse + four packages with no row" — UNDERSTATED. This is my substantive objection.**
The brief (and `dec-draft-4b91f4f7`) frame the gap as *"four un-modelled trees have no §3 row."* Direct grep shows the gap is **one level worse**: `okf`, `guillotine`, and `service` appear **zero times in `.ai-state/DESIGN.md` as a whole** — not merely absent from the §3 table — **and zero times in `docs/architecture.md` too**. They are not under-modelled; they are architecturally **undocumented in both architecture artifacts**. `dashboard` fares marginally better at one mention in the guide.

This matters for two reasons the current framing obscures. First, `docs/architecture.md` advertises itself as the as-built developer guide with a `Path (verified on disk)` column and a `Last verified against code` date of 2026-07-30 — a developer trusting it would conclude `guillotine/` does not exist, while `CLAUDE.md § Current status` describes the guillotine's behavior in detail and `docs/okf.md` documents `okf/`. **The narrative and structural surfaces disagree about the system's contents**, which is EC05's failure mode, not merely AC01's. Second, it means the remediation is a two-document pass, not a §3 edit.

On the brief's direct question — **severity and whether it warrants a ledger row: Important, and yes.** Reasoning in the Tech-Debt Findings section above. I want to add one point in the draft ADR's favour: its own `dissent:` field already anticipates this — *"A topology that silently under-covers ~24 modules is arguably worse than one with four honest synthetic subsystem names, because the gap is invisible at the point of use."* The ADR argued itself into the right decision (bind to §3, document the gap) while recording the strongest case against. `td-032` is the mechanism that keeps that recorded dissent from decaying into a note nobody re-reads — which is exactly the weakness the ADR's own steelmanned runner-up names: *"the mitigation depends on a human reading a note — the weakest enforcement mechanism available."*

---

## Recommended Actions (prioritized)

1. **Quote the two malformed ADR frontmatter values** (I1) — `018:7` and `068:13`. Two-line fix; unblocks every strict-YAML consumer including Praxion's own finalize path. *Owner: user. Effort: minutes.*
2. **Run a `DESIGN.md` §3 refinement pass** (I3 / `td-032`) — add rows for `okf/`, `guillotine/`, `service/`, `dashboard/`; mirror into `docs/architecture.md`; consider splitting the `core/` row. Then `/refresh-topology` to gain the corresponding groups and close the 128-test scoped-run gap. *Owner: systems-architect. Effort: one focused pass — the ADR correctly notes this is architectural judgment, not a mechanical edit.*
3. **Backfill the 22 missing `re_affirmed_by` entries** (I2), then decide S4 — narrow `re_affirms` to the formal protocol or accept broadened usage and automate reciprocity. *Owner: user.*
4. **Re-derive and bake in the spec traceability matrix** (I4 / `td-034`) — 42 REQ ids currently have no test mapping in the permanent record. *Owner: test-engineer.*
5. **Resolve the calibration-log schema deviation** (I5 / `td-035`) — conform the header, or write an ADR accepting the deviation and its unmonitored coverage. Either is fine; silence is not, because the gate reads green today. *Owner: user.*
6. **Add an ADR-metadata validation gate** (`td-033`) — frontmatter parses + cross-references reciprocal. `scripts/regenerate_adr_index.py` already walks every ADR; the validation is a natural extension of that walk. *Owner: doc-engineer.*
7. **Run `/project-metrics`** — would populate `.ai-state/metrics_reports/` and reactivate six currently-dark checks (TD01–TD04, RD01, and eventually TT04). *Owner: user. Lowest effort, highest information gain.*
8. **Fix the dangling draft pointer** in `dec-062` (S1) and backfill 3 `resolved-by` values (S2). *Owner: user.*
9. **Fold the duplicate `_is_topic`** into `core/vault_layout.py` (S3) — small, but it contradicts a stated `DESIGN.md` invariant. *Owner: implementer.*

---

*Read-only audit. No artifact was modified. Writes this run: this report, `.ai-state/sentinel_reports/SENTINEL_LOG.md`, and 4 appended rows in `.ai-state/TECH_DEBT_LEDGER.md`.*
