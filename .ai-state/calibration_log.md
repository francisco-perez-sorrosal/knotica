# Calibration Log

One row per completed task: tier chosen at intake vs what the work actually needed.

| Timestamp | Task | Signals | Recommended Tier | Actual Tier | Source | Retrospective |
|-----------|------|---------|------------------|-------------|--------|---------------|
| 2026-07-16 | eval-harness (PRE_PLAN Phase 2) |  | Standard | Standard | recommended | Shape: 33 planned steps + ~8 user-directed live-run additions; 11 new modules + CLI; 963 tests (baseline 689); 2 researchers, architect (+1 revision), planner, 16 impl/test pairs, 2 fable light-reviews, exhaustive fable verification (FAIL→rework→PASS-with-findings) — Tier fit well: the characterization-test-first gate and pre-mortem fold-ins each caught real issues (all five pre-mortem guards ended up exercised — live-vault guard, key-leak sweeps, spend ceiling fired live, instrument-drift warning, num_threads pin). Live first-runs surfaced 4 defect classes no offline test could (orphaned method via class-body dedent, raw SDK tracebacks, schema-valid-but-garbage citations → ENAMETOOLONG, mis-calibrated spend default) — budget a live-shakedown step in future instrument-building plans. Four subagent return truncations recovered via ground-truth handshake (RECOVERY_LOG.md); truncation rate suggests keeping heavyweight steps' final report short. Phase-2 criterion met: scalar 0.5707 reproduced bit-for-bit on frozen corpus git:599237b. |
| 2026-07-18 | autoresearch-gapfill research spike |  | Spike | Spike | recommended | Shape: 3 researchers (providers/internal/prior-art) + promethean in parallel; synthesis in .ai-work/autoresearch-gapfill/SYNTHESIS.md — Lens-independent fan-out reconciled cleanly; internal lens found the anticipated-but-unwired held_out_delta placeholder and the discarded retrieval trace — substrate gaps that reshaped the plan from "add search" to "add diagnostics first". Decision: layered spine P0-P4; implementation pending user green-light. |
| 2026-07-18 | gapfill-substrate (P0 diagnostic substrate) |  | Standard | Standard | recommended | Shape: 16 steps; architect (opus) + planner + 3 impl/test pairs + 2 forced light-reviews (both PASS) + opus verifier (PASS WITH FINDINGS); 1143 tests green (+16); live vault reconciled + gen-4 proof run 0.9655 — Architect Register Objection corrected a wrong brief assumption (no fingerprint rotation) before it cost anything — the checklist-driven brief+objection loop earned its overhead. Live proof surfaced two ops gotchas no test could: loop correctly refuses bookkeeping-only commits (Step 16 needed direct eval, not loop --once), and credential limits (API spend cap + OAuth window) dominated wall-clock; SDK retry budget raised in response (td-007 resolved). |
| 2026-07-19 | gapfill-classifier (P1 four-way fault cascade + loop hook) |  | Standard | Standard | recommended | Shape: 14 steps; opus architect + planner + 2 impl/test pairs + 2 forced light-reviews (both revise->closed) + opus verifier PASS-WITH-FINDINGS; 52 scoped tests; suite 1300 — Light-review caught a fabricated-fixture defect the pair missed: the war-story "dilution" test invented trace displacement absent from the real manifests — ground-truth diff (0/25 ids moved) reversed the folklore (regression was generation-fault). Honest-fixture discipline > plausible narrative; the reversal reshapes P3 planning. Ledger: td-008 (loop.py size). |
| 2026-07-19 | gapfill-discovery (P2 provider protocol + youcom/openalex + service) |  | Standard | Standard | recommended | Shape: 31 steps (3 skipped: Exa cut by user directive mid-pipeline); opus architect + planner + 3 impl/test pairs + 2 forced light-reviews + opus verifier PASS-WITH-FINDINGS; 114 discovery tests; no new deps — Two wave-3 agents returned truncated (recurring pattern on heavyweight finales — keep final reports short); ground-truth re-derivation + one DI-seam reconciliation recovered cleanly, cheaper than re-runs. Mid-pipeline scope cuts (Exa) propagate well via WIP strike-through + prompt updates. Live wire-shape verification deferred to user (needs KNOTICA_YOUCOM_API_KEY). |
| 2026-07-19 | gapfill-queue (P3 suggestion queue + approval surface) |  | Standard | Standard | recommended | Shape: 23 steps; opus architect + opus interface-designer shadow (no challenge loop needed) + planner + 3 impl/test pairs + dashboard step + doc-engineer + 3 light-reviews (2 revise->closed) + opus verifier PASS-WITH-FINDINGS; suite 1381 — Interface-designer shadowing paid for itself: the surface schemas were implemented byte-faithfully from INTERFACE_DESIGN with zero rework, and the architect/interface split (structure vs surface) reconciled cleanly via one orchestrator ruling. Recurring failure mode: pipeline-id citations in test docstrings slipped through a FOURTH time despite prompt warnings — the commit gate + light-reviews caught all instances, but the pattern suggests adding it to the test-engineer agent definition, not per-prompt reminders. Truncated finales again on the two heaviest agents; ground-truth recovery now routine (~2 one-line fixes). |
| 2026-07-19 | gapfill-nl-gaps (added-id classification + gap_report + retracted channel + provenance surfaces) |  | Lightweight | Lightweight | recommended — orchestrator-as-designer: orchestrator-authored ADR drafts and 3 impl/test pairs with 2 light-reviews, no architect or planner spawns, which is what the retired `Lightweight+` label was reaching for | correct — Shape: Orchestrator-authored ADR drafts + 3 impl/test pairs + 2 light-reviews (accept, accept) — no architect/planner spawns; 1427 suite — Orchestrator-as-designer worked at this tier because every house pattern was established upstream; the four-incident id-citation streak ended once the warning moved to CAPS with history attached. Guillotine examination flipped my initial surface judgment (its verdicts never delete pages) — evidence-first adjudication beat pattern-matching. |
| 2026-07-19 | gapfill-coherence (cross-spine audit + guillotine refactor + prioritization fix) |  | Standard | Standard | recommended | Shape: Opus architect coherence analysis + cross-spine integration test + refactor pair + 2 light-reviews (accept) + haiku doc sweep; 1430 suite — The coherence pass earned its cost: found one systemic HIGH (origin-blind prioritization — three sites burying reported/retracted gaps) invisible to per-pipeline verifiers, and adjudicated guillotine with evidence (demo-era rewriting machinery deleted; client-as-brain restored; frozen eval unharmed). Integration test green on first full run = the per-pipeline contract discipline (frozen consumer contracts + forced schema reviews) actually prevented seam drift. |
| 2026-07-19 | gapfill-source-gate (P4 candidate_kind gate + worktree ingest + dataset upgrade) |  | Standard | Standard | recommended | Shape: 35 steps, 13 parallel groups; opus architect + opus interface-designer shadow (challenge loop: 1 round, AC-1 pre-resolved) + planner + 6 impl/test pairs + haiku doc steps + 3 forced light-reviews (PASS, PASS-warn, FAIL->revise->closed) + opus verifier READY-WITH-WARNS (2 one-line fixes applied); suite 1505 — Test-engineer full-cycle discipline (drive poll_once, not module internals) caught two production defects unit tests would miss: the for-each-ref single-star glob that made every nested candidate branch invisible, and a grower wiring that fired a real metered API call from an existing test. The FAIL light-review found a silent wrong-branch commit path (checkout-restore swallowed) — and the implementer's revise correctly rejected half the suggested fix (loop-state write would itself misroute), evidence the review loop argues instead of complying. Convergent GREEN-first pattern now documented per-agent instead of objected-to case-by-case. |
| 2026-07-21 | loop-consolidation (P-A internals refactor + P-B two-tier tool surface + P-C conversational routing + P-D service lifecycle) |  | Full | Full | recommended | Shape: 55 planned steps + emergent 11b; worktree pipeline; 3 lens-independent researchers + opus interface-designer shadow (2 challenges, both ruled) + opus architect (+2 scoped re-entries: mid-flight user guidance, lock-scope ruling) + planner + ~20 impl/test spawns + 7 forced light-reviews (5 accept, 2 revise->closed) + opus verifier FAIL->fixes->PASS; suite 1417->1653 fast + 20 slow; 9 ADR drafts — The pre-mortem earned its keep twice: the flock-contention exercise it mandated surfaced a real pre-existing lock-scope gap (flock covered only the state commit, not the merge span — ~60% corruption under daemon-style contention) BEFORE the daemon shipped, and the no-auto-install hard rule caught a def-time subprocess.run default that would have silently defeated the mock net. Worktree cut from stale origin/main cost one researcher rework (agents self-corrected via git show main:). Truncated agent finales now ~1 per heavy agent; resume-with-punch-list is routine and cheap. Verifier FAIL was the fifth id-citation incident (draft-ADR ids in docstrings this time) — the pattern needs a pre-commit grep, not more prompt warnings. td-008 premise inverted: extractions moved code out but helpers/spans moved in (1096->1221); honest ledger row kept in-flight. |
| 2026-07-21 | daemon-env-bootstrap (service entry loads config-dir .env) |  | Direct | Direct | recommended | Shape: Single-behavior fix + 5 tests; 48 service-suite green — First live-install walkthrough caught what no pipeline test could: the no-live-service pre-mortem rule that protected the pipeline also guaranteed the launchd-empty-environment gap (process-env-only LLM credential) stayed invisible until a human actually installed. Step-by-step-with-verification surfaced it at step 1. |
| 2026-07-21 | ingest-merge-remnant-heal (heal-on-acquire for plain ops + daemon logging + hermetic char test) |  | Direct | Direct | recommended | Shape: 3 root-cause fixes + 4 tests; suite 1740 fast + contention green — Second live-E2E catch in one evening: the 11b heal was span-scoped so a daemon that died mid-merge bricked every plain ingest until hand-aborted; and the empty log dir meant launchd discarded the daemon's entire trail. Live use finds the seams between verified components — both fixes were sub-hour once ground truth (MERGE_HEAD + empty logs + launchctl state) was read directly instead of trusting the in-conversation diagnosis. Also: a user provisioning real keys flipped a non-hermetic characterization snapshot — env isolation belongs in every config-sensitive test, not just the suite that first needed it. |
| 2026-07-22 | eval-cadence-model-config (configurable eval cadence + td-011 fix + per-task model config + billed-trigger spend gate) |  | Standard | Standard | recommended | Shape: 31 planned steps + 3 user-directed mid-flight insertions (Step 8b failure-retry floor, Step 24b run_once gate collapse + cross-kind nonce test); worktree pipeline; 2 lens-independent researchers, opus architect (2 ADR drafts), planner, ~14 impl/test spawns, 6 forced light-reviews (4 accept-clean, 2 accept-with-FAIL-then-revise-then-PASS), haiku doc pass + 2 orchestrator doc-gap fixes; suite 1652->1711 fast + 20 slow — Forced review earned its cost twice on the two riskiest steps: the failure-retry floor's first cut gated on a global `pending_retry` flag with no same-content check — would have held unrelated new content hostage behind an unrelated prior failure — caught before merge, not in production. The billed-trigger review went further than the plan asked (user pushed for adversarial depth given real spend risk) and found the pre-existing, undeclared `run_once` MCP action was a second, ungated billing surface identical in kind to the one just gated — closed by reusing (not duplicating) the nonce infrastructure, then proven immune to cross-kind nonce redemption in both directions. Test-count discrepancies (25 vs 31 P-A files) resurfaced twice — once from the planner re-deriving instead of trusting orchestrator ground truth, once as legitimate marker-selection variance across parallel suite runs — worth a standing reminder that "run it yourself" beats re-trusting a prior count. One pre-existing test-hermeticity gap (hardcoded live-vault path, silent no-op on other machines) found during the final proof, filed as td-012, correctly left unfixed as out of declared scope. |
| 2026-07-22 | deprecated-alias-removal (mid-flight scope addition to eval-cadence-model-config: delete all 26 flat-tool aliases + telemetry) |  | Standard | Standard | recommended — folded into the in-flight eval-cadence-model-config pipeline rather than run as its own | correct — Shape: 1 implementer (removal, 2 truncation-recovery resumes) + 1 supersession ADR (dec-045 partial supersession) + 1 cleanup pass (28+2 obsolete parity tests across 7 files, 1 genuine-bug fix in test_mcp_source_ingest.py); suite 1711->1676 (net fewer tests: alias-comparison tests removed, no coverage lost — dispatcher behavior covered elsewhere) — User-driven simplification once premise was checked: the 'keep aliases for one release cycle' policy (dec-045) assumed external consumers needing gradual migration, but knotica has none (self-only, Claude Desktop/Code under the user's own control) — removing dead-weight backward-compat was the right call once surfaced, not scope creep. Two implementer completions truncated without a terminal marker on this wide-footprint (15+ file) task — resumed from transcript both times rather than restarting, which worked, but ground-truth verification (git status, grep, direct suite runs) caught both gaps the agent's own prose missed: an ADR that didn't exist yet cited by id in docs, and 30 parity tests whose entire premise (alias output == dispatcher output) evaporated with the alias. Lesson: wide-footprint mechanical edits need the completion handshake checked against grep/git status, not just trusted, even more than usual. |
| 2026-07-22 | loop-py-extraction (td-008 partial resolution: extract build_loop_runner/arena-race/candidate-gate clusters) |  | Standard | Standard | recommended | Shape: 1 researcher (opus-tier line-count math correcting the ledger's own optimistic estimate), 1 opus architect (3 sibling modules, source_gate.py-idiom for candidate-gate), 1 planner, 4 impl/test spawns (1 characterization backfill + 3 extractions), 2 forced light-reviews (both PASS on first pass); suite 1680 fast + 20 slow throughout — Research corrected a real process failure mode before it repeated a third time: the ledger's "next move" note named the same two extraction targets a prior pass had already *introduced* (not shrunk), and blindly following it would have produced a third overclaimed-resolution ledger entry. Measuring the actual arithmetic before committing to scope, and getting explicit user sign-off on a deliberately partial (low-risk-only) extraction rather than reaching for the higher-risk observe_default cluster in the same pass as its own recent adversarial review, kept this pass honest and low-risk. Three tests retargeted an obsolete monkeypatch path (knotica.core.loop.race_variants -> knotica.core.arena_resolve.race_variants: test_best_effort_characterization.py, test_loop_runner.py, test_source_gate.py) — only the first is one of the five off-limits P-A files, and its diff is a byte-identical single-line target swap with zero assertion change. The orchestrator's own blanket "don't touch P-A files" instruction had been copied from a different pipeline's context without checking fit; caught, reasoned through, and disclosed rather than silently overridden or blindly obeyed. The final verifier pass (opus) flagged that this calibration entry originally undercounted the deviation as "one file" rather than three — corrected here. Architect's ~1087-line estimate landed exactly on the measured result. |
| 2026-07-22 | knotica-init-mcp-from-detection-fix (Direct) |  | Direct | Direct | recommended | Shape: Single-file fix + 2 tests; suite 1784 fast green — Found live, mid-walkthrough: knotica init --desktop's local-checkout auto-detection (_mcp_from_source) walked only __file__'s parents, which resolves into the isolated uv-tool venv (not the repo) when invoked via the globally uv tool install'd binary -- exactly the sequence README's own Channel 2 documents. Silently fell back to the bare, unpublished package name "knotica", breaking uvx pre-warm and (via the same resolver) the Desktop MCP command args. Added a cwd-based fallback probe alongside the __file__ probe; verified end-to-end against the real Desktop config (uv run --directory <repo> ... replaces the broken uvx --from knotica ...) with no env-var workaround needed. Caught only because we deliberately ran the full documented install sequence after merging, rather than trusting the MCP server would "just restart cleanly." |
| 2026-07-24 | retrieval-scorer-fix (BM25 replaces raw-count search scoring) |  | Lightweight | Lightweight | recommended | Shape: Inline investigation + 1 background researcher (IR practice); 3-file change (scorer, contract, tests); suite 1810 green; free search-probe harness over all 25 goldens as the inner loop, zero billed evals — The brief's hypothesis arrived with two prior confident errors attached, and verifying instead of implementing paid off three times: the "index staleness" anomaly dissolved on code reading (no index exists — the unmoved score meant the stripped block contained no query terms); the eval's citation_validity turned out to be integrity-only (resolves-to-a-real-source), so the visible 23/25 masked a much larger retrieval defect (target-page containment was 16/25); and the committed bibliography-strip "fix" was A/B-tested on a clone and shown to fix nothing under BM25 — reverting it is now recommended rather than defended. Also caught own harness bug (report-page names compared by stem) before reporting numbers: baseline had to be recomputed from saved output to keep the before/after honest. Deliberately did not touch the live vault — a content commit there wakes the loop watcher and triggers billed eval spend. |
| 2026-07-24 | eval-error-visibility (live per-question eval outcome list in dashboard + td-013 fix) |  | Standard | Standard | recommended | Shape: Worktree pipeline; opus architect (2 ADR drafts, no interface-designer — additive list in existing card) + planner (14 steps, 5 RED-first groups) + 4 impl/test pairs + 3 forced light-reviews (B PASS-warn, C REVISE→fixed, D PASS-warn) + opus final verifier READY-WITH-WARNINGS (7/7 REQ); suite 1810→1848; zero eval spend (fake runners throughout) — Forced review earned its cost on the concurrency-critical step (C): the td-013 non-raising fix had three escape paths the RED tests missed — payload build (`int()`) ran BEFORE the try, `json.dumps` raises TypeError not OSError, and the finally-unlink was unguarded — each re-opening td-013's exact run-cancellation symptom via a narrower trigger. Reviewer reproduced all three; orchestrator applied the fix + added 2 regression tests proven to fail on the old code. Three of four heavy agents truncated their finales without a terminal marker; every one was recovered from ground truth (git diff + targeted pytest) rather than re-run — completion-handshake-against-grep is now routine, not exceptional. A stray `on_outcome=list.append` (arity-4 vs arity-1) in the RED tests and a pre-existing `IMPLEMENTATION_PLAN.md Step 3` id-citation in loop.py were both caught at orchestrator ground-truth checks, not by the agents. Two carried WARNs accepted as debt (harness_evaluate 60>50 lines → fold into td-008; +2 mypy int(object) of a pre-existing pattern, no [tool.mypy] gate exists). |
| 2026-07-29 | notes-overlay Phase 0 (folder-family foundations for the personal notes layer) |  | Standard | Standard | recommended | Shape: Design pass: 4 parallel researchers (3 codebase lenses + 1 opus external anchoring prior-art) -> opus architect + opus interface-designer shadow (4 Architecture Challenges, 1 orchestrator-mediated loop-back; 5 ADR drafts). Implementation: planner (10 steps) + 2 orchestrator-added steps + 12 total spawns (RED-first throughout) + 2 forced light-reviews (both PASS 0-FAIL-0-WARN) + opus verifier FAIL (1 FAIL / 8 WARN / 14 PASS) -> 5-row rework fixed in place -> scoped re-verification PASS-WITH-FINDINGS. Suite 1791 -> 1849, zero eval spend. — Two independent agents caught the same falsifiable error in each other's inputs: `bootstrap(pages=[note_path])` intersects with `entity_pages()` first, so the eval-bridge path the research pass called 'cheapest' selects zero pages and silently no-ops. Neither would have found it alone. The design phase also produced a genuine correction to its own external research: the recommended `^block-id` spine was rejected because it would have the unscored personal layer editing the scored corpus, and because `^id` tokens are literal BM25-scored text (~7% page-length inflation) -- deferred behind a two-part spike gate instead. THE FAIL WAS THE ORCHESTRATOR'S: U3 (loop watch exclusion) was deferred at intake on the reasoning 'no notes exist yet', then Step 7 shipped `vault-template/SCHEMA.md` promising loop-exclusion into every scaffolded vault -- so hand-authoring a note would have woken the loop and billed a real eval. Root cause found by the verifier and worth keeping: SYSTEMS_PLAN contradicted itself (§ Sequencing put U3 in Phase 0, § Prerequisites P0b deferred it), and one branch was chosen without noticing the conflict existed. Lesson: when a plan states the same item twice, reconcile before decomposing. The identical doc-overreach class recurred one bullet up ('retrieval corpus' vs `_corpus_stats`) and was fixed by narrowing the doc claim, not by relaxing the standard applied to the FAIL. SECOND ORCHESTRATOR ERROR, self-inflicted and worse: a `git checkout HEAD -- src/knotica/search/ripgrep.py` used to revert a deliberately-injected fitness-test bypass succeeded and therefore discarded the whole file, destroying two steps of production work (the `_classify` delegation and the retrieval family filter). Caught only because the full suite was re-run afterwards and went 2-red; restored by hand in ~10 minutes since the tests fully specified the target state. Same class of destructive-git mistake the orchestrator had just criticised an implementer for (bare `git stash` in a shared worktree, whose pop then failed on a uv.lock conflict and needed manual dangling-object recovery). Directive for future passes: never `git checkout HEAD -- <path>` on a file with uncommitted pipeline work; revert a surgical injection with a surgical edit. TOOLCHAIN FINDING with repo-wide impact: local `uv 0.5.24` predates `revision = 2` lockfile support, so EVERY plain `uv run` silently downgrades `uv.lock` (+1421/-1422, strips upload-time from ~300 packages). It recurred twice after reverting, because each agent invocation runs `uv run`. `uv run --frozen` is the verified mitigation and should be adopted everywhere (td-017, left open: artifact reverted, cause not). Ledger: td-014 (id-citation enforcement absent here -- no commit_gate.sh/detector, which is why 5 pipeline-id citations had to be stripped by hand, the FIFTH consecutive pipeline to hit this; it belongs in the agent definitions, not per-prompt reminders), td-015 (notes excluded from search results but still counted in BM25 corpus stats; scoring provably unaffected -- every scoring-path retrieval is topic-scoped), td-016/018 resolved in rework, td-017 + td-019 open. td-019 formalises what a prior calibration entry had already observed in passing ('no [tool.mypy] gate exists'): 69 unconfigured-mypy errors across 24 files, so every 'mypy clean' claim in this and prior pipelines was scoped to hand-picked paths under unagreed defaults. Also confirmed stale: td-001 (its three files already use importorskip), td-004 (13 -> 18 ruff errors), td-008 (loop.py 1087 -> 1123). Process notes that worked: concurrent full-suite runs in one worktree produce transient evals/* import-purity failures (6 observed) -- per-step suite totals are not gates, only a single quiet run is. Two agents registered scope objections rather than silently exceeding their briefs, and both escalations surfaced real defects; the verifier's in-band objection against emitting 5 rework worktrees for ~8 lines was accepted. Every finding this pass came from cross-checking BETWEEN agents' outputs (a doc sentence against a test fixture, a test assertion against an architect constraint, a completion claim against `git status`), never from any single report. |
| 2026-07-30 | notes-overlay Phase 1 (capture + page-level anchoring: the value floor) |  | Standard | Full | user-override — escalated at intake, after the objection was registered | under-calibrated — the procedure recommended Standard and Full was the right shape; escalated at intake and run as such. Shape: 7 waves / 12 steps / 16 agent spawns (2 researchers, planner, 5 test-engineer + 6 implementer RED-first, 2 doc-engineer) + 2 forced light-reviews + opus verifier FAIL (4 FAIL / 8 WARN / 14 PASS) -> all 12 findings fixed in place, no rework worktrees. 13 commits, 47 files, +7797/-83. Suite 1972 -> 2101, zero skipped, zero eval spend. `core/loop.py` byte-identical to main throughout. — **Every defect this phase came from a test or a review; none from reading finished code, and none from any single agent's self-report.** Four contamination leaks were found in sequence and they are one shape, which is the finding worth carrying: the layout excludes notes from scored surfaces *structurally*, but anything that copies a note's path or text into shared state routes around that guarantee. Phase 0 closed the wikilink-source route; W5 closed the commit title laundering a note's body into `log.md`; the verifier found note *deletion* raising `LOG_MISSING_PATH` on the scored topic (0->0->1, silent because the loop sleeps through note changes, surfacing later as an unearned regression); and the final pass found whole-vault lint reporting private notes as wiki defects — including every note that anchored a page the user deleted, which is precisely the case the resolver was built to absorb. Expect a fifth; the audit log is the recurring conduit. THE ISOLATION TEST IS THE PHASE'S MOST VALUABLE ARTIFACT: written to *confirm* a guarantee Phase 0 had carefully designed, it instead proved it false via a path the design never considered. Its author also disclosed that only the lint leg is exercised dynamically — the two judge-scored legs are shown to have an identical corpus, not re-run — and the verifier independently confirmed that disclosure accurate and the structural argument sound. That honesty is the reason the coverage claim can be trusted at all. RED-first earned its keep in an unadvertised way: three separate spec holes surfaced because a *test author* hit something unbuildable, not because a reviewer read code — a closed `Literal` that could not satisfy its own forward-tolerance rule; no defined `fidelity` for `anchor-invalid`; and, most consequentially, a grammar in which a topic-fidelity anchor was unexpressible, so a degraded capture would have kept the user's reflection and silently dropped the quote that provoked it. FIVE ORCHESTRATOR RULINGS overrode the plan, all in one direction — refusing to let a surface claim more than it can back: `unanchored` split from `orphaned` (three ordinary situations produce a page-less anchor, so shipping them as "orphaned" would have flagged routine captures as problems on day one); "drifted" narrowed to `orphaned` only against the plan's `{shifted, orphaned}`; the `notes` payload moved onto the topic row to match `lint_violations`; a `Projection.fidelity` biconditional enforced in `__post_init__` rather than by convention; and `resolved_anchors` renamed off a collision with `NoteDocument.anchors`. ORCHESTRATOR ERRORS, two: (1) I reported the id-citation gate clean having piped the script into `tail`, so the `$?` I read was `tail`'s — the verifier found the detector exiting 1 on three `AC-NN` comments. Reporting a gate green without reading its exit code is the same class of failure I spent the phase rejecting in agents; directive: never read an exit code through a pipe. (2) I authorized the MCP step to touch `capture_note.py` for one field, which destroyed the file-disjointness making two W5 tracks safe to parallelise and forced a serialization — a scope grant is a concurrency decision. Four agent finales truncated mid-sentence (now the norm on heavyweight steps, not the exception); every one recovered from git + targeted pytest rather than re-running, and one had already completed the work it was narrating. A doc-engineer on haiku reported "no discrepancies found" while having invented an `intent: scratch` value and marked two closed enumerations open — on a file that ships inside the user's vault telling them what to type; spot-checking a confident all-clear is not optional. Also caught: a four-state test quietly narrowed to three when its assertion was "fixed" to match a broken fixture rather than the fixture fixed to match its name. Ledger: td-023 filed (the `log.md` title-laundering generalises to `curate_example`; not fixed generically because the right home, `records.py`, is frozen in the file-size ratchet at 947 lines). Deferred by decision: `[notes]` config (every key belongs to Phase 2's thresholds), the eval bridge, and the fuzzy rung. Known and accepted: the dashboard has no test harness at all — its gate is `tsc` plus a recorded manual render, which this time was genuinely performed against notes written by the real capture tool rather than asserted. |
| 2026-07-31 | notes-overlay Phase 2 verification + fixes (the recovery ladder and review loop, verified against all 14 ACs and then repaired) |  | Full | Full | recommended | Shape: Verification-only intake, no implementation planned: 7 dimensions fanned out over 6 agents (3 opus verifiers, 1 opus architect-validator, 1 sonnet test-engineer, 1 opus implementer for the geometry fix) + the dashboard render performed by the orchestrator directly. Baseline 2353 green, unchanged throughout. 10 findings; 6 fixed, 8 deliberately deferred with costs stated. 2 ADRs finalized (061, 062 — the second rewritten, not merely promoted), 2 amended. — **THE HEADLINE FINDING WAS THAT THE PRIOR PHASE'S HEADLINE FINDING WAS WRONG.** The retrospective reported the `fuzzy` rung unreachable at `guess_threshold=0.75` (ceiling ~0.64 across the edit-class matrix) and proposed lowering the threshold to 0.55-0.60. Two independent measurements refuted the premise: `fuzzy` fires at **0.9946** for a one-character typo — when the quote is ~a whole sentence. Reachability is governed by the quote's **shape**, not the edit size: `generate_candidates` capped windows at 2x `len(quote)` and never spanned a sentence, so `SequenceMatcher.ratio()=2M/(len(a)+len(b))` was structurally capped near 0.667 for a sub-span *regardless of edit size* (typo 0.639, heavy paraphrase 0.647 — indistinguishable), and multi-sentence quotes were unreachable at ANY threshold. The proposed fix would therefore have done nothing for the dominant real-world shape, because `note_capture`'s own description tells clients to send "the passage you displayed". The lesson generalises: a matrix that varies one axis (edit size) and holds another constant (quote shape) will confidently report a ceiling that belongs to the held axis. **Measuring one more axis was the entire difference between a config change and a correct fix.** ORCHESTRATOR ERROR, and it is the phase's own recurring trap biting the orchestrator: I wrote an agent brief asserting `pages_used` must be `(B,)` for a superseded page-A anchor — copied from the plan's Step 17 body — and ruling A7 supersedes that text, making `(A, B)` correct. The reviewer refused to comply and proved it. That is the THIRD person this phase caught by trusting a step body over the rulings that superseded it; the rulings are the only spec and the step bodies should have been struck through when superseded, not left readable. VERIFYING AGENT REPORTS PAID FOR ITSELF THREE TIMES, all in one direction — agents overstating: (1) a reported "unconditional" note-body default is gated on `intent == "question"`, exactly as specified; (2) a reported tool count of "31->33" (which would mean two new tools) was measured against `origin/main`, 32 commits stale — against local `main` it is 33->33 with zero `@mcp.tool` lines in the diff; (3) a reported contamination-pin failure did not reproduce — a second agent tested both candidate pins with the guard monkeypatched off and both failed correctly, so the pin has teeth. Retraction recorded alongside the findings; a verification that only ever propagates and never retracts is not independent either. THE SKIPPED GATE WAS WORTH EXACTLY WHAT IT COST. Step 34's manual render was skipped last phase; performing it found that the promote dialog — `INTERFACE_DESIGN`'s "only modal in the pane", the visible boundary crossing the whole opt-in design rests on — **never renders**: its seven CSS classes are defined nowhere, it is the only component using `modal-backdrop`, and it appends unstyled below the fold. `tsc --noEmit` and `vite build` both pass; a class name resolving to no rule is not a type error. It compounds: that invisible textbox is the informed-consent surface for a promoted note body that reaches OpenAlex verbatim. THE PRIOR SESSION'S RENDER MYSTERY HAD A MUNDANE CAUSE worth keeping: `App.tsx` defaults the MCP endpoint to an absolute `http://127.0.0.1:8765/mcp`, not to its own origin — so the HTML came from the scratch server and the *data* came from a live-vault server on another port. `curl`-verifying the server was sound and could never have caught it. The fix that made the render trustworthy was a **canary topic** (`canary-9f3c1e2`, a name that cannot exist in the live vault), which makes browser-side isolation falsifiable rather than asserted — isolation was then proven four ways before a browser was opened. Adopt the canary for any future render against a seeded vault. SCORE-AS-SENTINEL: rung 8 reported `guess_threshold - CLAMP_EPSILON` (0.740) whenever scoring never ran, and that value was surfaced as the drift queue's `overlap` — so a **deleted** passage rendered as "74% of the pinned passage survives" with a one-click re-anchor target, out-ranking a genuine near-match at 0.20 on the same page. Ruling L5 chose the value correctly for its stated purpose (satisfying L1's no-null rule) without considering that a human would read it as a confidence percentage. The general lesson: an invariant that demands *a* number does not endorse *that* number, and a sentinel becomes a lie the moment it crosses into a rendering layer. Four agent finales truncated (now routine on heavyweight steps); all four recovered from ground truth — `git status`, a scoped pytest, and checking whether the artifact existed — and one had genuinely finished while another was mid-fixture-rebracketing, which only the test run distinguished. A test-engineer left a probe file inside `tests/` where pytest would collect it; caught by reading `git status` rather than trusting its "tree is clean" report. DELIBERATELY NOT FIXED, with costs stated rather than silently dropped: the `_NOTES_DIRECTORY_TEMPLATE` x4 duplication (batching a pure refactor onto a risk-bearing geometry diff makes a bisect hard), AC-09's missing byte-level test (behaviour correct, guard absent — the highest-value item left), a tautological contamination assertion, a half-populated `traceability.yml`, and the hardcoded dashboard port. |
| 2026-08-05 | sentinel remediation: 5 important findings + DESIGN.md §3 refinement + topology expansion |  | Standard | Standard | recommended | Shape: 6 agent spawns (2 systems-architect, 3 test-engineer, 1 sentinel) across two dependency-ordered waves; 23 files; DESIGN.md §3 18→23 Built rows; topology 11→14 groups; suite 2443→2461. — correct. Three lessons. (1) VERIFY AGENT FINDINGS BEFORE ACTING: sentinel reported 22 one-directional `re_affirms`; the measured figure was 8, and its cited example (dec-025) already carried both back-refs. Acting on the report unverified would have meant hunting 14 defects that did not exist. (2) A FIX CAN EXPOSE THE NEXT DEFECT: quoting the two unparseable ADR scalars would have leaked literal quotes into dec-018's index row, because `regenerate_adr_index.py` never unquoted — caught only by diffing a regeneration before trusting it. (3) GATES EARN THEIR KEEP IMMEDIATELY: the completeness cross-check added to `test_group.py --check` caught the 3 missing group blocks on its first real use, and the migrated calibration header surfaced 50 uncalibrated commits the dead gate had been swallowing. Also: one subagent return truncated mid-sentence with no terminal marker; re-deriving from ground truth (gate green, 14 blocks present) showed the work was done but TEST_RESULTS.md was never written — the handshake rule paid for itself. |
| 2026-08-05 | second sentinel audit: the architecture-doc gate + td-038/039/040 | 18 files; 5 behaviours; new gate wired into `make verify`; 2 ADR drafts | Full | Standard | user-override — the user supplied the ordered decomposition at intake (gate before fix, code before docs), which removed the research/architecture/planning phases Full would have added; no agent fan-out | correct. Shape: orchestrator-executed batch in the declared order, `make verify` green at 2534 (from 2520). — **THE ORDERING DIRECTIVE WAS THE WHOLE VALUE AND IT IS WORTH RESTATING.** Writing `scripts/check_architecture_coverage.py` BEFORE fixing td-038 meant its first run reported 16 findings against real drift; written after, it would have passed on run one and taught nothing. It also caught a false positive in itself that only a real corpus surfaces — a glob citation (`src/knotica/core/loop*.py`) truncated at the `*` and reported as dangling. Neither would have been visible against an already-clean tree. **THE GATE THE FINDING ASKED FOR WAS UNBUILDABLE, AND MEASURING IS WHAT SHOWED IT.** 'Every module named in a doc' flags 60 modules today; closing that reverses the residual decision the user explicitly fenced off (td-032's five-way `core/` decomposition). Relaxing to bare-stem matching drops it to 25 — and reports `core/topics.py` as *covered* because the word `topics` appears as a JSON key, i.e. it would report success on the exact module the finding is about. Per-package counts were chosen instead: exact integers, no false-positive surface, and they catch the actual mechanism (a module arriving unrecorded) even inside a residual row. Three pre-existing prose counts (`okf/` 11, `guillotine/` 9, `service/` 3) already matched the tree under an includes-`__init__.py` reading, so the convention was adopted rather than invented — then those three prose counts were deleted, because leaving ungated numeric claims in the file whose lesson is 'the ungated claim lags' would be self-refuting. **TWO OF THE SENTINEL'S THREE REMEDIES WERE WRONG, AND ONE WOULD HAVE BROKEN THE BUILD.** td-040 said to 'delete the now-unused `_is_directory`' — it has a second caller (`_check_reserved_names`) that needs the complement of `is_topic`. It also called the ordering divergence 'live'; `VaultStore.list_dir` is contractually sorted, so there was no observable difference at any call site and the defect was pure duplication. Reading the code before acting on a finding is now twice-vindicated: the prior calibration row records the same lesson about a 3x-inflated count. The corrections are written into the ledger rows rather than left in this log. **A FIX'S BLAST RADIUS IS BIGGER THAN ITS FINDING.** Widening §3's loop row for td-038 desynchronised the loop-sibling list at three sites in `TEST_TOPOLOGY.md` — including one across the systems-architect/test-engineer ownership boundary — and TT01 would have stopped resolving had only §3 moved. Adding one test file moved five published counts. Both were caught by re-deriving from the tree (TT01 re-verified 23/23 1:1; `pytest --collect-only` for every count), never by adjusting a number to taste. **THE NEW GATE'S FIRST REAL CATCH WAS THE SIBLING GATE CATCHING ME**: `check_adr_health.py` rejected one of the two ADRs written this pass, because a `summary:` beginning with a backtick is a YAML reserved indicator. A gate built three days ago failing an artifact authored by the person who wired it in is the cleanest evidence available that these checks are not ceremonial. |
| 2026-08-05 | arch-doc-cleanup (DESIGN.md + docs/architecture.md + ADRs + tech debt) |  | Full | Full | recommended | Shape: 9-agent parallel drift audit (232 findings) → wholesale rewrite of both architecture documents → ADR/ledger/topology reconciliation → 3 adversarial verifier rounds (FAIL 8 → FAIL 2+3 → PASS WITH FINDINGS). Docs 1049 → ~590 lines, almost all of it deleted duplication and document-maintenance archaeology. Tier fit: the fan-out was load-bearing — the docs were frozen at the P-B consolidation and drift was invisible to every existing gate (sentinel graded them B/B while §§5-8 of the guide did not exist, because AC01-AC12 check content and no section-structure invariant). Two structural side effects that only surfaced mid-pass: merging §3a into the gated inventory made the package root a "component" and would have obliged a test group for an `__init__.py` (reverted); and the two-tier §3 orphaned six TEST_TOPOLOGY subsystems, which the topology's own Note 1 predicts ("§3 refinement pass first, a topology edit second") — realized as dec-075. — RETROSPECTIVE: every failure across all four passes was one shape, *asserting a property adjacent to the one actually verified*. Original fragments (a `grep -l` count read as a transaction count), the rewrite ("dependency-light leaves" after measuring only which modules import), each remediation round (changed the model's count, did not grep the ADR sentence stating it), and finally the tooling (a batch script died partway; two edits never ran and were reported from partial output). Durable rule: when a claim contains a number or the word "only", re-derive it from the source at the moment of writing — never from the artifact that prompted the fix; and when a batch edits N durable artifacts, verify N, not the batch. |
| 2026-08-05 | versioning + release mechanism (commitizen, uv provider, CI-dispatched workflow) | 8 files; 2 behaviours (bump/release + the consistency gate); tooling decision worth an ADR | Standard | Standard | recommended | correct. Shape: no agent fan-out (session directive) — orchestrator worked directly through plan mode; 4 knotica commits + 1 upstream issue. **Reading the tool's source beat reading its docs.** Commitizen's published `bump` page answered none of the four questions the design turned on (version_files matching semantics, hook ordering, commit staging, provider inventory); ~15 min in site-packages plus a scratch-repo rehearsal on the real `pyproject.toml`/`uv.lock` answered all four and surfaced the `uv` version provider, which deleted an entire planned `pre_bump_hooks` mechanism. The decisive constraint (`uv.lock` records the project's own version, so a bump breaks `uv sync --locked` on the release commit) was found by *running* `uv lock --check` against a mutated copy, not by reasoning about it — a design built on the docs alone would have shipped a release that reddens `main` every time. Two hazards the skill's current text would have walked into: `version_files` replacement is `line.replace(current, new)`, so a naive `uv.lock:^version = ` entry silently rewrites a dependency pinned at `10.1.0`; and rehearsing the whole bump in a throwaway clone is cheap enough that there is no reason to first exercise a release mechanism in production. |
| 2026-08-06 | temp-dir leak: clear 25.7k leftover clones + fix the test-fixture cause | 1 file changed (tests/conftest.py); 1 behaviour; no architectural decision | Direct | Direct | recommended | correct. Shape: no agents (session directive) — orchestrator worked directly; cleanup (25,663 dirs) + a 33-line conftest fixture; `make verify` green (2711 passed). Cause: `tempfile.mkdtemp` has no cleanup contract, and the loop tests call it inside the injected `_evaluate` closure whose signature is fixed by `EvaluateFn`, so no pytest fixture is in scope there; 81 `_fake_evaluate` references made threading `tmp_path` through infeasible. A session-scoped autouse fixture repointing `tempfile.tempdir` at `tmp_path_factory.getbasetemp()` fixed all 16 call sites at once and holds for tests not yet written — same redirect-the-global tactic `isolated_home` already uses for HOME. Side effect worth knowing: the suite had been writing the developer's REAL judge cache, since `evals.harness` roots it at `tempfile.gettempdir()`; now isolated per session. Gotcha — verifying cost far more than fixing: the first post-fix count showed 11,657 dirs and looked like a total failure. It was two confounders, neither related to the change: a still-running background `rm -rf` draining the pre-existing backlog, and a sibling project's rapid pytest loop reaping knotica's basetemp (pytest keeps only the last 3 numbered dirs in the SHARED `pytest-of-<user>/` root, so another repo's test runs delete yours). Ground truth came from mtimes and a one-off probe test, never from counts. Second premise corrected mid-task: `harness.py`'s un-deleted eval clone reads like a missing `finally: rmtree`, but `EvalRunResult.clone_root` is documented as letting a caller point a human at the clone — retention is contractual, so that one needs a retention policy, not a cleanup call. Left open deliberately rather than "fixed" into a contract break. |
| 2026-08-27 | process-swimlanes M1 | Full | Full (as planned) | 23 steps, 6 agents-in-parallel peak, 5 tier-H reviews | Match | RED-first pairing held on every step; 3 review catches (id-citations, argv-shim edge, a description falsely promising two-phase gating on spend); truncation-resume pattern needed on 7 of 16 agent runs | dec-087..099 executed; baseline floors recalibrated per escape hatch |
| 2026-08-27 | process-swimlanes M2-M5 (continuation, close-out at Step 123) | Full | Full (as planned) | 78 steps (46-123: M2 read-only projections, M3 Improve+Tend, M4 Learn/Answer/Fill+handoff, M5 Home); 15 `tier: H` steps, 3+ `review: force` intra-step reviews (Step 79 deletion, Step 115 default-pane repoint), 11 `VERIFICATION_REPORT_*.md` fragments | Match | RED-first pairing and the census-early technique (M3's post-hoc census caught a miss only by luck; M4/M5 wrote theirs first) both held for the whole continuation; one traceability-merge crash on an unhashable M5 fragment value, recovered by reconstructing REQ-21c's matrix row from `LEARNINGS.md`/`TEST_RESULTS.md` prose rather than the YAML — durable rule: a merge script over agent fragments needs a type guard on every dict value before hashing, not just the top-level shape | dec-088..101 executed (plus dec-090's re-affirmation of dec-050, closing AC-C4); all 18 acceptance criteria MET at close-out; archived at `.ai-state/specs/SPEC_process-swimlanes_2026-08-27.md` |
