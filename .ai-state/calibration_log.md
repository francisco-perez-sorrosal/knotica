# Calibration Log

One row per completed task: tier chosen at intake vs what the work actually needed.

| Date | Task | Tier | Right call? | Actual shape | Retrospective |
|------|------|------|-------------|--------------|---------------|
| 2026-07-16 | eval-harness (PRE_PLAN Phase 2) | Standard | Yes | 33 planned steps + ~8 user-directed live-run additions; 11 new modules + CLI; 963 tests (baseline 689); 2 researchers, architect (+1 revision), planner, 16 impl/test pairs, 2 fable light-reviews, exhaustive fable verification (FAIL→rework→PASS-with-findings) | Tier fit well: the characterization-test-first gate and pre-mortem fold-ins each caught real issues (all five pre-mortem guards ended up exercised — live-vault guard, key-leak sweeps, spend ceiling fired live, instrument-drift warning, num_threads pin). Live first-runs surfaced 4 defect classes no offline test could (orphaned method via class-body dedent, raw SDK tracebacks, schema-valid-but-garbage citations → ENAMETOOLONG, mis-calibrated spend default) — budget a live-shakedown step in future instrument-building plans. Four subagent return truncations recovered via ground-truth handshake (RECOVERY_LOG.md); truncation rate suggests keeping heavyweight steps' final report short. Phase-2 criterion met: scalar 0.5707 reproduced bit-for-bit on frozen corpus git:599237b. |
| 2026-07-18 | autoresearch-gapfill research spike | Spike | Yes | 3 researchers (providers/internal/prior-art) + promethean in parallel; synthesis in .ai-work/autoresearch-gapfill/SYNTHESIS.md | Lens-independent fan-out reconciled cleanly; internal lens found the anticipated-but-unwired held_out_delta placeholder and the discarded retrieval trace — substrate gaps that reshaped the plan from "add search" to "add diagnostics first". Decision: layered spine P0-P4; implementation pending user green-light. |
| 2026-07-18 | gapfill-substrate (P0 diagnostic substrate) | Standard | Yes | 16 steps; architect (opus) + planner + 3 impl/test pairs + 2 forced light-reviews (both PASS) + opus verifier (PASS WITH FINDINGS); 1143 tests green (+16); live vault reconciled + gen-4 proof run 0.9655 | Architect Register Objection corrected a wrong brief assumption (no fingerprint rotation) before it cost anything — the checklist-driven brief+objection loop earned its overhead. Live proof surfaced two ops gotchas no test could: loop correctly refuses bookkeeping-only commits (Step 16 needed direct eval, not loop --once), and credential limits (API spend cap + OAuth window) dominated wall-clock; SDK retry budget raised in response (td-007 resolved). |
| 2026-07-19 | gapfill-classifier (P1 four-way fault cascade + loop hook) | Standard | Yes | 14 steps; opus architect + planner + 2 impl/test pairs + 2 forced light-reviews (both revise->closed) + opus verifier PASS-WITH-FINDINGS; 52 scoped tests; suite 1300 | Light-review caught a fabricated-fixture defect the pair missed: the war-story "dilution" test invented trace displacement absent from the real manifests — ground-truth diff (0/25 ids moved) reversed the folklore (regression was generation-fault). Honest-fixture discipline > plausible narrative; the reversal reshapes P3 planning. Ledger: td-008 (loop.py size). |
| 2026-07-19 | gapfill-discovery (P2 provider protocol + youcom/openalex + service) | Standard | Yes | 31 steps (3 skipped: Exa cut by user directive mid-pipeline); opus architect + planner + 3 impl/test pairs + 2 forced light-reviews + opus verifier PASS-WITH-FINDINGS; 114 discovery tests; no new deps | Two wave-3 agents returned truncated (recurring pattern on heavyweight finales — keep final reports short); ground-truth re-derivation + one DI-seam reconciliation recovered cleanly, cheaper than re-runs. Mid-pipeline scope cuts (Exa) propagate well via WIP strike-through + prompt updates. Live wire-shape verification deferred to user (needs KNOTICA_YOUCOM_API_KEY). |
| 2026-07-19 | gapfill-queue (P3 suggestion queue + approval surface) | Standard | Yes | 23 steps; opus architect + opus interface-designer shadow (no challenge loop needed) + planner + 3 impl/test pairs + dashboard step + doc-engineer + 3 light-reviews (2 revise->closed) + opus verifier PASS-WITH-FINDINGS; suite 1381 | Interface-designer shadowing paid for itself: the surface schemas were implemented byte-faithfully from INTERFACE_DESIGN with zero rework, and the architect/interface split (structure vs surface) reconciled cleanly via one orchestrator ruling. Recurring failure mode: pipeline-id citations in test docstrings slipped through a FOURTH time despite prompt warnings — the commit gate + light-reviews caught all instances, but the pattern suggests adding it to the test-engineer agent definition, not per-prompt reminders. Truncated finales again on the two heaviest agents; ground-truth recovery now routine (~2 one-line fixes). |
| 2026-07-19 | gapfill-nl-gaps (added-id classification + gap_report + retracted channel + provenance surfaces) | Lightweight+ | Yes | Orchestrator-authored ADR drafts + 3 impl/test pairs + 2 light-reviews (accept, accept) — no architect/planner spawns; 1427 suite | Orchestrator-as-designer worked at this tier because every house pattern was established upstream; the four-incident id-citation streak ended once the warning moved to CAPS with history attached. Guillotine examination flipped my initial surface judgment (its verdicts never delete pages) — evidence-first adjudication beat pattern-matching. |
| 2026-07-19 | gapfill-coherence (cross-spine audit + guillotine refactor + prioritization fix) | Standard | Yes | Opus architect coherence analysis + cross-spine integration test + refactor pair + 2 light-reviews (accept) + haiku doc sweep; 1430 suite | The coherence pass earned its cost: found one systemic HIGH (origin-blind prioritization — three sites burying reported/retracted gaps) invisible to per-pipeline verifiers, and adjudicated guillotine with evidence (demo-era rewriting machinery deleted; client-as-brain restored; frozen eval unharmed). Integration test green on first full run = the per-pipeline contract discipline (frozen consumer contracts + forced schema reviews) actually prevented seam drift. |
| 2026-07-19 | gapfill-source-gate (P4 candidate_kind gate + worktree ingest + dataset upgrade) | Standard | Yes | 35 steps, 13 parallel groups; opus architect + opus interface-designer shadow (challenge loop: 1 round, AC-1 pre-resolved) + planner + 6 impl/test pairs + haiku doc steps + 3 forced light-reviews (PASS, PASS-warn, FAIL->revise->closed) + opus verifier READY-WITH-WARNS (2 one-line fixes applied); suite 1505 | Test-engineer full-cycle discipline (drive poll_once, not module internals) caught two production defects unit tests would miss: the for-each-ref single-star glob that made every nested candidate branch invisible, and a grower wiring that fired a real metered API call from an existing test. The FAIL light-review found a silent wrong-branch commit path (checkout-restore swallowed) — and the implementer's revise correctly rejected half the suggested fix (loop-state write would itself misroute), evidence the review loop argues instead of complying. Convergent GREEN-first pattern now documented per-agent instead of objected-to case-by-case. |
| 2026-07-21 | loop-consolidation (P-A internals refactor + P-B two-tier tool surface + P-C conversational routing + P-D service lifecycle) | Full | Yes | 55 planned steps + emergent 11b; worktree pipeline; 3 lens-independent researchers + opus interface-designer shadow (2 challenges, both ruled) + opus architect (+2 scoped re-entries: mid-flight user guidance, lock-scope ruling) + planner + ~20 impl/test spawns + 7 forced light-reviews (5 accept, 2 revise->closed) + opus verifier FAIL->fixes->PASS; suite 1417->1653 fast + 20 slow; 9 ADR drafts | The pre-mortem earned its keep twice: the flock-contention exercise it mandated surfaced a real pre-existing lock-scope gap (flock covered only the state commit, not the merge span — ~60% corruption under daemon-style contention) BEFORE the daemon shipped, and the no-auto-install hard rule caught a def-time subprocess.run default that would have silently defeated the mock net. Worktree cut from stale origin/main cost one researcher rework (agents self-corrected via git show main:). Truncated agent finales now ~1 per heavy agent; resume-with-punch-list is routine and cheap. Verifier FAIL was the fifth id-citation incident (draft-ADR ids in docstrings this time) — the pattern needs a pre-commit grep, not more prompt warnings. td-008 premise inverted: extractions moved code out but helpers/spans moved in (1096->1221); honest ledger row kept in-flight. |
| 2026-07-21 | daemon-env-bootstrap (service entry loads config-dir .env) | Direct | Yes | Single-behavior fix + 5 tests; 48 service-suite green | First live-install walkthrough caught what no pipeline test could: the no-live-service pre-mortem rule that protected the pipeline also guaranteed the launchd-empty-environment gap (process-env-only LLM credential) stayed invisible until a human actually installed. Step-by-step-with-verification surfaced it at step 1. |
| 2026-07-21 | ingest-merge-remnant-heal (heal-on-acquire for plain ops + daemon logging + hermetic char test) | Direct | Yes | 3 root-cause fixes + 4 tests; suite 1740 fast + contention green | Second live-E2E catch in one evening: the 11b heal was span-scoped so a daemon that died mid-merge bricked every plain ingest until hand-aborted; and the empty log dir meant launchd discarded the daemon's entire trail. Live use finds the seams between verified components — both fixes were sub-hour once ground truth (MERGE_HEAD + empty logs + launchctl state) was read directly instead of trusting the in-conversation diagnosis. Also: a user provisioning real keys flipped a non-hermetic characterization snapshot — env isolation belongs in every config-sensitive test, not just the suite that first needed it. |
| 2026-07-22 | eval-cadence-model-config (configurable eval cadence + td-011 fix + per-task model config + billed-trigger spend gate) | Standard | Yes | 31 planned steps + 3 user-directed mid-flight insertions (Step 8b failure-retry floor, Step 24b run_once gate collapse + cross-kind nonce test); worktree pipeline; 2 lens-independent researchers, opus architect (2 ADR drafts), planner, ~14 impl/test spawns, 6 forced light-reviews (4 accept-clean, 2 accept-with-FAIL-then-revise-then-PASS), haiku doc pass + 2 orchestrator doc-gap fixes; suite 1652->1711 fast + 20 slow | Forced review earned its cost twice on the two riskiest steps: the failure-retry floor's first cut gated on a global `pending_retry` flag with no same-content check — would have held unrelated new content hostage behind an unrelated prior failure — caught before merge, not in production. The billed-trigger review went further than the plan asked (user pushed for adversarial depth given real spend risk) and found the pre-existing, undeclared `run_once` MCP action was a second, ungated billing surface identical in kind to the one just gated — closed by reusing (not duplicating) the nonce infrastructure, then proven immune to cross-kind nonce redemption in both directions. Test-count discrepancies (25 vs 31 P-A files) resurfaced twice — once from the planner re-deriving instead of trusting orchestrator ground truth, once as legitimate marker-selection variance across parallel suite runs — worth a standing reminder that "run it yourself" beats re-trusting a prior count. One pre-existing test-hermeticity gap (hardcoded live-vault path, silent no-op on other machines) found during the final proof, filed as td-012, correctly left unfixed as out of declared scope. |
| 2026-07-22 | deprecated-alias-removal (mid-flight scope addition to eval-cadence-model-config: delete all 26 flat-tool aliases + telemetry) | Standard (folded into in-flight pipeline) | Yes | 1 implementer (removal, 2 truncation-recovery resumes) + 1 supersession ADR (dec-045 partial supersession) + 1 cleanup pass (28+2 obsolete parity tests across 7 files, 1 genuine-bug fix in test_mcp_source_ingest.py); suite 1711->1676 (net fewer tests: alias-comparison tests removed, no coverage lost — dispatcher behavior covered elsewhere) | User-driven simplification once premise was checked: the 'keep aliases for one release cycle' policy (dec-045) assumed external consumers needing gradual migration, but knotica has none (self-only, Claude Desktop/Code under the user's own control) — removing dead-weight backward-compat was the right call once surfaced, not scope creep. Two implementer completions truncated without a terminal marker on this wide-footprint (15+ file) task — resumed from transcript both times rather than restarting, which worked, but ground-truth verification (git status, grep, direct suite runs) caught both gaps the agent's own prose missed: an ADR that didn't exist yet cited by id in docs, and 30 parity tests whose entire premise (alias output == dispatcher output) evaporated with the alias. Lesson: wide-footprint mechanical edits need the completion handshake checked against grep/git status, not just trusted, even more than usual. |
| 2026-07-22 | loop-py-extraction (td-008 partial resolution: extract build_loop_runner/arena-race/candidate-gate clusters) | Standard | Yes | 1 researcher (opus-tier line-count math correcting the ledger's own optimistic estimate), 1 opus architect (3 sibling modules, source_gate.py-idiom for candidate-gate), 1 planner, 4 impl/test spawns (1 characterization backfill + 3 extractions), 2 forced light-reviews (both PASS on first pass); suite 1680 fast + 20 slow throughout | Research corrected a real process failure mode before it repeated a third time: the ledger's "next move" note named the same two extraction targets a prior pass had already *introduced* (not shrunk), and blindly following it would have produced a third overclaimed-resolution ledger entry. Measuring the actual arithmetic before committing to scope, and getting explicit user sign-off on a deliberately partial (low-risk-only) extraction rather than reaching for the higher-risk observe_default cluster in the same pass as its own recent adversarial review, kept this pass honest and low-risk. Three tests retargeted an obsolete monkeypatch path (knotica.core.loop.race_variants -> knotica.core.arena_resolve.race_variants: test_best_effort_characterization.py, test_loop_runner.py, test_source_gate.py) — only the first is one of the five off-limits P-A files, and its diff is a byte-identical single-line target swap with zero assertion change. The orchestrator's own blanket "don't touch P-A files" instruction had been copied from a different pipeline's context without checking fit; caught, reasoned through, and disclosed rather than silently overridden or blindly obeyed. The final verifier pass (opus) flagged that this calibration entry originally undercounted the deviation as "one file" rather than three — corrected here. Architect's ~1087-line estimate landed exactly on the measured result. |
| 2026-07-22 | knotica-init-mcp-from-detection-fix (Direct) | Direct | Yes | Single-file fix + 2 tests; suite 1784 fast green | Found live, mid-walkthrough: knotica init --desktop's local-checkout auto-detection (_mcp_from_source) walked only __file__'s parents, which resolves into the isolated uv-tool venv (not the repo) when invoked via the globally uv tool install'd binary -- exactly the sequence README's own Channel 2 documents. Silently fell back to the bare, unpublished package name "knotica", breaking uvx pre-warm and (via the same resolver) the Desktop MCP command args. Added a cwd-based fallback probe alongside the __file__ probe; verified end-to-end against the real Desktop config (uv run --directory <repo> ... replaces the broken uvx --from knotica ...) with no env-var workaround needed. Caught only because we deliberately ran the full documented install sequence after merging, rather than trusting the MCP server would "just restart cleanly." |
