# Resolved Tech Debt

<!-- Sibling of TECH_DEBT_LEDGER.md holding rows with terminal status (resolved / wontfix).
     Rows arrive via scripts/finalize_tech_debt_ledger.py migration when status transitions
     to a terminal value. Schema, lifecycle, and re-open semantics are defined canonically
     in skills/software-planning/references/tech-debt-ledger.md. Do not duplicate
     the schema here — the skill reference is the single source of truth. -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § Schema for field definitions.

**Rows arrive here automatically** when status transitions to `resolved` or `wontfix`. The pair forms one logical namespace with `TECH_DEBT_LEDGER.md`: `id` and `dedup_key` are unique across both files. Cross-file `dedup_key` matches trigger re-open (the historical resolved row moves back to LEDGER).

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-041 | suggested | correctness | code-to-goals | src/knotica/mcp_server/tools_write.py, src/knotica/core/operations/write_page.py, src/knotica/core/operations/store_source.py | architecture | INTERFACE_DESIGN section 1.4 typed error envelope | orchestrator | 2026-08-05 | 2026-08-05 | implementer | resolved | 2026-08-05 orchestrator added `envelope.map_write_exception` and guarded `tools_write._write` | RESOLVED 2026-08-05 (orchestrator). ROOT CAUSE WAS STRUCTURAL, NOT PER-TOOL: the read path has `envelope.map_read_exception` (map the typed exceptions, re-raise anything else as a genuine bug); the write path had NO equivalent. `tools_write._write` rendered the envelopes its operations *returned* and called `operation(...)` unguarded, so anything an operation *raised* went straight out of the tool and FastMCP stringified it. The split was therefore by EXCEPTION TYPE, not by tool -- the same `write_page` answered a missing topic with a typed `TOPIC_NOT_FOUND` (returned) and an empty one with raw text (raised). CHARACTERIZED BEFORE CHANGING, and the measurement widened the row: the escaping class is not just the empty topic it was filed for. Eight cases were driven through the in-memory MCP session; FOUR were raw, not two -- `write_page` with an empty AND a path-bearing topic, `store_source` with an empty topic AND an empty citation_key. All four are the argument-validation guards in `core/page.py`, `core/schema.py`, and `operations/store_source.py`, every one of which raises a bare `ValueError`. FIX: `envelope.map_write_exception`, the write-side counterpart, wired into `_write`. `KnoticaError` keeps its own code; `ValueError` -- the argument-validation signal across `core` -- becomes `INVALID_ARGUMENT`, whose published fix ('Correct the named argument and call again') is exactly the remediation the raw string could not carry; anything else is RE-RAISED, matching the read side, so a `KeyError` in the mutation core stays a crash rather than becoming polite advice that cannot help. REJECTED ALTERNATIVE, and the grounding overturned my own earlier recommendation: raising a typed `KnoticaError` at the four core guards. It reads better in the abstract -- put the type where the knowledge is -- but the codebase says otherwise. `ValueError` from those helpers is already CAUGHT AND TRANSLATED at call sites (`vault_metadata_tree.py:35` -> `TopicNotFoundError`, `capture_note.py:204`), and 29 test files assert `pytest.raises(ValueError)`. Retyping the core would ripple through all of them to fix a defect that is purely adapter-shaped. `envelope.py`'s own docstring places this mapping at the adapter, and that is where it went. ORDERING HAZARD RECORDED IN THE MAPPER: `InvalidCursorError` subclasses `ValueError`, so a future branch for it must sit ABOVE the `ValueError` one or it renders as `INVALID_ARGUMENT`. No write path takes a cursor today, so no such branch was added -- the constraint is documented instead of speculatively coded. NON-REGRESSION IS THE LOAD-BEARING EVIDENCE: the four cases that were ALREADY typed (`RESERVED_NAME` x2, `TOPIC_NOT_FOUND` x2) are byte-unchanged, and `write_page`'s operational `TOPIC_NOT_FOUND` is asserted in the same parametrized test as its argument-validation `INVALID_ARGUMENT` -- so the test proves the fix narrowed to what escaped rather than flattening every error onto one code. Reverting the `_write` guard fails exactly the four raw cases and passes the three typed ones. | mutating-tools-bypass-typed-error-envelope |
| td-025 | suggested | other | code-to-goals | src/knotica/core/notes/candidates.py | architecture | PRE_PLAN - anchor recovery must not depend on where a passage sits on a page | orchestrator | 2026-07-31 | 2026-08-05 | implementer | resolved | 2026-08-05 orchestrator bounded `_block_starts` on both heading edges in `src/knotica/core/notes/candidates.py` | RESOLVED 2026-08-05 (orchestrator). THE ROW'S PREMISE HAD GONE STALE AND ITS REMEDY NO LONGER EXISTED: it prescribed 'a `_nearest_block_end` mirroring `_nearest_block_start`', and neither helper is in the tree -- `candidates.py` was rewritten twice after this row was filed (`b20df2c`, then `29b2bd8` for `dec-062`'s sub-span alignment), replacing the block-boundary approach with a sentence-bounded window plus `_clip_to_cap`. So the fix was re-derived by MEASUREMENT rather than applied as sketched. MEASURED, not assumed: all four shapes the row names as its trigger (unpunctuated fragment before closing chrome; heading with a preceding blank line; list item; table cell) were driven through `generate_candidates` against a page whose next section starts at a known offset. THREE OF FOUR WERE ALREADY BOUNDED by the rewrite. ONE WAS NOT: a heading with **no** preceding blank line -- the window ran to 76 where the chrome starts at 63, straight through `## References` into the next section. ROOT CAUSE, and it is a code-vs-docstring mismatch rather than a judgment call: `_block_starts` recorded only `match.end()` for every boundary match. For a blank line that is right; for a heading it records the edge *after* the heading only, so a heading never ended the block *preceding* it. The module docstring meanwhile claims a heading 'bounds the extension on *both* sides' and that the forward bound exists so 'absorbing neighbouring sentences must not become a licence to absorb the next section'. The code did not do what its own docstring said, conditionally on a blank line happening to be there. FIX: a heading match now contributes both edges (4 lines). All four shapes bounded; 316 notes-overlay tests unchanged, so behaviour is preserved everywhere it was already correct. REGRESSION GUARD: `test_a_heading_bounds_the_block_before_it_even_with_no_blank_line_between`, proven to bite by reverting the fix and watching it fail. USER IMPACT, since this row is one of only two with any: a note anchored to an unpunctuated passage -- a heading-like fragment, a list item, a table cell -- immediately followed by a section heading was scored against a window containing the next section's chrome, depressing its similarity and orphaning it when it should have resolved. Same failure mode as the backward-side defect this row was filed alongside, which was measured at 0.43 vs 0.72 for the same edit. | candidate-window-forward-extension-unbounded |
| td-037 | suggested | test-coverage | goals-to-code | .ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md | spec-req | SPEC_wiki-mvp-core | orchestrator | 2026-08-05 | 2026-08-05 | test-engineer | resolved | 2026-08-05 orchestrator closed all 8 gaps across 6 test files; spec matrix and coverage summary updated | RESOLVED 2026-08-05 (orchestrator). All eight REQ clauses now carry assertions; the spec's §Traceability matrix and coverage summary are updated (COVERED 31 -> 39, PARTIAL 7 -> 0, UNTESTED 1 -> 0). Every gap was RE-VERIFIED as still open before anything was written -- the matrix was a day old and td-025 had just proven a row can go stale under its own subject. All eight were still real. WHAT LANDED: REQ-PLUGIN-01 -> new `tests/test_plugin_manifest.py` pinning the launch line argument-for-argument, the single-server declaration, and `alwaysLoad`'s absence over raw bytes (`.mcp.json` had NO test of any kind). REQ-TOOL-05 -> all four mutating tools x {empty, whitespace} refused with no commit. REQ-CLI-07 -> curated / last-lint / unpushed asserted non-vacuously against a real curated example, a real lint log entry, and a real unpushed commit against a real upstream. REQ-CLI-06 -> LLM-freedom pinned structurally in a fresh interpreter, plus the host-dependent `mcp` row's presence pinned separately from its verdict. REQ-TOOL-02 -> the never-locks clause made behavioural for `core` (lint runs with the lock held; `vault_lock` is NOT re-entrant -- verified, a second acquire raises `LockBusyError` -- so an acquiring implementation would fail). REQ-REC-03 -> the constitution is read back and its five numbered record sections asserted. REQ-PROMPT-02 -> citation discipline and the grounding clause. REQ-PLUGIN-02 -> the pre-warm command, its `--from "$ROOT"` target, subshell backgrounding, and stdin/stdout detachment. TWO THINGS THIS ROW DOES NOT DISCHARGE, both recorded rather than absorbed. (1) REQ-PROMPT-02's `read -> act -> update index -> log` ORDERING is still unasserted as a sequence; the matrix row says so and the summary repeats it, because a coverage table reading `0 PARTIAL` is exactly where a reader stops looking. (2) Writing the REQ-TOOL-05 assertion SURFACED A NEW DEFECT -- the four mutating tools refuse an empty topic in three different shapes, two typed envelopes and two raw exception strings. Filed as td-041 rather than frozen into a per-tool code assertion, which would have turned the divergence into a pinned contract. | spec-wiki-mvp-core-uncovered-reqs |
| td-032 | important | drift | goals-to-code | src/knotica/okf/, src/knotica/guillotine/, src/knotica/service/, src/knotica/dashboard/, .ai-state/DESIGN.md | architecture | .ai-state/DESIGN.md 3. Components | sentinel | 2026-08-04 | 2026-08-05 | systems-architect | resolved | 2026-08-04 systems-architect landed the §3/architecture.md pass (`dec-070`); 2026-08-05 the topology refresh created the three groups (`dec-069` binding satisfied); closed 2026-08-05 by orchestrator after re-deriving every consequence from the tree | RESOLVED 2026-08-05 (orchestrator). Both halves are done and each consequence was re-derived from the live tree rather than taken from the account above. (a) CLOSED -- 14 groups, and `okf-conformance` / `guillotine-audit` / `service-lifecycle` all exist. TT01 measured by parsing all 14 fenced YAML blocks against §3's `Status: Built` rows: **23 subsystems / 23 Built rows, 0 unresolved, 0 uncovered** -- exactly 1:1, which also discharges surfaced-finding (2), the sequenced TT01 red window this row predicted. The 11 files / 128 tests are grouped; `TEST_TOPOLOGY.md`'s own §3-gap section reads `0 files`. (b) and (d) CLOSED -- `grep -c` per package across both documents: okf 21/12, guillotine 18/12, service 19/16, dashboard 25/23, where the original finding measured **zero in both** for three of the four. A systems-architect brownfield baseline no longer starts from a document that does not know these packages exist. (c) PARTLY, AND DELIBERATELY -- `query-compile` now carries 2 anchored subsystems, so Note 2's eight-module subtraction rests on a real §3 row instead of on nothing, which is the specific expectation this row recorded. `vault-semantics` remains a large residual over `src/knotica/core/`; shrinking it further means decomposing the `core/` row, which was weighed and declined on proportionality grounds and is recorded as the steelmanned runner-up in `dec-070`. Not carried forward as debt -- a decided trade-off is not an open row. SURFACED FINDING (1) CLOSED ELSEWHERE, BY DELETION: `guillotine/report.py::write_artifacts` no longer exists. `td-036` removed it in `e3c4ca8` (zero callers, so removal beat routing) and widened the mutating-store scan from a package name-list to the literal DESIGN.md rule, codebase-wide -- closing the unguarded counter-example and the blind spot that hid it in one commit. ONE DEFECT FOUND WHILE CLOSING, NOW FIXED: `.ai-state/DESIGN.md` still described that exception as **live** in two places -- the `guillotine/` row's "One live exception, recorded rather than assumed away" passage and the layer-position table's "One unguarded exception ... sits outside every current scan" cell -- eight commits after the function was deleted. Both now describe the resolved state. Worth naming because it is td-038's class in td-038's own document and the new `check_architecture_coverage.py` does **not** catch it: that gate checks package counts and path citations, so prose asserting a *function* exists is outside its reach, exactly as its docstring says. A closing re-read found it; no gate would have. | fd02643fd7b4 |
| td-031 | important | correctness | code-to-goals | src/knotica/core/loop.py | architecture | PRE_PLAN - one git commit per mutating vault operation | orchestrator | 2026-08-02 | 2026-08-02 | implementer | resolved | 2026-08-05 implementer added `src/knotica/core/loop_attempt.py`; ADR dec-072 | RESOLVED 2026-08-05 (implementer, task `sentinel-remediation`): the FAMILY is closed, not just the dec-068 instance. A new `core/loop_attempt.py` suppresses the state write when an attempt is materially identical to the one on record, where identity = (a) same content, compared via the existing `_content_changed_since` rather than sha equality -- the loop's own bookkeeping commits move HEAD between attempts, so sha equality would never converge -- and (b) every LoopState field equal outside a DENY-list of `updated_at` / `last_eval_started_at` / `candidate_sha`. Deny-list not allow-list on purpose: a field added later participates automatically, and `tests/test_loop_attempt.py` carries a coverage guard that fails when a LoopState field is added unclassified. The asymmetry is deliberate -- a too-broad identity silently swallows a NEW failure, which is far worse than a redundant commit. CRITICAL FINDING the original row did not anticipate: pacing read `last_eval_started_at`, which only advances when a write happens, so suppressing writes would have released the retry floor every tick and traded cheap commit spam for far more expensive EVAL spam (git clone + LLM calls every 5-30s instead of every 60s). The retry clock therefore moved out of git to a gitignored marker under `.knotica/locks/` beside the heartbeat, with `last_eval_started_at` as the fresh-machine fallback; in-process state was ruled out because `service.manager._default_run_topic` builds a fresh LoopRunner every supervision cycle. Characterization tests pinned the two-commit behavior before the change. `core/loop.py` net SHRANK 23 lines (td-008 unharmed). Accepted trade-off, recorded as the ADR's dissent: two upstream faults that stringify identically are now indistinguishable in history. Already-bloated vaults are still not repaired -- history rewriting stays out of scope, so the measured 14.8k commits stand. | loop-noop-attempt-writes-commits |
| td-033 | suggested | drift | goals-to-code | .ai-state/decisions/018-golden-set-split-and-bootstrap.md, .ai-state/decisions/068-loop-blocked-failure-backoff.md, .ai-state/decisions/DECISIONS_INDEX.md | architecture | rules/swe/adr-conventions.md Frontmatter + Re-affirmation Protocol | sentinel | 2026-08-04 | 2026-08-04 | doc-engineer | resolved | 2026-08-05 orchestrator added `scripts/check_adr_health.py`, wired into `make verify` | RESOLVED 2026-08-05 (orchestrator, task `sentinel-remediation`): both symptoms and the root cause are closed. Symptom 1 - 018 `summary:` and 068 `dissent:` wrapped in double quotes; all 69 frontmatter blocks now parse under yaml.safe_load with values byte-preserved. Symptom 2 - 8 one-directional `re_affirms` backfilled across 6 files (NOT the 22 the finding claimed; sentinel over-counted ~3x and its cited example dec-025 already listed both back-refs). Root cause - `scripts/check_adr_health.py` now gates frontmatter validity (all records incl. drafts) and reciprocity (finalized only, since a draft legitimately points at a finalized decision before finalize adds the backlink), running as the second step of `make verify`, with 8 tests in `tests/test_adr_health.py`. Fixing symptom 1 exposed a latent second defect: `regenerate_adr_index.py` did not unquote scalars, so the quoting would have leaked literal quotes into dec-018's index row at the next finalize; parser patched. Broadened-`re_affirms` semantics recorded as dec-071. | 1f19903f4064 |
| td-034 | important | drift | goals-to-code | .ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md | spec-req | SPEC_wiki-mvp-core Traceability | sentinel | 2026-08-04 | 2026-08-04 | test-engineer | resolved | 2026-08-04 test-engineer re-derived the matrix into `.ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md` §Traceability | RESOLVED 2026-08-04 (test-engineer, task `sentinel-remediation`): the §Traceability pointer was replaced with a rendered 42-row matrix (Requirement / Test(s) / Implementation / Status), carrying an explicit RE-DERIVED-NOT-CONTEMPORANEOUS banner that dates the reconstruction, names `.ai-work/` cleanup as the reason the original evidence is unrecoverable, and warns against reading a COVERED row as proof the test predates the REQ. All three dangling `.ai-work/wiki-mvp-core/` references were annotated as deleted-at-cleanup rather than silently dropped (two in the Sources row, one in §Traceability). Every one of the 300 cited pytest node ids was machine-verified to exist in the current suite. Sentinel SH04 is no longer vacuous: it now has 42 entries to check. RE-DERIVED COVERAGE: 31 COVERED, 7 PARTIAL, 1 UNTESTED, 3 DRILL (unrecoverable manual evidence). TWO OBLIGATIONS THIS ROW DOES NOT DISCHARGE, both needing their own row from a ledger writer (test-engineer is a consumer and may not add rows): (1) the GL03/SH02 gate-liveness observation in the original finding -- SH02's "present and non-empty" pass condition should gain a "contains at least 1 requirement row" clause, otherwise the same structural check will pass on the next zero-substance section; (2) the 8 genuine coverage gaps the re-derivation surfaced, which are NEW findings, not part of this row's scope: REQ-PLUGIN-01 (`.mcp.json` launch line and absent `alwaysLoad` asserted by nothing -- cheapest to close), REQ-REC-03 (no test asserts root SCHEMA.md documents the five record formats; the frozen grammars are re-declared in `tests/support/vault.py` rather than read back, so doc and code can diverge silently), REQ-CLI-07 (curated-examples / last-lint / unpushed-commits counts implemented but unasserted), REQ-CLI-06 ("never invokes an LLM" survives only as docstring prose; the doctor `mcp` registration row is unpinned), REQ-PROMPT-02 (citation discipline on `query` unasserted), REQ-TOOL-02 (no-write-lock clause proven only structurally for adapters, never behaviourally for a core read path), REQ-TOOL-05 (required-non-empty topic on MUTATING tools unasserted -- the one empty-topic rejection test is a read tool), REQ-PLUGIN-02 (pre-warm command / backgrounding / idempotence unasserted). Per the task's non-goals no test or source file was written; the gaps were recorded as findings and left visible. ORIGINAL FINDING: The archived spec Traceability section is a pointer to a deleted file, not a rendered matrix, so 42 REQ ids have no test or implementation mapping in the permanent record. The section (lines 225-231) reads "Live map: .ai-work/wiki-mvp-core/traceability.yml ... The rendered matrix (Requirement / Test(s) / Implementation / Status) is baked into this section at feature-end archival, before .ai-work/ cleanup." The bake-in never happened and .ai-work/wiki-mvp-core/ no longer exists (present slugs: kb-dashboard-switcher, loop-consolidation, notes-overlay, retrieval-scorer-fix, test-topology-init), so all three .ai-work/ references in the spec dangle. EVIDENCE IS UNRECOVERABLE, not merely missing -- the yml was deleted at cleanup, so the mapping must be re-derived from the current suite. That is real work with a named owner, which is why this is debt rather than a fix-now defect. Sentinel SH04 (no UNTESTED entries) passes only VACUOUSLY: there are no entries at all. Note this is also a gate-liveness (GL03) instance -- the SH02 pass condition asserts the section is "present and non-empty" and the section does hold prose, so a structural check passes on an artifact with zero substance; SH02 upstream should gain a "contains at least 1 requirement row" clause. | 5337d2983eef |
| td-035 | suggested | drift | goals-to-code | .ai-state/calibration_log.md | code-quality |  | sentinel | 2026-08-04 | 2026-08-04 | implementer | resolved | 2026-08-05 orchestrator migrated the log to the canonical 7-column schema | RESOLVED 2026-08-05 (orchestrator, task `sentinel-remediation`): header migrated to the canonical `Timestamp / Task / Signals / Recommended Tier / Actual Tier / Source / Retrospective`; the gate now parses all 21 rows and immediately reported 50 uncalibrated pipeline commits since 2026-07-31 - a real finding it had been silently swallowing. Migration verified lossless: every original cell survives, the sole transformed cell being `Standard declared, **Full actually**`, deliberately split across the two tier columns. `Actual shape` was NOT folded into `Signals` - canonical Signals is at-intake scoring while `Actual shape` is realized-after, so conflating them would falsify the record; it is preserved at the head of each Retrospective. Recommended Tier and Source were derived, not blanked, for the 20 rows whose `Right call?` was Yes. NOTE: this fixes knotica's side only. The checker also fails open on the lowercase header `/onboard-project` itself seeds, because two Praxion documents declare conflicting canonical schemas - filed upstream as praxion#58. | 308f69809e2e |
| td-036 | important | correctness | code-to-goals | src/knotica/guillotine/report.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | implementer | resolved | 2026-08-05 implementer deleted the dead writer and widened the fitness scan codebase-wide | RESOLVED 2026-08-05 (implementer, task `sentinel-remediation`): `write_artifacts` deleted outright rather than routed through VaultTransaction -- it had zero references in src, tests or scripts, so the simplest correct change was removal. The fitness test no longer scans a package NAME-LIST: `test_adapters_do_not_call_mutating_store_methods` now enforces the DESIGN.md rule literally, codebase-wide, excluding only `core/transaction.py` (the sanctioned writer) and `store/` (which implements the mutation primitive, so its own use is implementation, not bypass). A name-list could only ever approximate the rule, and the next package added would start out uncovered -- which is exactly how this writer survived. One collision handled structurally: `delete` is both a VaultStore mutator and a VaultTransaction STAGING call, so the scan skips receivers bound by `with VaultTransaction(...) as <name>` -- rename-robust, and it fails CLOSED (a transaction obtained another way is flagged loudly). SIDE EFFECT: the deletion dropped `guillotine/report.py` to 799 lines, under the 800 ceiling, so the file-size ratchet correctly failed and its baseline exemption was removed -- a paid-down exemption is never kept. | guillotine-report-writes-outside-transaction |
| td-038 | important | drift | goals-to-code | src/knotica/core/topics.py, src/knotica/core/jsonl.py, src/knotica/core/loop_attempt.py, .ai-state/DESIGN.md, docs/architecture.md | architecture | .ai-state/DESIGN.md section 3 Components | sentinel | 2026-08-05 | 2026-08-05 | systems-architect | resolved | 2026-08-05 orchestrator recorded the three modules in both architecture docs and added `scripts/check_architecture_coverage.py`, wired into `make verify` | RESOLVED 2026-08-05 (orchestrator, task `sentinel-arch-gate`): fixed, and the CLASS was gated before the instance was fixed. GATE FIRST, DELIBERATELY: `scripts/check_architecture_coverage.py` was written and run BEFORE any doc edit, and its first run reported 16 findings against the real drift; a gate authored after the fix passes on its first run and proves nothing about whether it bites. It is now the third step of `make verify` with 11 tests in `tests/test_architecture_coverage.py`, 8 of them golden bad-cases (module added / deleted / package absent / package gone / dangling citation in each document / glob matching nothing). WHAT THE GATE ACTUALLY CHECKS, stated precisely because the obvious rule is unbuildable: (1) every package under `src/knotica/` publishes a module count in a new DESIGN.md section-3 inventory table and the count matches disk; (2) every `src/knotica/...` path cited in either document resolves on disk. The rule the finding implied -- every module must be NAMED in a doc -- was measured and rejected: it flags 60 modules today, and demanding a sentence per module reverses the recorded residual decision (`core/` is 'a subtraction, never core/**'), i.e. it would reopen td-032's decomposition question by the back door. Bare-stem matching was also probed and rejected as unsafe: it reports `core/topics.py` as covered because the word `topics` appears as a JSON key elsewhere, which is a gate reporting success over something it never inspected. Counts are exact integers with no false-positive surface, and a module cannot arrive or leave without moving one. NAMED-BUT-UNDESCRIBED remains outside the gate and is stated as such in its docstring. FOURTH STALE CLAIM, not in the finding: the `vault_layout.py` row in BOTH documents still described 'the four `if name in RESERVED_TOP_LEVEL_NAMES: continue` topic-enumeration walks (`vault_metadata_tree`, `status`, `tools_read`, `service.manager`)'. Grep confirms none of those four modules references the constant any more -- they route through `core.topics` since fe10b7a. Fixed in both. COUNT CONVENTION: 56 -> 59 for `core/`, and the count includes `__init__.py`, which is what the pre-existing `okf/` (11), `guillotine/` (9) and `service/` (3) prose claims already meant -- all three matched the tree exactly under that reading, which is why the gate adopts it rather than inventing one. Those three prose counts were then REMOVED and replaced by table rows: a count published twice is a count that drifts once, and leaving them would have left three ungated numeric claims in the file whose whole lesson is that the ungated claim lags. | design-core-modules-unrecorded-in-arch-docs |
| td-039 | suggested | drift | goals-to-code | .ai-state/TEST_TOPOLOGY.md | architecture | .ai-state/TEST_TOPOLOGY.md Notes 2 and 3 | sentinel | 2026-08-05 | 2026-08-05 | test-engineer | resolved | 2026-08-05 orchestrator reconciled Notes 2 and 3 against the sections that completed after them, in one full read-through of `.ai-state/TEST_TOPOLOGY.md` | RESOLVED 2026-08-05 (orchestrator, task `sentinel-arch-gate`): both contradictions closed, and the full read-through the row prescribed found more than the two sentences. (a) Note 2's guillotine deferral rewritten to past tense; re-verified first with yaml.safe_load -- `guillotine-audit.file_dependencies` == ['src/knotica/guillotine/**', 'src/knotica/core/operations/guillotine.py'] and `vault-semantics`' hold no guillotine entry, so the finding's reading was right. (b) Note 3 rewritten: it now says none of the seven cross-cutting fitness tests OWNS a group (the durable claim) and points at the section that pinned six of them, naming `test_spine.py` as the sole un-grouped one. THREE THINGS THE FINDING DID NOT NAME, all found by reading the whole file as instructed. (1) `loop_attempt.py` was missing from the loop-sibling list at THREE sites, not one: the Subsystems cross-reference row, Note 2's subtraction table, and the `loop-runtime` group's own `subsystems:` YAML -- the last two of which are on opposite sides of the ownership boundary. Since section 3's loop row was widened for td-038, all three had to move together or sentinel TT01 would stop resolving; re-verified after the edit at 23 subsystems / 23 Built rows, 1:1, zero uncovered. `file_dependencies` already listed the module, which is exactly why the drift was invisible to `scripts/test_group.py --check`. (2) 'a loop row widened to name all fifteen siblings' -> sixteen. (3) The arithmetic moved under this task's own changes and was re-derived rather than adjusted: full suite 2520 -> 2534, files 175 -> 176, grouped-unique 2480 -> 2483 (3 new topics tests inside `vault-semantics`, whose runtime row goes 569 -> 572), un-grouped 40 -> 51 with `test_architecture_coverage.py` added to the un-grouped table beside its two `scripts/`-covering siblings. The self-checking column-sum identity was recomputed and still holds: 2609 - 126 = 2483. A third movement was appended to the history section rather than editing the first two. OWNERSHIP: the systems-architect-owned Notes and the test-engineer-owned YAML block were both edited in this pass. Recorded rather than hidden -- sentinel's own read was that routing these differently would not have prevented the defect and that the missing step was a closing read of the whole file, which is what this was. | test-topology-notes-contradict-later-sections |
| td-040 | suggested | correctness | code-to-goals | src/knotica/core/lint.py | architecture | .ai-state/DESIGN.md single-declaration rule for path and topic classification | sentinel | 2026-08-05 | 2026-08-05 | implementer | resolved | 2026-08-05 orchestrator moved `topic_directories` into `src/knotica/core/topics.py`; all three consumers now import it | RESOLVED 2026-08-05 (orchestrator, task `sentinel-arch-gate`): consolidated -- but TWO OF THE FINDING'S CLAIMS ARE WRONG and the row closes with them corrected rather than repeated. (1) 'Delete the now-unused `_is_directory`' would have broken the build: `lint.py` uses it at a SECOND site, `_check_reserved_names`, which needs the COMPLEMENT of `is_topic` -- a directory that claims a reserved name, exactly what the topic predicate returns False for by design. It is kept, with a docstring saying why so the next auditor does not re-propose the deletion. (2) The 'ORDERING, live' divergence is not live. `VaultStore.list_dir` is contractually sorted -- 'sorted lexicographically for deterministic output' in the protocol docstring, `sorted(...)` in `LocalFSStore` -- so lint's report ordering was never filesystem-dependent, and the `sorted()` in the other two copies was redundant over an already-sorted contract. The finding's second divergence (exception surface) was correctly labelled latent. So the defect was real DUPLICATION with zero observable behavioural difference at any call site, not a behavioural fork; severity `suggested` was right for the wrong reason. WHAT WAS DONE, wider than the row's ~4-line estimate because the row's own framing implies it: rather than pointing `lint.py` at `is_topic`, the enumeration itself moved to `core/topics.py` as a public `topic_directories(store)` and ALL THREE copies were deleted -- `status.py`, `vault_metadata_tree.py`, `lint.py`. Fixing only `lint.py` would have left two identical wrappers around a consolidated predicate, which is the same defect one refactor later; the finding's own diagnosis is that a wrapper around a consolidated predicate is still a copy of the policy, and that is invisible to whole-function AST comparison precisely because it is a different function. Canonical form is `[name for name in store.list_dir('') if is_topic(store, name)]` with the redundant re-sort dropped and the contract reliance documented. 3 tests added to `tests/test_core_topics.py` (empty vault, lexicographic order, exclusion of hidden/reserved/file entries); the stale `_topic_directories` reference in `okf/repair.py`'s comment updated. `make verify` green at 2534. | topic-directories-forked-predicate |
| td-029 | suggested | other | code-to-goals | .git/hooks/{post-merge,post-commit,post-checkout} | adr | adr-conventions - finalize at merge-to-main | orchestrator | 2026-08-02 | 2026-08-02 | implementer | resolved | 2026-08-02 scripts/install_git_hooks.sh + .claude/settings.json SessionStart re-assertion | RESOLVED 2026-08-02, to the ceiling of what is achievable. FIRST, a constraint that reframes the row: git NEVER installs a repository's own hooks on clone -- that is a security boundary, since otherwise `git clone` would execute arbitrary code -- and `core.hooksPath` lives in `.git/config`, which is not cloned either. So the row's option 2 (commit a githooks/ dir + set core.hooksPath) does NOT achieve automatic installation; it still needs one manual `git config` per clone. No option makes this automatic through git. A global `core.hooksPath` would, but it silences per-repo hooks machine-wide and would break `pre-commit` in every project that uses it -- which praxion's own coding-style baseline expects to work. SO the fix is a mechanism plus a trigger: (1) `scripts/install_git_hooks.sh`, idempotent, worktree-safe (resolves the shared hooks dir via `git rev-parse --git-common-dir`), refuses to replace any hook it did not install, degrades to a printed message rather than broken symlinks when praxion is absent, and resolves the dispatcher via PRAXION_ROOT -> ~/dev/praxion -> plugin cache (cache last, because its path carries a version number that a symlink would silently outlive); (2) a committed `.claude/settings.json` SessionStart hook running `scripts/session_state_guard.sh`, which re-asserts the hooks quietly every session and reports pending drafts or uncommitted finalize output. NOTE the guard is PROJECT-LOCAL, deliberately NOT the plugin's `hooks/session_start.sh` -- that one ships to every end user of the knotica plugin and must only concern itself with their vault, never with this repo's development workflow. Verified by direct test: idempotent re-run, refusal on a foreign hook, clean no-op with praxion absent, self-repair of a deleted hook, and silence when there is nothing to report (telemetry `observations.jsonl` is excluded from the dirty check precisely so the nudge is not fired every session into irrelevance). RESIDUAL, unavoidable: a clone on a machine with no praxion and no Claude Code session still has no hooks -- it gets a message when someone runs the installer. Also NOT auto-committing finalize output, which is derived and would be mechanically safe, because a session-start hook that creates commits is a surprise the first time it lands on work in progress; the guard names the command instead. ORIGINAL FINDING: The ADR finalize protocol is now wired locally but NOT reproducibly. On 2026-08-02 the three git hooks were symlinked to `~/dev/praxion/scripts/git-finalize-hook.sh` (the multiplexed dispatcher; post-merge runs reconcile + finalize + squash-safety, post-commit and post-checkout catch the non-merge paths that also land drafts on main). Verified: exit 0 off main, correctly a no-op. RESIDUAL: `.git/hooks/` is not version-controlled, so a FRESH CLONE of this repo has no hooks again and silently reverts to "someone must remember to run finalize_adrs.py by hand" -- the exact failure the Phase 4 brief recorded, which had until now gone unnoticed through four phases. The symlink also points at a developer-local praxion checkout (`~/dev/praxion`), so it is machine-specific as well as clone-specific. DURABLE FIXES, in preference order: (1) run praxion's `/onboard-project` Phase 4 on this repo, which is the sanctioned installer and keeps the wiring consistent with every other managed project; (2) commit a `githooks/` directory and set `core.hooksPath` to it -- self-contained and clone-safe, but note this repo already has a `hooks/` directory for CLAUDE PLUGIN hooks, so the new one needs a distinct name to avoid conflating two unrelated concepts; (3) document the install step in CONTRIBUTING.md and accept that it depends on a human reading it, which is what the current state already effectively is. NOT urgent while this is a single-developer, single-machine repo, but it silently degrades the moment anyone else clones it or this machine is replaced. | git-finalize-hooks-not-reproducible-from-a-fresh-clone |
| td-027 | suggested | other | code-to-goals | src/knotica/core/operations/reanchor_note.py | behavioral | SYSTEMS_PLAN AC-09 - a corrected anchor's original record is never modified or removed | orchestrator | 2026-07-31 | 2026-08-02 | implementer | resolved | 2026-08-02 append_anchor_text splices the new bullet onto the file's existing bytes instead of reserializing | RESOLVED 2026-08-02 via the fix the row itself specified: `_append_and_commit` now calls the new byte-preserving `anchor.append_anchor_text(store.read_text(path), new_anchor)` rather than `serialize_note(replace(document, anchors=...))`, so a correction appends bytes and rewrites none. Safe as a plain append because the anchors section is always last -- the serializer emits frontmatter, body, then `## Anchors`, and `parse_note` consumes that section to end-of-file. The characterization test named in this row did exactly what its docstring promised: it started failing the moment the fix landed, and was replaced with the byte-prefix assertion its two siblings use (`test_reanchor_preserves_a_hand_authored_anchor_bullets_formatting`), which now asserts the doubled spacing the user typed survives verbatim. Both `reanchor` and `detach` go through the same helper, so both are fixed. NOTE: a sibling primitive `page.set_frontmatter_scalar` was added earlier the same day for the frontmatter case (the `promoted:` stamp), for the identical reason -- the two together mean no note write path re-renders a document to change part of it. ORIGINAL FINDING: `reanchor` and `detach` rewrite the on-disk formatting of anchors they are not correcting, when the note was hand-authored with valid-but-non-canonical spacing. Both operations parse the whole document and call `serialize_note` on it, so every bullet is re-rendered in canonical form -- a user's `-  [[page]]  -  `span`  ·  pinned@`sha`` (doubled spaces) comes back as `- [[page]] - `span` · pinned@`sha``. The parsed `AnchorRecord` is unchanged, which is why this went unnoticed: the two record-level tests compare parsed dataclasses, and `AnchorRecord` equality is insensitive to formatting. AC-09 says the original anchor of record 'is never modified or removed', and the phase's own A1-A5 ruling states the standard explicitly -- 'The tuple is not the artifact; the note file is'. Hand-authoring is a first-class capture surface (AC-13), so this is a live path, not a hypothetical one. IMPACT is bounded: no data is lost, every field survives, and the note stays valid; what is lost is the user's own formatting in the user's own file, plus a clean `git diff` -- a reanchor now shows spurious changes on lines the user did not touch, which is noise in the audit trail the one-commit-per-operation design exists to keep readable. FIX: preserve the original byte range of every anchor bullet the operation is not itself writing, rather than reserializing the whole document -- e.g. splice the appended bullet onto the original text instead of round-tripping through `serialize_note`. REPRODUCTION: `tests/core/notes/test_reanchor_note.py::test_reanchor_normalizes_a_hand_authored_anchor_bullets_formatting` pins the current behaviour as a characterization test; it is written to START FAILING when this is fixed, and its docstring says to replace it with the byte-prefix assertion its two siblings use. Found while adding the byte-level AC-09 guard that the phase-2 verification identified as missing. | reanchor-normalizes-hand-authored-anchor-formatting |
| td-001 | important | drift | code-to-goals | tests/test_evals_golden.py:89, tests/test_evals_harness.py:102, tests/test_evals_program.py:69 | claude-md | Project conventions - run via uv run pytest | verifier | 2026-07-16 | 2026-07-16 | test-engineer | resolved | 2026-07-29 orchestrator verification | VERIFIED RESOLVED (orchestrator, 2026-07-29): all three named files now use `pytest.importorskip("dspy")`, and the full suite runs 1849 passed / 17 skipped with `dspy` ABSENT from the resolved environment -- so the documented `uv run pytest` entry point in CLAUDE.md, README.md and docs/architecture.md is accurate, closing the doc-alignment half as well. Neither half of this row describes current reality. ORIGINAL: Documented `uv run pytest` aborts at collection (unguarded `import dspy` x3); fix = importorskip guards + doc alignment; rework rw-ed8a97e8 | 5d264f152bd7 |
| td-003 | suggested | dead-code | code-to-goals | src/knotica/evals/harness.py:686 | spec-req | REQ-METRICS-02 | verifier | 2026-07-16 | 2026-07-16 | implementer | resolved | 2026-07-29 orchestrator verification | VERIFIED RESOLVED (orchestrator, 2026-07-29): `failure_score=config.failure_score` IS passed to `dspy.Evaluate` at harness.py:686, and the function docstring (harness.py:673-678) now states the exact contract this row asked for -- 'a caller that raised it must see it reach dspy.Evaluate, not dspy's own default'. Wired, not dropped; the row was never flipped. ORIGINAL: `failure_score` fingerprinted + manifest-recorded but never passed to dspy.Evaluate; inert under abort-on-failure; wire or drop; rework rw-bfab7c61 | 2d627c6191db |
| td-004 | suggested | other | code-to-goals | (cleared repo-wide) | code-quality |  | verifier | 2026-07-16 | 2026-07-16 | implementer | resolved | 2026-07-29 orchestrator verification | RESOLVED (orchestrator, 2026-07-29): ruff check AND ruff format --check are both clean across 325 files. 14 errors auto-fixed (F401 unused imports, F541 placeholder-less f-strings); 7 F841 dead locals removed by hand after individually verifying none carried a side effect -- including `masked_line` in okf/links.py, checked against the parser to confirm inline-code masking is genuinely implemented via the parts/last split rather than by that variable, and a removal cascade in tests/test_okf_links.py where deleting one dead binding orphaned the next. Also reformatted the two long-standing offenders scripts/review_golden.py and tests/test_okf_cli.py. Count had drifted 13 -> 18 before this pass. ORIGINAL: 13 pre-existing ruff errors (F401/F841) from cf2bf53 (OKF/guillotine/PDF-reflow); 11 auto-fixable via `ruff check --fix`; out of eval-harness scope | 2c9cc0b9e4aa |
| td-005 | suggested | coverage-gap | goals-to-code | tests/test_architecture_boundaries.py, src/knotica/evals/golden.py:668-701 | adr | dec-015 | verifier | 2026-07-16 | 2026-07-16 | test-engineer | resolved | tests/test_architecture_boundaries.py::test_adapters_do_not_perform_raw_filesystem_writes | RESOLVED 2026-07-29 for the adapter packages (`cli`, `mcp_server`, `evals`) with a 4-entry allowlist. CARVE-OUT NOW CLOSED (2026-07-29): `okf/` was initially outside the scan because `okf/repair.py` was a live single-writer violation; td-020 fixed that, and the scan was widened to cover `okf/` via a separate `RAW_WRITE_PACKAGES` constant (okf is not an adapter, so only the raw-write rule applies to it). Work survived a shared-worktree `git stash` incident (a sibling agent ran the forbidden bare stash; its pop conflicted and the agent terminated mid-recovery, reverting several siblings' uncommitted files). Restored surgically from the stash object by the orchestrator and re-verified independently before flipping. Single-writer fitness scan cannot see raw pathlib writes; `golden._write_staging` is the sanctioned exception — extend the scan with an explicit allowlist | 34caedbb4f6b |
| td-006 | important | other | code-to-goals | src/knotica/cli/eval.py | architecture | PRE_PLAN - loops return branches for review | verifier | 2026-07-16 | 2026-07-16 | implementer | resolved | 2026-07-29 orchestrator verification | VERIFIED RESOLVED (orchestrator, 2026-07-29): cli/eval.py resolves `clone_root / record.artifact_ref` and returns it, and `_json_payload(record, clone_root)` carries both the `clone_root` the run committed to and the resolved `manifest_path` in the --json envelope. The 'clone root never surfaced / artifact_ref unresolvable' condition no longer holds. ORIGINAL: Clone root never surfaced by CLI; artifact_ref unresolvable; add clone-root output line and/or --work-root flag; Step-33 prerequisite; rework rw-bfab7c61 | 29f79c5b8899 |
| td-012 | suggested | other | code-to-goals | tests/test_text_reflow.py:83-89 | test-quality |  | orchestrator | 2026-07-22 | 2026-07-29 | implementer | resolved | tests/fixtures/text_reflow/hu2025memory-s1-intro-raw-excerpt.md | RESOLVED (test-engineer, 2026-07-29): the hardcoded live-vault path and its `if not path.exists(): return` early-return are removed; the test now reads a committed fixture (`tests/fixtures/text_reflow/hu2025memory-s1-intro-raw-excerpt.md`, git-tracked, confirmed via `git check-ignore -v`) containing a verbatim, pre-reflow excerpt of the same source page ("Key Questions" + "Contributions" sections, copied from an earlier un-reflowed git revision of the live document) — retains real mid-sentence column wraps, a hyphenated line break, a run-in heading, and a paragraph boundary. Renamed to `test_reflow_is_idempotent_on_real_extracted_pdf_text` (behavior-named, no longer document-identifier-named). Verified red/green: perturbing the fixture's hyphenated join point breaks the idempotence assertion (confirmed FAILED), reverting restores green; full suite unaffected in skip count (17 before/after — this test was never a skip, it was a silently-passing no-op). ORIGINAL: `test_hu2025memory_s1_intro_reflow_is_idempotent` reads a hardcoded absolute live-vault path (`/Users/fperez/dev/data/knotica/sources/...`) guarded by `if not path.exists(): return` — a silent no-op on any machine without that exact local path, so the test provided zero coverage in CI or on any other contributor's machine. Read-only (no write risk to the live vault), but violated test hermeticity and the project's 'tests never touch the live vault' spirit even though it satisfied the write-only letter of that invariant. |  |
| td-014 | suggested | other | code-to-goals | hooks/, scripts/ | rule | id-citation-discipline (inbound enforcement) | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | scripts/check_id_citation_discipline.py + .pre-commit-config.yaml | RESOLVED 2026-07-29. Work survived a shared-worktree `git stash` incident (a sibling agent ran the forbidden bare stash; its pop conflicted and the agent terminated mid-recovery, reverting several siblings' uncommitted files). Restored surgically from the stash object by the orchestrator and re-verified independently before flipping. The inbound half of the id-citation discipline is unenforced in this repo: neither `hooks/commit_gate.sh` nor `scripts/check_id_citation_discipline.py` exists (`hooks/` holds only `hooks.json` + `session_start.sh`), so nothing mechanically blocks a commit that embeds ephemeral pipeline identifiers in code. Empirically load-bearing, not theoretical: during notes-overlay Phase 0, five such citations had to be stripped by hand from committed files — `dec-draft-<hash>` in a production module docstring (`core/vault_layout.py`) and `PM-N` + `WIP.md` references in `tests/test_vault_layout.py`. Two independent agents introduced the same class of violation, and one then misattributed the manual fix to a non-existent gate, which is how the belief that it is covered propagates. Note the asymmetry that makes this easy to get wrong: `.ai-state/DESIGN.md` and `docs/**` are inside the ADR finalize walk scope and therefore MUST cite `dec-draft-<hash>` ids, while code must never — so a naive repo-wide grep is the wrong check. Fix: port the praxion detector + commit-gate hook, honouring the exempt-path list. | id-citation-inbound-enforcement-missing |
| td-015 | suggested | other | code-to-goals | src/knotica/search/ripgrep.py:145,209 | architecture | notes are excluded from retrieval by construction | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | tests/test_search.py (BM25 corpus-stats parity guard) | RESOLVED 2026-07-29. Work survived a shared-worktree `git stash` incident (a sibling agent ran the forbidden bare stash; its pop conflicted and the agent terminated mid-recovery, reverting several siblings' uncommitted files). Restored surgically from the stash object by the orchestrator and re-verified independently before flipping. Unscoped search (`topic=""`) excludes `notes/` from *results* (family filter in `_collect_matches`) but still counts them in *BM25 corpus statistics*: `_corpus_stats(scan_dirs)` walks `[root]` for an empty topic, so note files inflate `doc_count` (idf) and average byte length (the `b=0.75` length-normalisation term). Net effect: a note cannot be returned, yet it perturbs the score of pages that are — idf computed over a corpus containing documents that are unreachable by construction, which is incoherent. **Scoring impact: none, verified.** Both scoring-path retrievals (`evals/runner.py:_retrieve`, `core/query_engine.py:_retrieve_page_paths`) pass an explicit topic, and topic-scoped `_scope_dirs` returns `[root/<topic>, root/sources/<topic>]`, which never includes `notes/`; no production caller searches unscoped. So the eval composite scalar is unaffected and the Phase 0 no-contamination guarantee holds. Reachable by hand today (not only from Phase 1) because the shipped `vault-template/SCHEMA.md` invites authors to hand-write notes in Obsidian. Fix: thread `root` into `_walk_markdown_files`/`_corpus_stats` (3 call sites) and apply the same `family_of(rel) in SCORED_FAMILIES` predicate. Guarding test: a page's score is identical with and without a sibling populated `notes/` tree — the search-side analogue of the lint-side byte-identity guard in `tests/test_score_isolation_characterization.py`. Surfaced as a registered objection by the implementer rather than silently resolved; deferred to Phase 1 alongside the notes-search surface. | notes-excluded-from-results-but-not-bm25-corpus-stats |
| td-016 | important | drift | code-to-goals | src/knotica/core/loop.py:583-606, vault-template/SCHEMA.md:78 | adr | dec-060 (third predicate use: loop._content_changed_since skips the note family) | verifier | 2026-07-29 | 2026-07-29 | implementer | resolved | src/knotica/core/loop.py::LoopRunner._content_changed_since (unscored-family `continue` branch) + tests/test_loop_content_classification.py | Loop-watch exclusion (U3) never implemented, while the shipped vault constitution says it was. `_content_changed_since` is byte-unchanged from main: a `notes/<topic>/x.md` path is not `log.md` and holds no `.knotica` segment, so control reaches `return True` on the first iteration and `observe_default()` proceeds to a full billed eval (clone + retrieval + Anthropic synthesis + LLM judge). `vault-template/SCHEMA.md` — copied into every scaffolded vault and every test fixture — nonetheless lists "the loop's change detection and improvement observation" among the exclusions that hold "by construction", and in the same section invites the user to hand-author notes in Obsidian. The other two claims in that bullet list are true (lint content-page count + orphan check; search results); only this one is prose. Root cause is an unreconciled inconsistency inside SYSTEMS_PLAN itself: § Sequencing puts U3 in Phase 0, § Prerequisites P0b defers it to "the same phase as the first note write" — no agent surfaced the conflict, and Step 7 shipped the guarantee anyway. Under a `best` baseline policy the unearned eval also enters the high-water-mark comparison. Fix (either sufficient, first preferred): (a) add one `continue` branch keyed on `family_of(path) == "note"` mirroring the existing `.knotica` branch (~4 lines; `family_of` already available), plus a unit test asserting a notes-only changed-path set is loop-inert; or (b) delete the loop sentence from SCHEMA.md until U3 lands. Note (a) is smaller than (b) and closes a billing exposure instead of documenting one. **Resolved** via remedy (a), with two deliberate deviations from the sketch above: the branch is keyed on `family_of(path) not in SCORED_FAMILIES` rather than `== "note"` (the method's real question is "did any *scored* content change?", so a future third unscored family inherits the inertness instead of silently re-billing; behaviourally identical today because `family_of` returns only `page`/`source`/`note`), and the `family_of` call is wrapped in `except ValueError: pass` so an unclassifiable path falls through to `return True` — erring toward observing, never toward silence, matching the method's existing defensive treatment of `changed_paths`. `vault-template/SCHEMA.md:78` is now true as written and was left unchanged. Guarded by `tests/test_loop_content_classification.py` (5 cases): a `notes/` commit is inert; page, `sources/` and `.knotica/prompts/` commits all still observe (the over-broadness guard — an exclusion that silenced prompts would stop the loop watching its own evolvable substrate); and a commit bundling a note *with* a page still observes, pinning the exclusion as per-path rather than per-commit. Non-vacuity proven by neutralising `SCORED_FAMILIES` at runtime, which restores the pre-fix `True`. | loop-watch-note-exclusion-missing-while-documented |
| td-017 | suggested | other | code-to-goals | uv.lock | code-quality |  | verifier | 2026-07-29 | 2026-07-29 | implementer | resolved | pip-installed uv 0.5.24 removed; uv.lock regenerated with 0.7.16 | RESOLVED 2026-07-29. The shadowing pip install was removed, so `uv --version` now reports 0.7.16 (Homebrew). PROVEN, not assumed: a PLAIN `uv run` (no `--frozen`) now leaves `git status -s uv.lock` empty, where before it rewrote 1421/1422 lines and stripped `revision = 2`. The lockfile was regenerated with 0.7.16 and carries `revision = 2` plus the locked mypy. `--frozen` remains good practice in agent prompts but is no longer load-bearing. ORIGINAL: RE-DIAGNOSED 2026-07-29, and the original diagnosis was WRONG: this is not an out-of-date uv, it is PATH shadowing. Two installs coexist -- `/Users/fperez/.pyenv/shims/uv` (pip-installed 0.5.24, FIRST on PATH, and the one that downgrades the lockfile) and `/opt/homebrew/bin/uv` (0.7.16, already present and unused). That is also why `uv self update` refuses: it only supports standalone-installer binaries, not a pip install inside a pyenv shim. Verified 0.7.16 preserves `revision = 2` and produces a 183-line/+5-package diff, versus the 2843-line downgrade 0.5.24 produces. The lockfile has now been regenerated with 0.7.16 and is correct. REMAINING USER ACTION: make 0.7.16 the resolved `uv`, either `python3 -m pip uninstall uv` (uv is a standalone tool, not a project dependency, so Homebrew is its natural home) or `python3 -m pip install --upgrade uv`. Until then a plain `uv run` still downgrades the lockfile and `--frozen` remains the mitigation. Row stays OPEN pending that. ROOT CAUSE + MITIGATION (orchestrator, 2026-07-29): the local toolchain is `uv 0.5.24` (2025-01-23), which predates `revision = 2` lockfile support, while the committed `uv.lock` carries `revision = 2` from a newer uv. So EVERY plain `uv run` / `uv sync` here silently rewrites the lockfile downward. Not an agent error and not one-off: it recurred immediately after two separate reverts, because each agent invocation runs `uv run`. Practical mitigation, verified working: **`uv run --frozen ...`** leaves the lockfile untouched -- adopt it for every agent and CI invocation in this repo. Real fix: upgrade the local uv to the version that produced the committed lock, or pin the toolchain. Until then treat `git status -s uv.lock` as a mandatory pre-commit check. Row stays OPEN deliberately -- the artifact was reverted, the cause was not. ORIGINAL FINDING: Undeclared `uv.lock` rewrite in the notes-overlay-phase0 worktree: +1421/−1422 lines with no step declaring the file. Characterised, not guessed — `revision = 2` dropped from line 2 (an older `uv` than the one that wrote the committed file rewrote it) and every `upload-time` field stripped from every sdist/wheel entry. Verified benign in substance: `grep -cE '^[+-]name = &#124;^[+-]version = '` → 0, so no package was added, removed or version-changed, and all hash/size fields are byte-identical. So this is a metadata-fidelity regression, not a supply-chain substitution — hence `suggested`. Cost if committed: silently downgrades the lockfile format for every future contributor and CI run, discards `upload-time` provenance for ~300 packages (the field that answers "was this artifact published inside the compromise window?" during an incident), and churn-conflicts against main for reasons unrelated to the feature, making the Phase 0 diff hard to review and hard to revert cleanly. Likely origin is the Step 3 bare-`git stash`/`stash pop` recovery (WIP.md records a `uv.lock` conflict as what blocked the pop) or a `uv sync`/`uv run` under a different `uv` binary. Process gap that let it through: Step 10's integration gate asserts "No files were changed by this step" and reports suite + static-check results, but never reconciles `git status` against the union of the steps' declared `Files`. Fix: `git checkout HEAD -- uv.lock`, then re-lock with the project's pinned `uv` only if a refresh is actually wanted; confirm `revision = 2` survives. | uv-lock-undeclared-format-downgrade |
| td-018 | suggested | coverage-gap | goals-to-code | tests/test_architecture_boundaries.py, src/knotica/core/__init__.py:12-16 | adr | dec-060 (vault_layout is a leaf; search may depend on it without a cycle) | verifier | 2026-07-29 | 2026-07-29 | test-engineer | resolved | tests/test_architecture_boundaries.py::test_search_depends_on_core_only_through_the_vault_layout_leaf | The `search → core`-is-`vault_layout`-only invariant is documented in three places and enforced in none. notes-overlay Phase 0 introduced the codebase's first `search → core` import edge (`search/__init__.py` and `search/ripgrep.py` both import `core.vault_layout`) and bounded it with an explicit claim in `core/__init__.py`'s boundary docstring — "The single exception is `vault_layout` … Any *other* inbound edge into this package is a layering violation" — restated in `docs/architecture.md`'s `search/` row and in the ADR. `tests/test_architecture_boundaries.py` read in full: its four fitness tests govern mutation/git/lock discipline (`_imports_core_lock`, mutating store/vcs calls, `core.transaction` as sole writer) and none examines import direction between `search/` and `core/`. Failure mode: a future `from knotica.core.config import …` in `search/ripgrep.py` passes every check, and `search/` — today importable with stdlib plus one pure leaf — silently acquires a transitive dependency on the config/schema layer, weakening the structural backing for the import-purity guarantee that keeps `anthropic`/`dspy` off the MCP launch path. Fix: an AST fitness test asserting every `knotica.core.*` import under `src/knotica/search/` targets `core.vault_layout`, mirroring `_imports_core_lock`'s existing shape (handles `import`, `from … import`, and relative forms). Independently surfaced by the Step 6 forced light-review, which correctly declined to fix it out of scope. **Resolved** by `test_search_depends_on_core_only_through_the_vault_layout_leaf` (AST fitness test covering absolute + relative import forms, all three shapes) plus a non-vacuity guard `test_search_actually_depends_on_vault_layout`; verified red on a deliberately injected `from knotica.core.lint import INDEX_PATH` in `search/ripgrep.py`, then reverted. | search-to-core-layering-edge-unenforced |
| td-019 | important | other | code-to-goals | pyproject.toml | rule | coding-style baseline configuration | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | pyproject.toml [tool.mypy] strict + 126 error fixes across 45 files | RESOLVED 2026-07-29: `[tool.mypy] strict = true` scoped to `src/knotica`, mypy declared as a dev dependency, and all 126 strict-mode errors fixed -- `mypy` now reports 'no issues found in 170 source files'. Two narrow, documented concessions, both properties of an untyped dependency rather than of this code: `ignore_missing_imports` for `dspy`/`dspy.*` (the tree's only stub-less module), and `disallow_subclassing_any = false` for the two modules that must subclass `dspy.Module` -- worth exactly 3 errors, with every other strict check still applying to them. Hand-written dspy stubs were rejected as worse (an untyped fast-moving library's stubs become a confident lie). `tests/` is deliberately out of scope, with the reasoning recorded in the config. The eval instrument fingerprint `harness_version` was captured before the pass and verified byte-identical after (8c34c82e...), so no stored scalar was invalidated and the loop's gate baseline never refroze. Suite unchanged at 1853 passed / 17 skipped throughout. Strict typing paid for itself immediately by surfacing three real defects, filed/fixed separately in-pass: a latent TypeError (`TokenUsage()` built with no arguments where both counters are required), raw `str` passed where a `Literal` is required on LLM-derived guillotine verdicts, and a mis-declared protocol (`CitingPrediction.citations` was settable while every implementer is a frozen dataclass, so the concrete `Prediction` did not actually satisfy its own protocol). ORIGINAL: No type-checker is configured in this project: there is no `[tool.mypy]` or `[tool.pyright]` table in `pyproject.toml`, no `mypy.ini`/`.mypy.ini`/`setup.cfg`/`pyrightconfig.json`, and neither mypy nor pyright is a declared dependency or dependency-group member. The praxion coding-style baseline lists a type-check config as mandatory alongside linter, formatter, `.editorconfig`, pre-commit, and CONTRIBUTING. Consequence is not merely a missing file: it makes every 'mypy clean' claim in a verification or step report ambiguous, because an ad-hoc `uv run mypy <path>` uses default settings that were never agreed. Measured 2026-07-29: unconfigured `mypy src/knotica` reports 69 errors in 24 files of 170 checked, and 12 of them are in `core/loop.py` alone -- so a per-file invocation can read 'clean' or 'dirty' purely on which paths were passed. Surfaced as a registered objection by the implementer of the notes-overlay Phase 0 loop-exclusion rework, which correctly declined to claim mypy cleanliness it could not substantiate. Fix: install the canonical `skills/python-development/assets/mypy-baseline.toml` into `pyproject.toml`, add the tool to a dependency group, and either fix or explicitly baseline the 69 existing errors so future diffs are gated against a known-good state. | no-type-check-config |
| td-020 | important | other | code-to-goals | src/knotica/okf/repair.py:79,98,148-155 | architecture | PRE_PLAN - one git commit per mutating vault operation, flock-guarded | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | src/knotica/okf/repair.py routed through VaultTransaction; tests/test_okf_repair_characterization.py | RESOLVED 2026-07-29: `repair_vault` now performs its page writes AND its report write inside one `VaultTransaction`, producing one flock-guarded commit. `subprocess`, `_git_add_commit` and `_git_head` are gone. PROVEN, not assumed: with the vault lock already held, `repair_vault(apply=True)` raises `LOCK_BUSY` and leaves the tree untouched -- it can no longer race the loop's mutation span. Verified end-to-end: exactly one commit, subject in the frozen grammar (`knotica(repair): okf — native OKF compatibility`), `result.commit_sha` equal to HEAD, report committed, clean tree after. Two design subtleties the fix had to handle, both found by reading rather than assuming: (1) `VaultTransaction` REFUSES `log.md` as a write target (`_normalize_write_path` raises), while repair legitimately canonicalizes the log -- so a new narrow `VaultTransaction.rewrite_log()` escape valve lets the operation declare a full log re-render that the transaction's own entry then composes with; the orchestrator's original design (prefer a buffered `log.md` write) was impossible. (2) A log-only rewrite must still make the transaction effective, since `changed` is computed from `_writes`, which can never contain `log.md`. Result verified: the post-repair log is canonical AND carries the operation entry -- repair now has an audit-trail record of its own runs, which it never had. Also removed a dead `## Rollback` branch: the report was rendered before `commit_sha` was set, so no report ever contained it, and it is impossible by construction (the sha hashes a tree containing the report). The raw-write fitness scan now covers `okf/` via a separate `RAW_WRITE_PACKAGES` constant -- okf is not an adapter, so only the raw-write rule applies to it -- closing the scope carve-out recorded on td-005. Suite 1963 -> 1972. Two further findings from the characterization pass are filed separately as td-021 and td-022. ORIGINAL: SECOND WRITER: `okf.repair.repair_vault` mutates the vault and commits entirely outside `core.transaction`, bypassing the vault flock. Verified by reading the file: line 79 writes content via `store.write_text_atomic`, line 98 calls `_git_add_commit`, and lines 148-155 shell out to `git add` + `git commit -m 'knotica(okf): repair native OKF compatibility'` directly. No `VaultTransaction`, no `vault_mutation_span`, no `core.lock` import anywhere in the module. It substitutes a hand-rolled dirty-tree precondition (line 50, `raise ValueError('git working tree is dirty; ...')`) for the flock, which narrows but does not close the race: the loop's mutation span was recently WIDENED to bracket the whole checkout->merge->branch-delete->commit sequence with crash self-heal, and a concurrent loop mutation can still interleave between this module's dirty check and its commit. The report it writes lands in `vault_root/reports/okf/`, which the vault template's .gitignore does NOT cover, so those files are genuinely committed vault content rather than scratch. Live and user-reachable on two surfaces today: `knotica okf repair --apply` and MCP `vault_health(action=okf_repair, mode=apply)` (`mcp_server/tools_vault.py:36` imports `repair_vault`). Surfaced by the raw-write fitness scan added for td-005, which correctly declined to allowlist it or to fix it. This is why `okf/` is deliberately OUTSIDE that scan's scope -- including it would fail the suite on a pre-existing real defect. Fix: route the write and the commit through `core.transaction.VaultTransaction` like every other mutating operation, then widen the raw-write scan to cover `okf/` and drop the scope carve-out. | okf-repair-second-writer |
| td-021 | important | other | code-to-goals | src/knotica/okf/repair.py:57-77 | architecture | PRE_PLAN - the vault is the user's data; operations touch only what they declare | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | src/knotica/okf/repair.py::_dirty_paths + RepairResult.skipped_dirty | RESOLVED 2026-07-30. Measured first, and the behaviour was worse than this row recorded: the draft was not merely rewritten, it was COMMITTED into the repair's own commit (`knotica(repair): okf - native OKF compatibility`) and the working tree went CLEAN afterwards -- so the user lost the fact that they had uncommitted work at all, with no signal. Content survived inside that commit, so not data loss, but an unrequested commit of work-in-progress and the silent destruction of the working-tree state they were relying on. Ruled out a much broader cause before fixing: a clean tree afterwards would also follow if `VaultTransaction` staged everything, which would mean EVERY mutating operation sweeps foreign dirt. It does not -- `VaultVcs.commit_paths` scopes both the `add` and the `commit` to declared paths, verified empirically (`capture_note` on a dirty vault leaves the dirt untouched and uncommitted). The draft was committed only because `_plan_repairs` chose to rewrite it, making it a declared write. So the fix is one function, not a transaction-layer change. FIX: `_dirty_paths` takes one `git status --porcelain` reading (not a per-path `is_dirty`, which would be N subprocesses) and `_plan_repairs` declines any path that is modified, staged or untracked, recording it on the new `RepairResult.skipped_dirty`. Applied unconditionally -- on a clean tree it is a no-op, so no `--force` special case. The governing distinction: `--force` means 'proceed even though the tree is dirty', NOT 'and adopt my dirt'; those read alike on a command line and differ enormously in consequence. Skips are REPORTED, not silent (repair report section, a CLI line plus a WARN per path, and `skipped_dirty` on the `vault_health(action=okf_repair)` payload) -- declining to touch a file the user may have wanted repaired is only safe if they are told. Dry run surfaces the same list in advance. VERIFIED across four cases: a clean file needing repair is still repaired; the same file left dirty is declined, reported, and byte-identical; the tree stays dirty afterwards; dry run predicts the skip. td-022 deliberately untouched -- its characterization test passes unchanged. ORIGINAL: `okf repair --apply --force` silently rewrites the user's UNCOMMITTED work. The planning loop walks every `.md` in the vault (`iter_page_paths(store)`, repair.py:57) with no filter for what the run was asked to touch, so a foreign in-progress edit is swept into `normalize_concept_frontmatter` and rewritten with injected frontmatter. Surfaced by the td-020 characterization pass, which deliberately used a `.txt` foreign-edit file in its dirty-tree tests to avoid the confound rather than pinning the behaviour. Gated behind `--force` (the default `force=False` dirty-tree guard refuses first), so it is opt-in -- but `--force` reads as 'proceed despite dirt', not as 'rewrite my dirt'. Routing through `VaultTransaction` (td-020) does NOT fix this: the transaction changes how writes are committed, not which files repair decides to rewrite. Fix options: scope the plan to tracked+clean paths, or make `--force` enumerate what it would touch and require a second confirmation. | okf-repair-force-rewrites-uncommitted-foreign-edits |
| td-022 | important | other | code-to-goals | src/knotica/okf/repair.py (report relocated to .knotica/) | code-quality |  | orchestrator | 2026-07-29 | 2026-07-29 | implementer | resolved | _REPORTS_DIR moved under .knotica/ + in-transaction migration of legacy reports | RESOLVED 2026-07-30; severity raised suggested->important because reproducing it surfaced a second, worse defect with the same cause. MEASURED: three consecutive `repair --apply` runs on a vault needing nothing produced three commits, each changing only the report from the run before; and every dry run afterwards permanently claimed the vault needed repair, because the report re-triggered itself. THE WORSE DEFECT: the report landed in a vault-ROOT `reports/`, which is not a reserved top-level name, so topic enumeration treated it as a topic -- `okf repair` invented a phantom topic that appeared in `wiki_status` (`['agentic-systems', 'reports']`), the topic count, `list_topics`, and the session topic-seed. Same shape as the notes leaks: an unreserved vault-root folder mistaken for knowledge content. `guillotine` never had this because it nests reports under `<topic>/reports/guillotine`; `okf repair` is vault-wide and has no topic to nest under, so root was the wrong answer. FIX: `_REPORTS_DIR` moved to `.knotica/reports/okf`. `.knotica/` is committed by explicit design (the vault template's own .gitignore says so), so the audit trail is preserved, and it is dot-prefixed, so `iter_page_paths` skips it -- killing the re-normalisation loop at the root rather than special-casing the report's path, and removing the phantom topic. One constant, both defects. Deliberately NOT solved by reserving `reports` or adding a third folder family: that is machinery for a directory with exactly one writer. MIGRATION (user-authorised): a vault that already ran repair keeps its leftovers and therefore its phantom topic, so repair now relocates `reports/okf/*` into the new location INSIDE the same `VaultTransaction` -- one commit, not a second unguarded write. This required a new `VaultTransaction.delete`, which shares `write`'s guards (refuses absolute paths and `log.md`), supersedes a pending write for the same path, and no-ops on an already-absent file. Only `reports/okf/` is reclaimed and the root `reports/` directory is removed only if it ends up empty -- a user's own files under `reports/` survive, verified. Relocation is reported on the CLI, in the MCP payload and in the report body; the orchestrator added the CLI and MCP halves, which the implementer had left on the result object only, since a silent move of files visible in Obsidian is the same class of surprise this pass exists to remove. VERIFIED: 3 runs -> 1 commit; leftovers migrate in 1 commit with the phantom topic gone from the real `wiki_status` topic list; a second run moves and commits nothing; a foreign `reports/mine.md` and its directory survive untouched. NOTE for the live vault: existing `reports/okf/*` there are untouched by this change until `knotica okf repair --apply` is run against it, which is the user's call -- that commit wakes the loop watcher. ORIGINAL: `okf repair --apply` is not idempotent: it commits a report into `reports/okf/`, and because `iter_page_paths` walks every non-dot directory (including a vault-root `reports/`), the NEXT run sweeps up that report as a concept file, re-normalizes it and commits again. Measured and pinned by the td-020 characterization pass: a second same-day apply yields `files_changed == ['reports/okf/<date>-okf-repair.md']` plus a second distinct commit. Net effect is an unbounded commit per invocation even when the vault needs no repair. Not harmful, but it pollutes the audit trail the one-commit-per-operation invariant exists to make meaningful. Fix options: exclude the report family from the plan (cleanest -- a report is an artifact of the operation, not vault content the operation repairs), or gitignore `reports/okf/` (weaker: the report is deliberately committed content today). | okf-repair-not-idempotent-sweeps-own-report |
| td-023 | important | other | code-to-goals | src/knotica/core/transaction.py (resolved at the chokepoint) | architecture | PRE_PLAN - one git commit per mutating vault operation; scored surfaces must not absorb un-scored text | orchestrator | 2026-07-30 | 2026-07-30 | implementer | resolved | src/knotica/core/transaction.py::neutralize_wikilinks | RESOLVED 2026-07-30; severity raised suggested->important on measurement -- the exposure was live, not theoretical. Measured against `curate_example`, which has nothing to do with notes: a curated eval question whose text quotes a page by wikilink took orphan violations 1 -> 0. The act of curating a training example moved the `lint_violations` leg of the very scalar that example feeds. Fixed at the chokepoint rather than per-operation: the title enters through `VaultTransaction.__init__` and from there feeds BOTH the commit subject and the log entry, so `neutralize_wikilinks` there covers every operation present and future, and `capture_note`'s local `_delink` was removed as redundant. `transaction.py` (536 lines) is not in the file-size ratchet baseline -- which is why this location was available where `records.py` was not. The earlier judgement that the fix had to wait for a `records.py` budget was simply wrong about where the title enters. One subtlety: titles are truncated upstream, so a wikilink can be cut in half, and a dangling `[[` is NOT inert -- the log appends `([[<written path>]])` after the title and a scanner will close the orphaned bracket against that group, forging a link. The neutraliser flattens fragments too. Verified: orphan count 1 -> 1, log carries no wikilink. ORIGINAL: GENERALISATION of a confirmed defect fixed in the notes layer. Any operation that puts user-authored free text in a `VaultTransaction` title slot launders that text into `log.md`, and `log.md` is a vault-root file whose family is `page` -- a SCORED family. `lint._vault_link_map` scans every page's full text for `[[...]]` with no position awareness, so a wikilink appearing anywhere in a title becomes a genuine inbound link from a scored source, suppressing a `PAGE_ORPHANED` violation on its target and moving the `lint_violations` leg of the eval scalar. Phase 0's `_is_scored_link_source` filter cannot catch this: it excludes links whose SOURCE is a note-family file, and here the source really is `log.md`. MEASURED in the notes layer (an isolation test captured a note whose body contained a wikilink and watched its target stop being reported as orphaned) and fixed there by neutralising wikilink syntax in `capture_note._title`. The remaining exposure is `curate_example`, whose title is the caller-supplied `query` -- a plausible carrier, since a curated question may well quote a page by wikilink. `create_topic`'s title is a validated topic slug and cannot contain brackets, so it is not exposed. Deliberately NOT fixed generically at the `records.py` log-entry layer, which would be the structurally right home: that module is 947 lines and sits in the file-size ratchet's frozen over-ceiling baseline with zero slack, so the fix belongs in a pass that can also shrink it. Fix options: neutralise wikilink syntax in the shared title-rendering path (needs the `records.py` budget), or repeat the notes-layer guard per operation (cheap, but a third repetition is the point at which it should be hoisted). | user-text-title-launders-wikilinks-into-scored-log |
| td-024 | suggested | other | code-to-goals | src/knotica/core/operations/promote_note.py | architecture | PRE_PLAN - one git commit per mutating vault operation | orchestrator | 2026-07-30 | 2026-08-02 | implementer | resolved | 2026-08-02 promote_note stamps `promoted: eval:<qa-id>` via curate_example's new extra_writes hook | RESOLVED 2026-08-02 for the trainset side, which is this row's entire scope (its own text: the gap path already carries reverse traceability via `reported_reason = note:<path>#0`, so only TRAINSET promotions were untraceable). Fix option 1 was taken as written -- an optional `extra_writes` callback on `curate_example`, additive, default None preserving every existing caller -- so the stamp rides curate's own VaultTransaction and the crossing stays exactly one commit (the frozen one-commit test still passes). `promoted` is a first-class NoteDocument field rather than a loose frontmatter key because `serialize_note` emits a fixed field set: an unmodelled key would be silently dropped the first time `reanchor`/`detach` round-tripped the note, erasing the audit trail exactly when the note is corrected (regression test: test_a_promoted_notes_stamp_survives_a_later_reanchor). Written through the new byte-preserving `page.set_frontmatter_scalar` so this write path does not repeat `td-027` on the frontmatter. TWO SLIVERS DELIBERATELY LEFT, both documented in the code: a gap-target promotion is not stamped (threading extra_writes through report_gap -> _file_synthetic_gap -> write_gap_records touches a path other callers share, and gaps are already traceable in reverse), so a gap-promoted note reads `promoted: none`; and a duplicate promotion is not stamped, since extra_writes runs only on a real append -- the count of note-derived QUESTIONS stays correct, which is what dec-059's trigger asks for. ORIGINAL FINDING: A promoted note carries no durable record, on the note itself, that it was promoted to the trainset. INTERFACE_DESIGN section 6 defines a `promoted:` frontmatter scalar (`none` / `gap:<id>` / `eval:<id>`) as the audit trail for the opt-in boundary crossing, visible to the human who owns the note; it is NOT implemented. The orchestrator originally ruled it stored in the same commit as the dataset write (ruling P2) and the implementer registered an objection rather than complying, correctly: `curate_example()` and `report_gap()` each own a private `VaultTransaction`, so a note-frontmatter write can only join their commit by extending BOTH shared operations with an extra-writes parameter, and a separate second commit would break the one-commit-per-operation invariant plus the frozen test asserting exactly one. The objection was accepted and P2 reversed. The gap is narrower than it first appears and ASYMMETRIC: the gap path already carries reverse traceability, since `reported_reason` records `note:<path>#0` and a grep of gaps.jsonl finds it, so only TRAINSET promotions are untraceable from the note side. Deriving the trainset case is not available either -- nothing in a QARecord identifies the source note, and putting a note pointer into qa.jsonl is forbidden by the contamination rulings and by three shipped tests that assert the note body and path reach no scored surface. Fix options, in preference order: (1) add an optional extra-writes parameter to `curate_example` (small, additive, default preserves every existing caller) and pass the note file through it; (2) accept two commits and relax the one-commit test for this operation only, which weakens a project-wide invariant for one feature and is not recommended; (3) leave it, and rely on git history plus the dataset record. Deferred out of Phase 2 deliberately: the crossing still happens, is human-gated, produces exactly one commit, and is visible in the response payload via `NoteActionResult.promoted_to`. | promote-note-frontmatter-audit-trail-absent |
| td-028 | important | other | code-to-goals | src/knotica/core/operations/promote_note.py, src/knotica/core/records.py | adr | dec-059 - revisit when the trainset holds >=10 note-derived questions | orchestrator | 2026-08-02 | 2026-08-02 | implementer | resolved | 2026-08-02 `promoted: eval:<qa-id>` note stamp makes the count a grep over notes/<topic>/ | RESOLVED 2026-08-02 by the same change that closed `td-024`: notes now carry `promoted: none / gap:<id> / eval:<id>` and a trainset promotion stamps `eval:<qa-id>`, so `dec-059`'s trigger is evaluable by grepping `promoted: eval:` across the topic's notes. The trigger itself was restated against the recorded form in the re-affirmation ADR, since the original wording counted a quantity nothing recorded. Chose the note-side mechanism over adding a note-derived QA_SOURCES member specifically to keep note provenance OFF a scored surface -- `qa.jsonl` is untouched, the contamination rulings need no re-litigation, and compile demo selection is unaffected. Residual: nothing SURFACES the count to a human yet (no `wiki_status` field), which is the re-affirmation ADR's own falsifier 2 -- technically observable but practically dead if nobody runs the grep. ORIGINAL FINDING: `dec-059`'s reversal trigger -- "revisit when the trainset holds >=10 note-derived questions and a compile/eval cycle has run over them" -- CANNOT EVER FIRE, because nothing records that a trainset question came from a note. Traced end to end: `promote_note(target="trainset")` delegates to `curate_example` passing NO source override; `curate_example` always stamps `source="curate_example"`; `QA_SOURCES` is exactly {curate_example, distillation, seed_train} with no note-derived member; the commit and `log.md` title are derived from the QUERY, not the note; `NoteActionResult.promoted_to` is a response-payload field that persists nowhere in `src/`; and the `promoted:` frontmatter scalar that would carry the note-side audit trail is specified but unimplemented (see `td-024`). A note-promoted trainset question is therefore byte-indistinguishable from a hand-curated one. `td-024` recorded the same asymmetry (gap promotions carry `reported_reason = note:<path>#0`, trainset promotions carry nothing) but framed it as a per-note audit-trail gap; the consequence for `dec-059` was not drawn -- a deferral conditioned on an unobservable trigger becomes PERMANENT BY ACCIDENT, and silently, since nobody is ever prompted to notice. Currently harmless: measured 2026-08-02, zero notes exist in either configured vault (`notes/` has never appeared in either history), so the count is 0 and no signal has been lost yet. It stops being harmless the first time a note is promoted. FIX OPTIONS: (1) add a note-derived member to `QA_SOURCES` and pass it through `curate_example` -- counting becomes a grep, but the contamination rulings must be checked first (a source LABEL carries neither note body nor note path, so it appears legal against the three shipped tests asserting neither reaches a scored surface -- that check IS the decision and is not made here); (2) implement `td-024` option 1 (optional extra-writes parameter on `curate_example`) and count the notes tree instead, keeping `qa.jsonl` untouched -- more conservative on contamination, wider change. Evidence: docs/designs/notes-overlay/STEP3_GOLDEN_PROMOTION.md. | dec-059-reversal-trigger-unobservable |
| td-011 | suggested | other | goals-to-code | src/knotica/core/loop.py:200-222, src/knotica/cli/loop.py | code-quality |  | orchestrator | 2026-07-22 | 2026-07-22 | implementer | resolved | cdbfcc7 (feat: configurable eval cadence — added tests/test_td011_eval_rearm.py) | Failed observation eval consumes the cursor (anti-storm by design) but leaves no retry affordance: loop --once no-ops with 'only bookkeeping changed' and the failed HEAD is never re-scored; operator must know to reach for `knotica eval --topic` (cursor-free) or wait for the next content commit. Surfaced in first live E2E (rate-limit-starved eval, 25/25 unscored). Options: a `loop retry`/`rebaseline`-adjacent re-eval affordance, or stage:failed surfacing a fix-command in wiki_status/nudge. RESOLVED (eval-cadence-model-config, 2026-07-22): observe_default()'s failure branch no longer calls mark_processed — the cursor stays unadvanced and `pending_retry=True` is set; a `_failure_retry_hold` floor (60s, same-content-gated) prevents immediate re-hammering while still allowing prompt retry once elapsed. Proof: tests/test_td011_eval_rearm.py (4 tests) + tests/test_loop_cadence_characterization.py; independently reviewed (Steps 8/8b forced light-review, verdict PASS after one revision cycle). | cc276c585845 |
| td-013 | suggested | other | code-to-goals | src/knotica/core/loop_progress.py:62-64 | code-quality |  | orchestrator | 2026-07-24 | 2026-07-24 | implementer | resolved | dec-054 (fe96d46) | Per-question progress heartbeat write is not concurrency-safe: all 4 parallel scoring threads share one `.tmp` path, so `tmp.write_text` + `os.replace(tmp, path)` interleave and a thread can lose the race (`[Errno 2] .tmp -> .json`), and the escaping exception makes dspy cancel the whole eval run (`Execution cancelled due to errors or interruption`). Observed live 2026-07-24 04:39 during a rate-limit-starved observation eval; benign to loop state (re-arms normally) but aborts otherwise-viable runs. RESOLVED (eval-error-visibility, 2026-07-24): `write_progress` now allocates a unique per-write temp file via `tempfile.mkstemp(dir=path.parent)` (no shared rename target, race-free by construction) and the entire write is wrapped non-raising (`OSError` swallowed, logged at debug, temp file cleaned up on failure) so a progress hiccup can never again cancel a run; proof is `tests/test_loop_progress.py`'s 16-thread barrier-synced concurrency regression test, run 3x green. Step 11's single-writer lock in `loop.py` (not yet landed) further hardens *coherence* for the new multi-thread `examples` list on top of this already-resolved race. | loop-progress-tmp-rename-race |
| td-007 | suggested | coverage-gap | code-to-goals | src/knotica/evals/llm.py:337-352, 384-419 | code-quality |  | verifier | 2026-07-18 | 2026-07-18 | implementer | resolved | 9e9d5af (fix: raise eval LLM in-process retry budget) | `complete()` has no in-process retry loop: a 429/529/5xx is flagged `retryable=True` on the envelope but re-raised immediately — metered mode relies on the SDK's own backoff, OAuth mode on manual re-run. Surfaced by gapfill-substrate verifier as a follow-up. RESOLVED 2026-07-18 at pipeline close-out: both SDK client constructions now pass `max_retries=_SDK_MAX_RETRIES` (5; SDK default was 2) so transient 429/5xx/connection errors retry in-process with the SDK's backoff honoring retry-after; regression test `test_sdk_client_carries_the_raised_in_process_retry_budget` covers both auth modes. A hand-rolled jitter loop was rejected as duplicating SDK behavior | llm-429-no-inprocess-retry |
| td-010 | suggested | drift | goals-to-code | .ai-work/gapfill-queue/SYSTEMS_PLAN.md:426-429 (§P4 Consumer Contract #4), .ai-state/decisions/drafts/20260719-1210-...-suggestion-record-committed-queue.md (body §Decision/Consequences) | adr | dec-030 | verifier | 2026-07-19 | 2026-07-19 | systems-architect | resolved | 97741d6 (docs: close P3 pipeline — ADR addendum) | P3→P4 hand-forward drift: both the SYSTEMS_PLAN §P4 Consumer Contract and the ADR body reserve an `ingest_ref` field and mark the `ingested` transition P4-only, but as-built ships `ingested_at` (no `ingest_ref`) and implements `mark_ingested` as a live P3 action (per INTERFACE_DESIGN §D1/§D2). The ADR amendment reconciled 3 of 4 §D2 deltas (five states, decided_reason, no proposer_version) but missed this one. SYSTEMS_PLAN is ephemeral; the ADR finalizes to dec-NNN and will mislead P4 (look for a non-existent `ingest_ref`, re-implement an existing mark_ingested). Fix = one-line ADR amendment addendum: field is `ingested_at`, `mark_ingested` enum slot lands in P3, calling ownership is the P4 coordination note | p3-p4-handforward-ingest-ref-drift |
