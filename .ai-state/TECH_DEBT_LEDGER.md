# TECH_DEBT_LEDGER

Active debt rows (`status ∈ {open, in-flight}`). Terminal rows migrate to `TECH_DEBT_RESOLVED.md` at post-merge finalize. Writers: verifier, sentinel, orchestrator, architect-validator only. Schema: praxion `skills/software-planning/references/tech-debt-ledger.md`.

**The file-size rows (td-002, td-008, td-009, td-042) share one bounding instrument.** `tests/test_file_size_ratchet.py` scans `src/knotica` and `tests/`, each with its own baseline, under three rules parametrised over both roots: a baseline module may shrink but never grow, a non-baseline module may never cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three were proven to fire before landing. The rows below therefore record **bounded, unpaid** debt — the ratchet stops the growth, not the debt. Stating the instrument once here rather than per row is deliberate: the paragraph was previously duplicated verbatim across three rows, and a single edit to the gate falsified all three at once without any of them noticing. Measured 2026-08-05: `src/knotica` carries 1,196 lines of overage across five modules; `tests/` carries 5,407 across eleven — the larger debt is in the test tree by 4.5×.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|------------|--------|-------------|-------|-----------|
| td-002 | important | complexity | code-to-goals | src/knotica/evals/harness.py, src/knotica/evals/golden.py | code-quality | coding-style - file size hard ceiling 800 lines | verifier | 2026-07-16 | 2026-08-05 | implementer | open |  | Measured 2026-08-05: `harness.py` 1214 (+414), `golden.py` 975 (+175); both equal their ratchet baselines, so neither has grown. Bounded by the shared instrument above. The split stays deliberately deferred: `harness.py` is the eval instrument, and changing it rotates `harness_version` and refreezes every topic's baseline — that cost belongs to its own pass, not to a feature phase. Split by cohesion via the refactoring skill, or accept with rationale. Prior remeasurements had drifted: the row simultaneously asserted 1214/975 in one sentence and 1200/970 in the next, so a reader could not tell which half was authoritative; corrected here to the single measured pair. | 370309c1624b |
| td-008 | important | complexity | code-to-goals | src/knotica/core/loop.py | code-quality | coding-style - file size hard ceiling 800 lines | verifier | 2026-07-19 | 2026-08-05 | implementer | open |  | Measured 2026-08-05: 1125 lines, 325 over; ratchet baseline 1146, so the file has shrunk 21 lines and cannot grow. Status corrected `in-flight` → `open`: nothing is underway. The last structural work was the 2026-07-22 third-pass extraction (`build_loop_runner` → `core/loop_factory.py`, `_run_arena_and_resolve` → `core/arena_resolve.py`, the candidate-gate cluster → `core/candidate_gate.py`, characterization-tests-first, two forced light-reviews both PASS); every commit since is a bug fix, no `WIP.md` exists under `.ai-work/`, and no loop-refactor branch exists. `in-flight` claimed for two weeks with no work suppresses the row from reading as actionable backlog. The remaining move is smaller than previously recorded: `observe_default` measures **264** lines (loop.py:207-470), not 397, and the gap-classification sub-cluster measures ~93 across `_classify_and_persist_gaps` and `_record_classification_failure`, not 147. That correction matters because the estimate is what the deferral rests on. Next move: a fourth pass targeting `observe_default`, once that code has proven stable in production or a compelling reason forces revisiting it. | loop-py-file-size-ceiling |
| td-009 | suggested | complexity | code-to-goals | src/knotica/core/records.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-07-19 | 2026-08-05 | implementer | open |  | Measured 2026-08-05: 947 lines, 147 over, equal to its ratchet baseline. Growth history 719 → 858 → 947 as `GapRecord` (P1) and `SuggestionRecord` (P3) landed. Cohesive record-family module; split by record family (eval / gap / suggestion / note) or accept with rationale. Deferred per the user's ceiling directive alongside td-002 and td-008. | records-py-file-size-ceiling |
| td-042 | suggested | complexity | code-to-goals | src/knotica/core/gapfill.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-08-05 | 2026-08-05 | implementer | open |  | 935 lines, 135 over, equal to its ratchet baseline. Filed because the module was invisible to the ledger's own schema: it has sat in `OVER_CEILING_BASELINE` since 2026-07-29 and is named in three other rows' prose, but appeared in no row's `location` field — so any consumer querying the ledger by location rather than reading prose found nothing. The module carries three distinct responsibilities the split should follow: **file** (`report_gap`, `file_retracted_gap`, and the classifier's own writes — the three gap origins), **drain** (`refresh_suggestions_for_gaps`, `build_suggestion_records`, `formulate_query`, `build_default_discovery_service`), and **decide/gate** (`plan_decision`, `apply_decision`, `apply_gate_outcome`). Watch, not debt: `guillotine/report.py` sits at 799 with no baseline entry, so ratchet rule 2 fires on its next two-line addition; `tests/test_evals_llm.py` (792) and `tests/test_mcp_write.py` (790) are 8 and 10 lines from the same trip. **UPDATED 2026-08-10 - the measurement above is superseded and the baseline was RAISED.** The gap-lifecycle writers landed here (a merged gate verdict closes the originating gap; `apply_gap_decision` gives a human the dismiss/reopen transition), taking the file 944 -> **1115** and `OVER_CEILING_BASELINE` with it: **+171, now 315 over the ceiling, 39% over**. Justified in place in `tests/test_file_size_ratchet.py`, and NOT taken on cohesion - the alternative was measured. Moving `apply_gap_decision` alone to a new `core/gap_lifecycle.py` sheds only **99-132** lines, because three shared helpers serve the machine half and the drain both; that lands 983-1016, still 183-216 over, so **the baseline rises either way** (+39..+72 instead of +171) at the cost of a 4-file crossing (`DESIGN.md` 3a 62->63, `docs/architecture.md`, `TEST_TOPOLOGY.md`). Worse, it cuts on a FOURTH axis rather than this row's file/drain/decide-gate, stranding `report_gap`/`file_retracted_gap` - the queue's other writers - and making this debt harder to pay, not easier. THE RIGHT CUT IS NOW MEASURED: the whole gap-queue writer set, ~330 lines, which may **retire** this entry rather than shrink it. That is a `[Phase: Refactoring]` step with two public import paths, not a feature-step extraction. Recorded because a ratchet is only an instrument if each raise costs a decision - this one did. | gapfill-py-file-size-ceiling |
| td-026 | suggested | other | code-to-goals | dashboard/src/app.css, dashboard/src/VaultPane.tsx, dashboard/src/LoopPane.tsx, dashboard/src/types.ts | architecture | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-07-31 | 2026-08-05 | implementer | open |  | REMEASURED 2026-08-05 and the original inventory was wrong in both directions. `dashboard/src/toolClient.ts` was named as an offender at "~750 and growing" — it measures **748, under the ceiling**, and was under when the row was filed; it has not changed since. Meanwhile **two larger offenders went unnamed**, both predating the row: `VaultPane.tsx` **1366** (unchanged since 2026-07-18) and `LoopPane.tsx` **1358** (unchanged since 2026-07-24), each bigger than the `types.ts` 1117 the row does track. `app.css` is **2890**, 3.6× the ceiling — whether the file ceiling binds a stylesheet is a judgement no document has made, and unstated it defaults to nobody looking, which is this row's own complaint. So the row that exists to say the ceiling silently does not apply to a fifth of the shipped source undercounted that source by its three biggest files; its own method — measure rather than trust — was not applied to itself. THE STANDING FINDING IS UNCHANGED AND NOW SHARPER: `tests/test_file_size_ratchet.py` scans `src/knotica` and `tests/` only, so `dashboard/src` has no gate at all and is the only shipped source tree in that position. Two ways to close it: extend the ratchet's scan roots to `dashboard/src` (cheap; needs a baseline seeded with the four files above, and a decision on whether `.css` is in scope), or split by domain (notes / sources / arena / loop), which is the fix `types.ts` and the two panes actually want. The second is preferable and larger. | dashboard-typescript-invisible-to-file-size-ratchet |
| td-030 | suggested | complexity | code-to-goals | tests/test_mcp_notes.py, tests/test_evals_harness.py, tests/core/notes/test_anchor.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-08-02 | 2026-08-05 | test-engineer | open |  | Eleven test modules over the ceiling; remeasured 2026-08-05, still exactly eleven, none new, none grown. `test_mcp_notes.py` 2506 (more than 3× the ceiling), `test_evals_harness.py` 1565, `core/notes/test_anchor.py` 1534, `core/notes/test_resolve.py` 1489, `test_evals_golden.py` 1372, `core/notes/test_reanchor_note.py` 1111 (baseline records 1112; it shrank by one), `test_search.py` 1086, `core/notes/test_capture_note.py` 950, `core/notes/test_store.py` 938, `test_transaction.py` 847, `test_cli_eval.py` 809. Total overage **5,407 lines — 4.5× the source tree's 1,196**, which is the number a reader prioritising paydown most needs and which the row previously did not state. GROWTH IS CLOSED: the ratchet gained `tests/` as a second scan root with its own baseline on 2026-08-02, parametrised over both trees so a rule added for one is added for both; the gate was verified to fail (a 900-line probe file was rejected by rule 2 with the correct message, and the suite went green on its removal). WHAT REMAINS is the paydown, deliberately not demanded here for the same reason the source baseline does not demand it: splitting a 2500-line behavioural test file risks losing coverage in the move and should be done per-file by someone with the domain in context, not as a bulk sweep. Priority is by size and edit frequency — `test_mcp_notes.py` is both the largest and among the most frequently touched. Several of these grew during the notes-overlay phases, so their baselines record a high-water mark that includes those additions. | test-tree-invisible-to-file-size-ratchet |
| td-046 | suggested | correctness | code-to-goals | src/knotica/core/config_write.py:153, src/knotica/core/config_write.py:210 | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | implementer | open |  | Table HEADER segments are guarded by `is_bare_key` (config_write.py:51-54) as of the 2026-08-05 vault-name fix; the two value-emitting lines are not. Both build `f"{key} = ..."` and assume every key is a TOML bare key. Reachable only via a hand-written quoted key in config.toml, so rarer than the vault-name case that motivated the guard, but it lands in the identical amplifier: `read_config` answers a TOMLDecodeError with an empty dict (config_write.py:52-53), so the NEXT write rebuilds the config from nothing and silently drops every vault, default_vault, [loop], [models] and [gapfill]. Fix is roughly 2 lines reusing `is_bare_key`. ONE DECISION THE FIX MUST MAKE RATHER THAN INHERIT: rejecting beat quoting for vault NAMES because a name is typed back as a `--vault` flag and a tool argument, and that argument does not apply to an arbitrary config key, so quoting may be the better answer here. Not yet characterized - write the failing test first, per tests/CLAUDE.md. | config-write-non-table-keys-unvalidated |
| td-047 | suggested | other | code-to-goals | src/knotica/core/config_write.py, src/knotica/core/config.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | implementer | open |  | A vault named with a dot BEFORE the 2026-08-05 bare-key guard landed has already round-tripped into genuine TOML nesting (vaults then my then name), so the guard sees two individually-valid segments, never fires, and the file re-emits as a fixed point. Benign rather than silent: `list_vaults` surfaces it as a vault with no path configured, so the phantom entry is visible to a user. Newly-introduced dotted names are rejected at the seam, so the exposure is bounded to configs written earlier. DECIDE, do not reflexively fix: (a) accept and document, (b) detect-and-warn in `knotica doctor`, (c) offer a migration. Establish the population first - the two vaults configured on the authoring machine were `main` and `decision-making`, neither dotted, so the affected set may be empty and (a) may be correct. Owner proposed as implementer; escalate to systems-architect only if (c) is chosen. | pre-existing-dotted-vault-names-unreachable |
| td-048 | suggested | drift | code-to-goals | vault:main/agentic-systems/reports/guillotine/2026-07-07-reasoning-only-systems-hallucinate-because-they-are-closed.md, vault:main/agentic-systems/reports/guillotine/2026-07-08-reasoning-only-systems-hallucinate-because-they-are-closed.md | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | doc-engineer | open |  | Commit 5706f22 removed `PassageRole.DEPENDS_ON` - declared, wired into four lookups, advertised in the report legend, and never assigned by the classifier, so the legend documented a verdict the code could not produce. Reports generated since no longer carry the line. Two already-committed reports still do, both in the `main` vault at ~/dev/data/knotica, NOT the currently-active `decision-making` vault - so acting on this means switching vaults deliberately. LOCATION IS OUTSIDE THIS REPO: these are vault data, and the vault is the user's data, so no code change reaches them and no gate will ever flag them. Defensible to leave as dated artifacts of past runs. If corrected, the write MUST go through core.transaction like any other vault mutation - one git commit per mutating operation is an invariant, and hand-editing the vault bypasses the flock and the log append. | guillotine-reports-carry-retired-depends-on-legend |
| td-049 | important | other | code-to-goals | tests/test_hooks_session_start.py, hooks/session_start.sh:74 | test-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | test-engineer | open |  | `test_configured_vault_emits_the_topic_list` is load-sensitive and fails intermittently in full-suite runs while passing standalone and passing when its own file runs alone. MECHANISM, confirmed at source rather than inferred: hooks/session_start.sh:74 bounds its warmth probe with `run_bounded 1 uvx --from "$ROOT" knotica --version`, a hard one-second budget. Under the ~2,690-test suite the probe overruns, the hook takes its cold-path early exit, and stages (d) through (f) - including the topic-list nudge the test asserts on - never emit. OBSERVED TWICE on 2026-08-05: failed in one full `make verify`, passed in the next, under byte-identical code. Filed as important rather than suggested because an intermittent failure in the canonical gate erodes trust in the gate itself and already cost investigation time. The test is not wrong; the coupling to wall-clock is. Fix by injecting the bound so the test can pin a generous value, or by stubbing the probe so the test exercises nudge logic rather than machine load. DO NOT widen the production budget - one second is a deliberate choice keeping the hook off the session critical path. | session-start-hook-test-load-sensitive |
| td-050 | suggested | other | code-to-goals | src/knotica/service/manager.py, src/knotica/core/loop_factory.py, src/knotica/cli/loop.py | adr | dec-078 | orchestrator | 2026-08-05 | 2026-08-05 | implementer | open |  | Commit 450ccd5 wired the arena scorer into the OS-supervised daemon and into `loop action=run_eval`, matching what dec-044 and DESIGN.md had always claimed and closing a silent no-op. CONSEQUENCE, recorded here because it is residual risk rather than a defect: the daemon now mutates `<topic>/.knotica/prompts/query.md` UNATTENDED on an arena win, plus roughly seven arena-state commits per race at the default arena_n=4. `--no-arena` exists only on `knotica loop` (cli/loop.py); there is no daemon-level or config-level off switch. dec-078 names the remedy explicitly: if unattended arena promotion degrades a topic's prompts, add a config knob - and NOT a re-omission of `arena_score`, which is exactly the bug that was fixed. WATCH ITEM, not actionable yet: the trigger is an observed prompt regression on a supervised topic. Filing it so the trigger is written down somewhere a future reader will look, rather than living only in one ADR's Consequences prose - which is the failure mode dec-074 was itself filed to correct. | daemon-has-no-arena-off-switch |
| td-051 | suggested | correctness | code-to-goals | src/knotica/core/gapfill.py:681 | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | implementer | open |  | `_build_synthetic_gap` hardcodes `reference_pages_exist=False` on the reported/retracted gap path, alongside the genuinely-zero eval fields (`scalar_at_detection`, `baseline_scalar`, empty evidence, empty `manifest_ref`). Unlike those, this one is not a placeholder for an absent measurement — it is a CHECKABLE property being asserted false without checking. The eval path computes it honestly at gap_classifier.py:155 (`any(store.exists(page_path(topic, page)) ...)`). MEASURED on the live decision-making vault: the gap filed 2026-08-06 records `False` while all three of its reference pages exist on disk. INERT TODAY, which is why this is suggested and not important: grep finds no reader outside the classifier's own `_classify_one` (gap_classifier.py:269), which only ever sees the eval path's honestly-computed value, and `gaps_read`/the dashboard surface the field but branch on nothing. The risk is a future consumer trusting it — a UI badge, a discovery heuristic, a re-classification pass — at which point every reported gap silently claims its references are missing. Fix is to compute it the same way the eval path does; the store is already in scope at the call site. Characterize first: assert the current False, then flip it. | reported-gap-reference-pages-exist-hardcoded-false |
| td-052 | important | other | code-to-goals | .github/workflows/dashboard.yml | code-quality |  | orchestrator | 2026-08-10 | 2026-08-10 | cicd-engineer | open |  | `dashboard.yml` is the **only** one of the six workflows with **no `permissions:` block and no SHA-pinned actions**. MEASURED 2026-08-10 across all six: `ci.yml`, `release.yml`, `ci-autofix.yml`, `cross-model-review.yml` and `labels-reconcile.yml` each carry a `permissions:` block and pin at least one action to a full 40-char SHA; `dashboard.yml` carries zero of both. The two gaps compound rather than sit side by side: without an explicit `permissions:` block the job inherits the repository default `GITHUB_TOKEN` scope, and the job runs `npm ci`, which executes install scripts from the whole dependency tree — third-party code under an unaudited token, with actions resolved by mutable `@v4` tags rather than by digest. Bounded, not urgent: a fork `pull_request` token is read-only regardless of the block, so the exposure is the `push` path and any same-repo branch. Scope note — `timeout-minutes` (2 of 6) and `concurrency` (1 of 6) are NOT outliers and should not be folded in as if they were; the reported "outlier on four axes" is really an outlier on two. Surfaced by the `cicd-engineer` during Step 45 and deliberately NOT fixed there: the step's job was to make the dashboard test suite run in CI at all, and widening it to harden the workflow would have risked the gate it was landing. Fix is self-contained — add a least-privilege `permissions:` block and pin the actions to digests, matching `ci.yml`. | dashboard-workflow-no-permissions-no-sha-pinning |
| td-053 | important | other | code-to-goals | dashboard/, dashboard/package.json | code-quality | coding-style - baseline linter and formatter configuration | orchestrator | 2026-08-10 | 2026-08-10 | implementer | open |  | MEASURED 2026-08-10: `dashboard/src` carries **11,830 lines** of `.ts`/`.tsx` and the repo has **no** biome, eslint or prettier configuration at any level — verified by `find` for `biome.json` / `eslint.config.*` / `.prettierrc*` outside `node_modules`, which returns nothing. `npm run build` runs `tsc --noEmit`, so the TypeScript half of the shipped source is **type-checked but neither linted nor format-checked**, while the Python half is gated by `ruff check` **and** `ruff format --check` as two of `make verify`'s seven steps. The coding-style rule states a linter config and a formatter config as baseline configuration every managed project must carry, and names the canonical TS assets (`skills/typescript-development/assets/{biome.json, eslint.config.mjs, prettierrc.json}`) which install idempotently — so the fix is adopting an existing asset, not authoring a standard. Filed `important` rather than `suggested` on the amplifier, not the current state: **M3 dissolves `VaultPane` and adds six lane components to this exact tree**, so every line added between now and then is written without a style gate and normalized afterwards in a diff that will be indistinguishable from behavioural change. Cheapest before M3, most expensive during it. DISTINCT FROM td-026, which records that `dashboard/src` is invisible to the *file-size* ratchet: same blind spot, different instrument, and neither implies the other. Surfaced by the implementer during Step 3, which reported "no formatter/linter is configured for TS in this repo" as an incidental observation while following the universal format-then-lint workflow; independently verified here. Deliberately NOT folded into the swimlane plan — it is the sixth CI/quality item declined on scope grounds, consistent with R2, td-026, the ext-apps SDK bump, td-052 and the dashboard-workflow diff-path cleanup. | dashboard-typescript-no-lint-or-format-config |
