# TECH_DEBT_LEDGER

Active debt rows (`status ∈ {open, in-flight}`). Terminal rows migrate to `TECH_DEBT_RESOLVED.md` at post-merge finalize. Writers: verifier, sentinel, orchestrator, architect-validator only. Schema: praxion `skills/software-planning/references/tech-debt-ledger.md`.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|------------|--------|-------------|-------|-----------|
| td-002 | important | complexity | code-to-goals | src/knotica/evals/harness.py, src/knotica/evals/golden.py | code-quality |  | verifier | 2026-07-16 | 2026-07-18 | implementer | open |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: harness.py 880→1117→1200 lines (grew +122 in gapfill-substrate P0), golden.py 970 vs 800 ceiling; docstring-dominated but now well over — split by cohesion via refactoring skill or accept with rationale; severity raised suggested→important after growth REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | 370309c1624b |
| td-008 | important | complexity | code-to-goals | src/knotica/core/loop.py | code-quality |  | verifier | 2026-07-19 | 2026-07-22 | implementer | in-flight |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: loop.py STILL over ceiling after a third pass (1137 lines vs 800 ceiling, 337 over). History: 924 pre-P1, 1047 post-P1, 1221 post-consolidation, 1347 post-eval-cadence-model-config, 1087 post-loop-py-extraction, 1137 measured 2026-07-29 (2026-07-22: extracted build_loop_runner -> core/loop_factory.py verbatim, _run_arena_and_resolve -> core/arena_resolve.py as a free function, and the candidate-gate cluster (poll_once/next_candidate/process_candidate/keep/discard) -> core/candidate_gate.py as free-functions-on-runner mirroring source_gate.py's idiom; characterization-tests-first, two forced light-reviews both PASS, full suite green throughout). PARTIAL RESOLUTION ONLY — research (loop-py-extraction/RESEARCH_FINDINGS.md) found the ledger's own prior estimate was optimistic: these were the low-risk, low-coupling extractions; genuinely clearing 800 requires extracting the 397-line observe_default cluster (billed-trigger nonce + td-011 fix + cadence guards, dec-048) and the 147-line gap-classification sub-cluster, both deliberately deferred as higher-risk (they are the most recently and carefully adversarially-reviewed code in the file). Next move: a fourth pass targeting observe_default specifically, once that code has had time to prove stable in production, or when a compelling reason forces revisiting it anyway. REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | loop-py-file-size-ceiling |
| td-009 | suggested | complexity | code-to-goals | src/knotica/core/records.py | code-quality |  | orchestrator | 2026-07-19 | 2026-07-19 | implementer | open |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: records.py 719->858->947 lines after GapRecord (P1) + SuggestionRecord (P3) additions crossed the 800 ceiling. Cohesive record-family module; split by record family (eval/gap/suggestion) or accept with rationale. Deferred per user ceiling-directive alongside td-002/td-008 REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | records-py-file-size-ceiling |
| td-025 | suggested | other | code-to-goals | src/knotica/core/notes/candidates.py | architecture | PRE_PLAN - anchor recovery must not depend on where a passage sits on a page | orchestrator | 2026-07-31 | 2026-07-31 | implementer | open |  | The candidate window's FORWARD extension carries the same defect just fixed on the backward side. Backward extension used to run past headings and blank lines to offset 0, so a quote near a page's top was scored against a window that was mostly heading chrome with the quote truncated -- measured at 0.43 near-top versus 0.72 mid-page for the SAME one-word edit, i.e. position on the page dominated the score, and notes anchored near a page top (where opening claims live, so the common case) orphaned when they should not have. Fixed by bounding backward extension at the nearer of a blank-line run, an ATX heading, or the enclosing sentence start; the headline regression test now requires near-top and mid-page to score within 0.05, and they are identical. The forward side was probed directly and has the symmetric hole: an unpunctuated trailing sentence before page-closing chrome swallows it forward. NOT fixed, deliberately: no test exercises it, and every real anchored quote ends in terminal punctuation which already bounds the window, so the fix would be untested speculation about a case nobody has hit. The symmetric fix is sketched in the module docstring (a `_nearest_block_end` mirroring `_nearest_block_start`, combined with `min()` instead of `max()`). Trigger to act: a note whose anchored passage ends without terminal punctuation -- a heading-like fragment, a list item, or a table cell -- resolving worse than its position on the page should warrant. | candidate-window-forward-extension-unbounded |
| td-026 | suggested | other | code-to-goals | dashboard/src/types.ts | architecture | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-07-31 | 2026-08-02 | implementer | open |  | GREW AGAIN 2026-08-02: `types.ts` is now 1117 lines (was 1093 when this row was filed), +24 for the `NoteDrift.cause` field carrying the supersession classifier to the drift UI. Recorded rather than quietly absorbed -- the point of this row is that nothing gates it, so every growth has to be noticed by a human or not at all. Note the asymmetry that makes this sharper: the Python test tree WAS closed off on the same day (`td-030` extended `tests/test_file_size_ratchet.py` to a second root), so `dashboard/` is now the only shipped source tree with no ceiling enforcement at all. ORIGINAL FINDING: The 800-line file-size ceiling is enforced by `tests/test_file_size_ratchet.py`, which scans `src/knotica` only -- i.e. Python only. The TypeScript dashboard is invisible to it, and two files are already over the general coding-style ceiling with no gate noticing: `dashboard/src/types.ts` (1093 lines) and `dashboard/src/toolClient.ts` (~750 and growing). Phase 2 grew `types.ts` further (+156) and the Phase 2 verification pass grew it again. This is not a Python-vs-TypeScript exemption anyone decided on -- it is a blind spot in where the ratchet looks, so the ceiling silently does not apply to roughly a fifth of the shipped source. Two ways to close it: extend the ratchet's scan roots to `dashboard/src` (cheap, and would fail immediately on the two existing files, so it needs a baseline entry like the Python `OVER_CEILING_BASELINE`); or split `types.ts` by domain (notes / sources / arena / loop), which is the fix the file actually wants. The second is preferable and larger. Flagged in Phase 2's LEARNINGS as "pre-existing tech debt outside this phase's scope to fix -- for the verifier or sentinel to formally ledger, not silently carry"; this row is that ledgering. | dashboard-typescript-invisible-to-file-size-ratchet |
| td-030 | suggested | complexity | code-to-goals | tests/test_mcp_notes.py, tests/test_evals_harness.py, tests/core/notes/test_anchor.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-08-02 | 2026-08-02 | test-engineer | open |  | ELEVEN test modules sit over the 800-line ceiling, now BOUNDED but not paid down. The Phase 4 brief flagged `tests/test_search.py` (1086) as the offender; measuring rather than trusting that found it is the SEVENTH largest, behind `test_mcp_notes.py` at 2506 -- more than three times the ceiling -- plus `test_evals_harness.py` 1565, `core/notes/test_anchor.py` 1534, `core/notes/test_resolve.py` 1489, `test_evals_golden.py` 1372, `core/notes/test_reanchor_note.py` 1112, then test_search.py, `core/notes/test_capture_note.py` 950, `core/notes/test_store.py` 938, `test_transaction.py` 847, `test_cli_eval.py` 809. Same blind spot as `td-026` (the TypeScript dashboard), one directory over: `tests/test_file_size_ratchet.py` scanned `src/knotica` only, so the ceiling silently did not apply to the test tree at all. FIXED 2026-08-02 to the extent that matters for GROWTH: the ratchet now scans `tests/` as a second root with its own baseline, parametrised over both trees so a rule added for one is added for both. All three rules apply -- the eleven may shrink but never grow, a new test module may never cross the ceiling, and a baseline entry that falls to the ceiling must be removed. Verified the gate actually fails: a 900-line probe file was rejected by rule 2 with the correct message, and the suite went green again on its removal. WHAT REMAINS is the paydown, which is deliberately NOT demanded here for the same reason the source baseline does not demand it -- splitting a 2500-line behavioral test file is a real refactor with real risk of losing coverage in the move, and it should be done per-file by someone with the domain in context, not as a bulk sweep. Priority order is by size and by how often the file is edited: `test_mcp_notes.py` is both the largest and among the most frequently touched. NOTE for whoever pays it down: several of these grew during the notes overlay phases, so their baselines record a high-water mark that includes this session's additions. | test-tree-invisible-to-file-size-ratchet |
| td-037 | suggested | test-coverage | goals-to-code | .ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md | spec-req | SPEC_wiki-mvp-core | orchestrator | 2026-08-05 | 2026-08-05 | test-engineer | open |  | The td-034 traceability re-derivation surfaced 8 genuine coverage gaps - REQs whose behavior is implemented but asserted by no test (1 UNTESTED, 7 PARTIAL where a named clause is unasserted). Filed as ONE row, not eight: they share a cause (the original .ai-work/ traceability was never baked in, so nothing ever compared REQ to test) and they resolve as one coverage pass. Sharpest three: REQ-PLUGIN-01 (.mcp.json launch line + absent `alwaysLoad` asserted by nothing - cheapest to close); REQ-REC-03 (nothing asserts root SCHEMA.md documents the five record formats, and the grammars are re-declared in tests/support/vault.py rather than read back, so doc and code can diverge silently); REQ-CLI-07 (curated / last-lint / unpushed counts implemented but unasserted). Full per-REQ status is the Status column of the spec's §Traceability matrix. | spec-wiki-mvp-core-uncovered-reqs |
