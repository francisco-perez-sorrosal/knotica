# TECH_DEBT_LEDGER

Active debt rows (`status ∈ {open, in-flight}`). Terminal rows migrate to `TECH_DEBT_RESOLVED.md` at post-merge finalize. Writers: verifier, sentinel, orchestrator, architect-validator only. Schema: praxion `skills/software-planning/references/tech-debt-ledger.md`.

**The file-size rows (td-002, td-008, td-009, td-042) share one bounding instrument.** `tests/test_file_size_ratchet.py` scans `src/knotica` and `tests/`, each with its own baseline, under three rules parametrised over both roots: a baseline module may shrink but never grow, a non-baseline module may never cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three were proven to fire before landing. The rows below therefore record **bounded, unpaid** debt — the ratchet stops the growth, not the debt. Stating the instrument once here rather than per row is deliberate: the paragraph was previously duplicated verbatim across three rows, and a single edit to the gate falsified all three at once without any of them noticing. Measured 2026-08-05: `src/knotica` carries 1,196 lines of overage across five modules; `tests/` carries 5,407 across eleven — the larger debt is in the test tree by 4.5×.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|------------|--------|-------------|-------|-----------|
| td-002 | important | complexity | code-to-goals | src/knotica/evals/harness.py, src/knotica/evals/golden.py | code-quality | coding-style - file size hard ceiling 800 lines | verifier | 2026-07-16 | 2026-08-05 | implementer | resolved | 48de546 | Measured 2026-08-05: `harness.py` 1214 (+414), `golden.py` 975 (+175); both equal their ratchet baselines, so neither has grown. Bounded by the shared instrument above. The split stays deliberately deferred: `harness.py` is the eval instrument, and changing it rotates `harness_version` and refreezes every topic's baseline — that cost belongs to its own pass, not to a feature phase. Split by cohesion via the refactoring skill, or accept with rationale. Prior remeasurements had drifted: the row simultaneously asserted 1214/975 in one sentence and 1200/970 in the next, so a reader could not tell which half was authoritative; corrected here to the single measured pair. RESOLVED (dec-123): Gate 0 proved the fingerprint digests only its five declared inputs (the deferral's premise was never tested); harness/ + golden/ packages, max module 303, zero consumer churn, baselines deleted. | 370309c1624b |
| td-009 | suggested | complexity | code-to-goals | src/knotica/core/records.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-07-19 | 2026-08-05 | implementer | resolved | bf8b7f4 | Measured 2026-08-05: 947 lines, 147 over, equal to its ratchet baseline. Growth history 719 → 858 → 947 as `GapRecord` (P1) and `SuggestionRecord` (P3) landed. Cohesive record-family module; split by record family (eval / gap / suggestion / note) or accept with rationale. Deferred per the user's ceiling directive alongside td-002 and td-008. RESOLVED (dec-121): core/records/ package, 8 modules along the family axis (partition argued in the ADR), zero churn, baseline deleted. | records-py-file-size-ceiling |
| td-030 | suggested | complexity | code-to-goals | tests/test_mcp_notes.py, tests/test_evals_harness.py, tests/core/notes/test_anchor.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-08-02 | 2026-08-05 | test-engineer | open |  | Eleven test modules over the ceiling; remeasured 2026-08-05, still exactly eleven, none new, none grown. `test_mcp_notes.py` 2506 (more than 3× the ceiling), `test_evals_harness.py` 1565, `core/notes/test_anchor.py` 1534, `core/notes/test_resolve.py` 1489, `test_evals_golden.py` 1372, `core/notes/test_reanchor_note.py` 1111 (baseline records 1112; it shrank by one), `test_search.py` 1086, `core/notes/test_capture_note.py` 950, `core/notes/test_store.py` 938, `test_transaction.py` 847, `test_cli_eval.py` 809. Total overage **5,407 lines — 4.5× the source tree's 1,196**, which is the number a reader prioritising paydown most needs and which the row previously did not state. GROWTH IS CLOSED: the ratchet gained `tests/` as a second scan root with its own baseline on 2026-08-02, parametrised over both trees so a rule added for one is added for both; the gate was verified to fail (a 900-line probe file was rejected by rule 2 with the correct message, and the suite went green on its removal). WHAT REMAINS is the paydown, deliberately not demanded here for the same reason the source baseline does not demand it: splitting a 2500-line behavioural test file risks losing coverage in the move and should be done per-file by someone with the domain in context, not as a bulk sweep. Priority is by size and edit frequency — `test_mcp_notes.py` is both the largest and among the most frequently touched. Several of these grew during the notes-overlay phases, so their baselines record a high-water mark that includes those additions. | test-tree-invisible-to-file-size-ratchet |
| td-048 | suggested | drift | code-to-goals | vault:main/agentic-systems/reports/guillotine/2026-07-07-reasoning-only-systems-hallucinate-because-they-are-closed.md, vault:main/agentic-systems/reports/guillotine/2026-07-08-reasoning-only-systems-hallucinate-because-they-are-closed.md | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | doc-engineer | open |  | Commit 5706f22 removed `PassageRole.DEPENDS_ON` - declared, wired into four lookups, advertised in the report legend, and never assigned by the classifier, so the legend documented a verdict the code could not produce. Reports generated since no longer carry the line. Two already-committed reports still do, both in the `main` vault at ~/dev/data/knotica, NOT the currently-active `decision-making` vault - so acting on this means switching vaults deliberately. LOCATION IS OUTSIDE THIS REPO: these are vault data, and the vault is the user's data, so no code change reaches them and no gate will ever flag them. Defensible to leave as dated artifacts of past runs. If corrected, the write MUST go through core.transaction like any other vault mutation - one git commit per mutating operation is an invariant, and hand-editing the vault bypasses the flock and the log append. | guillotine-reports-carry-retired-depends-on-legend |
| td-050 | suggested | other | code-to-goals | src/knotica/service/manager.py, src/knotica/core/loop_factory.py, src/knotica/cli/loop.py | adr | dec-078 | orchestrator | 2026-08-05 | 2026-08-05 | implementer | open |  | Commit 450ccd5 wired the arena scorer into the OS-supervised daemon and into `loop action=run_eval`, matching what dec-044 and DESIGN.md had always claimed and closing a silent no-op. CONSEQUENCE, recorded here because it is residual risk rather than a defect: the daemon now mutates `<topic>/.knotica/prompts/query.md` UNATTENDED on an arena win, plus roughly seven arena-state commits per race at the default arena_n=4. `--no-arena` exists only on `knotica loop` (cli/loop.py); there is no daemon-level or config-level off switch. dec-078 names the remedy explicitly: if unattended arena promotion degrades a topic's prompts, add a config knob - and NOT a re-omission of `arena_score`, which is exactly the bug that was fixed. WATCH ITEM, not actionable yet: the trigger is an observed prompt regression on a supervised topic. Filing it so the trigger is written down somewhere a future reader will look, rather than living only in one ADR's Consequences prose - which is the failure mode dec-074 was itself filed to correct. | daemon-has-no-arena-off-switch |
| td-054 | important | other | code-to-goals | .git/hooks/pre-commit | rule | id-citation-discipline - inbound enforcement | orchestrator | 2026-08-10 | 2026-08-10 | orchestrator | resolved | 3ea4fac | **The inbound id-citation commit gate is silently disabled for every commit made inside a Praxion worktree — which Standard/Full tiers MANDATE.** Found while verifying the M0 commit: the pre-commit hook reported `scanned 0 code file(s)` on a commit staging 21 `.py`/`.ts` files. Cause is in the plugin's checker (`~/.claude/plugins/cache/bit-agora/i-am/0.22.0/scripts/check_id_citation_discipline.py`), which the hook runs in preference to this repo's own copy: `EXCLUDED_PATH_FRAGMENTS` contains `"/.claude/worktrees/"` and `is_excluded_path()` matches it against the **absolute** path. The fragment's own comment states the intent — skip *sibling* worktrees when scanning from the canonical checkout, because *"Each worktree is scanned by its own run"* — and the implementation defeats exactly that case: in the worktree's own run every file's absolute path contains the fragment, so all are dropped. **Proven by differential**: the identical file scans as 1 from the canonical checkout and from a fresh clone, and as 0 from the worktree. This repo's own `scripts/check_id_citation_discipline.py` is a different, diverged script (393 lines vs 383, different exempt sets) and scans correctly — run explicitly over all 21 committed files it reported **0 violations**, so no contaminated code landed; the gate, not the code, is the defect. Note the shape is the same one `td-044` recorded against the ADR gate: a check whose scope is narrower than the claims resting on it. Fix is upstream (match the fragment against the path **relative to repo_root**, or skip it when repo_root is itself inside a worktree) — file via the `upstream-stewardship` skill. Interim mitigation: run `uv run python scripts/check_id_citation_discipline.py --files $(git diff --cached --name-only)` by hand before any worktree commit. RESOLVED (dec-120), deeper than filed: BOTH gate layers were dead everywhere (renamed plugin key -> silent exit 0), not only in worktrees. Repo hook prefers the repo checker and fails loudly; upstream praxion source fixed locally (publish is the user's). | commit-gate-disabled-inside-worktrees |
| td-062 | suggested | drift | code-to-goals | dashboard/src/lanes/LaneRail.tsx:15, dashboard/src/lanes/LaneRail.tsx:43, dashboard/src/lanes/improve/ImproveLane.tsx:24, dashboard/src/lanes/improve/ImproveLane.tsx:206 | rule | id-citation-discipline - ephemeral document references | verifier | 2026-08-27 | 2026-08-27 | doc-engineer | resolved | 3ea4fac | **Committed source cites an ephemeral interface-design document by section in ~200 places, and TWO DIFFERENT documents now share that filename with clashing section numbering.** The id-citation-discipline rule of thumb is explicit -- if the identifier lives in a document deleted with `.ai-work/`, it does not belong in code -- but the section-symbol shape is not in `check_id_citation_discipline.py`'s pattern set, so nothing has ever flagged it, and td-054 records that the gate is silently disabled inside worktrees anyway. PRE-EXISTING AND PERVASIVE: dozens of files untouched by this pipeline carry the citations from the swimlane pipeline. What is NEW as of 2026-08-27 is the AMBIGUITY -- failure mode #2 in the rule. The dashboard-ui-redesign pipeline wrote its own interface-design doc at the same path with independent numbering, and single files now cite both: `LaneRail.tsx:15` cites the swimlane doc's rail wire contract while `LaneRail.tsx:43` cites the redesign doc's focus axis; `ImproveLane.tsx:24` and `:206` do the same. Both documents are deleted at cleanup and a reader has no way to tell which numbering a given comment means. MEASURED: 51 lines added by this pipeline carry such a citation; zero `REQ-`/`AC-`/`Step N`/`dec-draft-` were added, so the mechanical discipline held perfectly and only this unpatterned shape leaked. Fix, in order of value: (1) rewrite the citations to state the CONSTRAINT instead of pointing at a section -- most already do this in the surrounding prose, so the pointer is redundant rather than load-bearing; (2) where the rationale is genuinely worth preserving, cite the finalized `dec-NNN` (three drafts from this pipeline cover the design-language adoption, the shared presentation primitives, and the focus dimension); (3) extend `check_id_citation_discipline.py` to flag the shape so it cannot silently re-accumulate. Do (3) LAST -- landing it before (1) turns every commit red. RESOLVED: ~272 pointers retired across dashboard+python (deleted/inlined/dec-cited); checker learned ephemeral-doc-ref + section-citation shapes with durable targets exempt; tree scans 709 files clean. | interface-design-section-citations-ambiguous-across-two-docs |
| td-071 | suggested | correctness | code-to-goals | src/knotica/core/records.py (GapRecord), src/knotica/core/gapfill/drain.py, src/knotica/core/gapfill/gap_review.py | behavior | records.py unknown-field tolerance contract | orchestrator | 2026-08-31 | 2026-08-31 | implementer | resolved | bf8b7f4 | **`GapRecord` lacks the unknown-field round-trip `SuggestionRecord` got** (the F-FL-08 `extra` carrier): every full-file rewriter of `gaps.jsonl` re-serializes from the fixed field list and silently drops fields a newer version wrote. The td-070 stamp made the drain a SECOND gaps.jsonl rewriter (alongside gap_review's decision path), widening a pre-existing surface — flagged by that batch's registered objection, deliberately not fixed in-scope. Fix shape mirrors F-FL-08: an `extra` mapping on `GapRecord` round-tripped by `to_json_line`, with the guarantee stated in the docstring; then the "tolerated on read" claim is true through mutations for both record families. RESOLVED: GapRecord.extra round-trip mirroring SuggestionRecord; unit + drain end-to-end tests proven to fail without the carrier. | gaprecord-extra-roundtrip |
