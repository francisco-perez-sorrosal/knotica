# TECH_DEBT_LEDGER

Active debt rows (`status ∈ {open, in-flight}`). Terminal rows migrate to `TECH_DEBT_RESOLVED.md` at post-merge finalize. Writers: verifier, sentinel, orchestrator, architect-validator only. Schema: praxion `skills/software-planning/references/tech-debt-ledger.md`.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|------------|--------|-------------|-------|-----------|
| td-002 | important | complexity | code-to-goals | src/knotica/evals/harness.py, src/knotica/evals/golden.py | code-quality |  | verifier | 2026-07-16 | 2026-07-18 | implementer | open |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: harness.py 880→1117→1200 lines (grew +122 in gapfill-substrate P0), golden.py 970 vs 800 ceiling; docstring-dominated but now well over — split by cohesion via refactoring skill or accept with rationale; severity raised suggested→important after growth REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | 370309c1624b |
| td-008 | important | complexity | code-to-goals | src/knotica/core/loop.py | code-quality |  | verifier | 2026-07-19 | 2026-07-22 | implementer | in-flight |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: loop.py STILL over ceiling after a third pass (1137 lines vs 800 ceiling, 337 over). History: 924 pre-P1, 1047 post-P1, 1221 post-consolidation, 1347 post-eval-cadence-model-config, 1087 post-loop-py-extraction, 1137 measured 2026-07-29 (2026-07-22: extracted build_loop_runner -> core/loop_factory.py verbatim, _run_arena_and_resolve -> core/arena_resolve.py as a free function, and the candidate-gate cluster (poll_once/next_candidate/process_candidate/keep/discard) -> core/candidate_gate.py as free-functions-on-runner mirroring source_gate.py's idiom; characterization-tests-first, two forced light-reviews both PASS, full suite green throughout). PARTIAL RESOLUTION ONLY — research (loop-py-extraction/RESEARCH_FINDINGS.md) found the ledger's own prior estimate was optimistic: these were the low-risk, low-coupling extractions; genuinely clearing 800 requires extracting the 397-line observe_default cluster (billed-trigger nonce + td-011 fix + cadence guards, dec-048) and the 147-line gap-classification sub-cluster, both deliberately deferred as higher-risk (they are the most recently and carefully adversarially-reviewed code in the file). Next move: a fourth pass targeting observe_default specifically, once that code has had time to prove stable in production, or when a compelling reason forces revisiting it anyway. REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | loop-py-file-size-ceiling |
| td-009 | suggested | complexity | code-to-goals | src/knotica/core/records.py | code-quality |  | orchestrator | 2026-07-19 | 2026-07-19 | implementer | open |  | BOUNDED 2026-07-29 by `tests/test_file_size_ratchet.py`: the over-ceiling set may shrink but never grow, no module outside the recorded baseline may cross 800, and a baseline entry that falls to the ceiling must be removed so the exemption cannot silently return. All three rules were proven to fire before landing. The refactor itself remains deliberately deferred -- `evals/harness.py` is the eval instrument and `core/loop.py` is the most adversarially-reviewed code in the tree, so splitting them belongs in its own pass, not bolted onto a feature phase. Measuring every file (rather than trusting this ledger) also found TWO over-ceiling modules no row tracked at all: `core/gapfill.py` (938) and `guillotine/report.py` (847). Final baseline: harness 1214, loop 1146, golden 975, records 947, gapfill 938, guillotine/report 847 -- 1267 lines of total overage, now frozen. Row stays OPEN: the debt is bounded, not paid. File-size ceiling: records.py 719->858->947 lines after GapRecord (P1) + SuggestionRecord (P3) additions crossed the 800 ceiling. Cohesive record-family module; split by record family (eval/gap/suggestion) or accept with rationale. Deferred per user ceiling-directive alongside td-002/td-008 REMEASURED 2026-07-29 (orchestrator): counts in this row had drifted below reality; corrected above. | records-py-file-size-ceiling |
| td-025 | suggested | other | code-to-goals | src/knotica/core/notes/candidates.py | architecture | PRE_PLAN - anchor recovery must not depend on where a passage sits on a page | orchestrator | 2026-07-31 | 2026-07-31 | implementer | open |  | The candidate window's FORWARD extension carries the same defect just fixed on the backward side. Backward extension used to run past headings and blank lines to offset 0, so a quote near a page's top was scored against a window that was mostly heading chrome with the quote truncated -- measured at 0.43 near-top versus 0.72 mid-page for the SAME one-word edit, i.e. position on the page dominated the score, and notes anchored near a page top (where opening claims live, so the common case) orphaned when they should not have. Fixed by bounding backward extension at the nearer of a blank-line run, an ATX heading, or the enclosing sentence start; the headline regression test now requires near-top and mid-page to score within 0.05, and they are identical. The forward side was probed directly and has the symmetric hole: an unpunctuated trailing sentence before page-closing chrome swallows it forward. NOT fixed, deliberately: no test exercises it, and every real anchored quote ends in terminal punctuation which already bounds the window, so the fix would be untested speculation about a case nobody has hit. The symmetric fix is sketched in the module docstring (a `_nearest_block_end` mirroring `_nearest_block_start`, combined with `min()` instead of `max()`). Trigger to act: a note whose anchored passage ends without terminal punctuation -- a heading-like fragment, a list item, or a table cell -- resolving worse than its position on the page should warrant. | candidate-window-forward-extension-unbounded |
| td-026 | suggested | other | code-to-goals | dashboard/src/types.ts | architecture | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-07-31 | 2026-08-02 | implementer | open |  | GREW AGAIN 2026-08-02: `types.ts` is now 1117 lines (was 1093 when this row was filed), +24 for the `NoteDrift.cause` field carrying the supersession classifier to the drift UI. Recorded rather than quietly absorbed -- the point of this row is that nothing gates it, so every growth has to be noticed by a human or not at all. Note the asymmetry that makes this sharper: the Python test tree WAS closed off on the same day (`td-030` extended `tests/test_file_size_ratchet.py` to a second root), so `dashboard/` is now the only shipped source tree with no ceiling enforcement at all. ORIGINAL FINDING: The 800-line file-size ceiling is enforced by `tests/test_file_size_ratchet.py`, which scans `src/knotica` only -- i.e. Python only. The TypeScript dashboard is invisible to it, and two files are already over the general coding-style ceiling with no gate noticing: `dashboard/src/types.ts` (1093 lines) and `dashboard/src/toolClient.ts` (~750 and growing). Phase 2 grew `types.ts` further (+156) and the Phase 2 verification pass grew it again. This is not a Python-vs-TypeScript exemption anyone decided on -- it is a blind spot in where the ratchet looks, so the ceiling silently does not apply to roughly a fifth of the shipped source. Two ways to close it: extend the ratchet's scan roots to `dashboard/src` (cheap, and would fail immediately on the two existing files, so it needs a baseline entry like the Python `OVER_CEILING_BASELINE`); or split `types.ts` by domain (notes / sources / arena / loop), which is the fix the file actually wants. The second is preferable and larger. Flagged in Phase 2's LEARNINGS as "pre-existing tech debt outside this phase's scope to fix -- for the verifier or sentinel to formally ledger, not silently carry"; this row is that ledgering. | dashboard-typescript-invisible-to-file-size-ratchet |
| td-030 | suggested | complexity | code-to-goals | tests/test_mcp_notes.py, tests/test_evals_harness.py, tests/core/notes/test_anchor.py | code-quality | coding-style - file size hard ceiling 800 lines | orchestrator | 2026-08-02 | 2026-08-02 | test-engineer | open |  | ELEVEN test modules sit over the 800-line ceiling, now BOUNDED but not paid down. The Phase 4 brief flagged `tests/test_search.py` (1086) as the offender; measuring rather than trusting that found it is the SEVENTH largest, behind `test_mcp_notes.py` at 2506 -- more than three times the ceiling -- plus `test_evals_harness.py` 1565, `core/notes/test_anchor.py` 1534, `core/notes/test_resolve.py` 1489, `test_evals_golden.py` 1372, `core/notes/test_reanchor_note.py` 1112, then test_search.py, `core/notes/test_capture_note.py` 950, `core/notes/test_store.py` 938, `test_transaction.py` 847, `test_cli_eval.py` 809. Same blind spot as `td-026` (the TypeScript dashboard), one directory over: `tests/test_file_size_ratchet.py` scanned `src/knotica` only, so the ceiling silently did not apply to the test tree at all. FIXED 2026-08-02 to the extent that matters for GROWTH: the ratchet now scans `tests/` as a second root with its own baseline, parametrised over both trees so a rule added for one is added for both. All three rules apply -- the eleven may shrink but never grow, a new test module may never cross the ceiling, and a baseline entry that falls to the ceiling must be removed. Verified the gate actually fails: a 900-line probe file was rejected by rule 2 with the correct message, and the suite went green again on its removal. WHAT REMAINS is the paydown, which is deliberately NOT demanded here for the same reason the source baseline does not demand it -- splitting a 2500-line behavioral test file is a real refactor with real risk of losing coverage in the move, and it should be done per-file by someone with the domain in context, not as a bulk sweep. Priority order is by size and by how often the file is edited: `test_mcp_notes.py` is both the largest and among the most frequently touched. NOTE for whoever pays it down: several of these grew during the notes overlay phases, so their baselines record a high-water mark that includes this session's additions. | test-tree-invisible-to-file-size-ratchet |
| td-031 | important | correctness | code-to-goals | src/knotica/core/loop.py | architecture | PRE_PLAN - one git commit per mutating vault operation | orchestrator | 2026-08-02 | 2026-08-02 | implementer | resolved | 2026-08-05 implementer added `src/knotica/core/loop_attempt.py`; ADR dec-draft-7f4c1a92 | RESOLVED 2026-08-05 (implementer, task `sentinel-remediation`): the FAMILY is closed, not just the dec-068 instance. A new `core/loop_attempt.py` suppresses the state write when an attempt is materially identical to the one on record, where identity = (a) same content, compared via the existing `_content_changed_since` rather than sha equality -- the loop's own bookkeeping commits move HEAD between attempts, so sha equality would never converge -- and (b) every LoopState field equal outside a DENY-list of `updated_at` / `last_eval_started_at` / `candidate_sha`. Deny-list not allow-list on purpose: a field added later participates automatically, and `tests/test_loop_attempt.py` carries a coverage guard that fails when a LoopState field is added unclassified. The asymmetry is deliberate -- a too-broad identity silently swallows a NEW failure, which is far worse than a redundant commit. CRITICAL FINDING the original row did not anticipate: pacing read `last_eval_started_at`, which only advances when a write happens, so suppressing writes would have released the retry floor every tick and traded cheap commit spam for far more expensive EVAL spam (git clone + LLM calls every 5-30s instead of every 60s). The retry clock therefore moved out of git to a gitignored marker under `.knotica/locks/` beside the heartbeat, with `last_eval_started_at` as the fresh-machine fallback; in-process state was ruled out because `service.manager._default_run_topic` builds a fresh LoopRunner every supervision cycle. Characterization tests pinned the two-commit behavior before the change. `core/loop.py` net SHRANK 23 lines (td-008 unharmed). Accepted trade-off, recorded as the ADR's dissent: two upstream faults that stringify identically are now indistinguishable in history. Already-bloated vaults are still not repaired -- history rewriting stays out of scope, so the measured 14.8k commits stand. | loop-noop-attempt-writes-commits |
| td-032 | important | drift | goals-to-code | src/knotica/okf/, src/knotica/guillotine/, src/knotica/service/, src/knotica/dashboard/, .ai-state/DESIGN.md | architecture | .ai-state/DESIGN.md 3. Components | sentinel | 2026-08-04 | 2026-08-04 | systems-architect | in-flight |  | ARCHITECT HALF DONE 2026-08-04 (`dec-draft-c20759d6`), TOPOLOGY HALF OUTSTANDING -- status is `in-flight`, not `resolved`, because consequence (a) is still live: the 11 files / 128 tests still belong to no group until `/refresh-topology` runs, and that run is the user's next step, deliberately excluded from this pass so the test-engineer's verified-selector discipline is not bypassed. WHAT LANDED: `.ai-state/DESIGN.md` section 3 gained five Built rows -- `src/knotica/okf/`, `src/knotica/guillotine/`, `src/knotica/service/`, a Dashboard row spanning `dashboard/` + `src/knotica/dashboard/`, and a new `core/` compile-chain row (`compile_run`/`compile_state`/`compile_promote`/`compiled`/`query_engine`/`trainset`/`models_config`/`prompt_diff`) that had no anchor at all -- and three rows were revised: the loop row's Component cell widened to name the eleven siblings its own prose already described, the residual `core/` row re-labelled an explicit residual with its omitted read/aggregate substrate enumerated, and a new layer-position table added below section 3 recording where each non-core domain layer sits relative to the inward-arrow rule. `docs/architecture.md` gained matching `okf/`, `guillotine/`, `service/` rows, a Dashboard row that now says what its `src/knotica/dashboard/` path holds, six new Navigation entries, and every path in its table re-verified on disk. GRANULARITY CALLS (each argued in the ADR): one row per package for okf/guillotine/service because their modules are one vocabulary with several entry points and change together; NO row for `src/knotica/dashboard/` alone -- a 24-line `importlib.resources` loader is a packaging seam, not a component, so it is modeled inside the Dashboard row while the two mounts stay with `mcp_server/`. TWO NEW FINDINGS SURFACED, NEITHER FIXED (documentation-only pass): (1) `guillotine/report.py::write_artifacts` calls `store.write_text_atomic` three times outside any transaction and has ZERO callers in src/ or tests/ -- a pre-transaction path `core/operations/guillotine.py` replaced, sitting outside every fitness scan (the mutating-store-method check is scoped to ADAPTER_PACKAGES; the raw-write check reasons about `Path.write_text`, not store methods), so the single-writer rule has an unguarded live counter-example; (2) sentinel TT01 will FAIL against TEST_TOPOLOGY.md until `/refresh-topology` re-derives its Subsystems table, since five rows were added and one Component cell changed -- a known sequenced red window, not a regression. EXPECT ON REFRESH: groups become creatable for okf/guillotine/service/dashboard, `query-compile` gains a real section-3 anchor for the eight core modules Note 2 currently claims on no basis, and `vault-semantics` should shrink by roughly 4,500 lines of loop-sibling and compile-chain code. ORIGINAL FINDING: DESIGN.md 3. Components under-models the codebase in two directions at once. MISSING ENTIRELY: four shipped top-level packages have no component row -- src/knotica/okf/ (11 modules), guillotine/ (9), service/ (3), dashboard/ (1) = 24 modules. Sentinel verified by grep that okf, guillotine and service appear ZERO times in .ai-state/DESIGN.md as a whole AND zero times in docs/architecture.md -- they are architecturally undocumented in both artifacts, which is one level worse than the "no section-3 row" framing in dec-draft-4b91f4f7. dashboard appears once in the developer guide. TOO COARSE: src/knotica/core/ is a single row standing for 56 top-level modules. CONSEQUENCES: (a) 11 test files / 128 tests belong to no TEST_TOPOLOGY.md group so scoped runs silently skip them; (b) the systems-architect brownfield baseline for future guillotine/ or okf/ work starts from a document that does not know those packages exist; (c) vault-semantics is a 550-test residual group whose size is unfixable from inside the topology; (d) docs/architecture.md advertises a Path (verified on disk) column and a Last verified 2026-07-30 date, so a developer trusting it would conclude guillotine/ does not exist while CLAUDE.md Current status describes its behavior in detail -- the narrative and structural surfaces disagree about the system contents. Deliberately deferred by dec-draft-4b91f4f7 (topology binds to section-3 granularity; no synthetic subsystem names), whose own dissent field anticipates this row: "a topology that silently under-covers ~24 modules is arguably worse than one with four honest synthetic subsystem names, because the gap is invisible at the point of use". That ADR is an unfinalized draft and therefore excluded from DECISIONS_INDEX.md by design, so this row is the only discoverable home for the obligation. TRIGGER TO ACT: the ADR reversal trigger (a) -- refine section 3, then run /refresh-topology to gain the corresponding groups. FIX IS A TWO-DOCUMENT PASS (DESIGN.md + docs/architecture.md), not a section-3 edit. | fd02643fd7b4 |
| td-033 | suggested | drift | goals-to-code | .ai-state/decisions/018-golden-set-split-and-bootstrap.md, .ai-state/decisions/068-loop-blocked-failure-backoff.md, .ai-state/decisions/DECISIONS_INDEX.md | architecture | rules/swe/adr-conventions.md Frontmatter + Re-affirmation Protocol | sentinel | 2026-08-04 | 2026-08-04 | doc-engineer | resolved | 2026-08-05 orchestrator added `scripts/check_adr_health.py`, wired into `make verify` | RESOLVED 2026-08-05 (orchestrator, task `sentinel-remediation`): both symptoms and the root cause are closed. Symptom 1 - 018 `summary:` and 068 `dissent:` wrapped in double quotes; all 69 frontmatter blocks now parse under yaml.safe_load with values byte-preserved. Symptom 2 - 8 one-directional `re_affirms` backfilled across 6 files (NOT the 22 the finding claimed; sentinel over-counted ~3x and its cited example dec-025 already listed both back-refs). Root cause - `scripts/check_adr_health.py` now gates frontmatter validity (all records incl. drafts) and reciprocity (finalized only, since a draft legitimately points at a finalized decision before finalize adds the backlink), running as the second step of `make verify`, with 8 tests in `tests/test_adr_health.py`. Fixing symptom 1 exposed a latent second defect: `regenerate_adr_index.py` did not unquote scalars, so the quoting would have leaked literal quotes into dec-018's index row at the next finalize; parser patched. Broadened-`re_affirms` semantics recorded as dec-draft-df837e3b. | 1f19903f4064 |
| td-034 | important | drift | goals-to-code | .ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md | spec-req | SPEC_wiki-mvp-core Traceability | sentinel | 2026-08-04 | 2026-08-04 | test-engineer | resolved | 2026-08-04 test-engineer re-derived the matrix into `.ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md` §Traceability | RESOLVED 2026-08-04 (test-engineer, task `sentinel-remediation`): the §Traceability pointer was replaced with a rendered 42-row matrix (Requirement / Test(s) / Implementation / Status), carrying an explicit RE-DERIVED-NOT-CONTEMPORANEOUS banner that dates the reconstruction, names `.ai-work/` cleanup as the reason the original evidence is unrecoverable, and warns against reading a COVERED row as proof the test predates the REQ. All three dangling `.ai-work/wiki-mvp-core/` references were annotated as deleted-at-cleanup rather than silently dropped (two in the Sources row, one in §Traceability). Every one of the 300 cited pytest node ids was machine-verified to exist in the current suite. Sentinel SH04 is no longer vacuous: it now has 42 entries to check. RE-DERIVED COVERAGE: 31 COVERED, 7 PARTIAL, 1 UNTESTED, 3 DRILL (unrecoverable manual evidence). TWO OBLIGATIONS THIS ROW DOES NOT DISCHARGE, both needing their own row from a ledger writer (test-engineer is a consumer and may not add rows): (1) the GL03/SH02 gate-liveness observation in the original finding -- SH02's "present and non-empty" pass condition should gain a "contains at least 1 requirement row" clause, otherwise the same structural check will pass on the next zero-substance section; (2) the 8 genuine coverage gaps the re-derivation surfaced, which are NEW findings, not part of this row's scope: REQ-PLUGIN-01 (`.mcp.json` launch line and absent `alwaysLoad` asserted by nothing -- cheapest to close), REQ-REC-03 (no test asserts root SCHEMA.md documents the five record formats; the frozen grammars are re-declared in `tests/support/vault.py` rather than read back, so doc and code can diverge silently), REQ-CLI-07 (curated-examples / last-lint / unpushed-commits counts implemented but unasserted), REQ-CLI-06 ("never invokes an LLM" survives only as docstring prose; the doctor `mcp` registration row is unpinned), REQ-PROMPT-02 (citation discipline on `query` unasserted), REQ-TOOL-02 (no-write-lock clause proven only structurally for adapters, never behaviourally for a core read path), REQ-TOOL-05 (required-non-empty topic on MUTATING tools unasserted -- the one empty-topic rejection test is a read tool), REQ-PLUGIN-02 (pre-warm command / backgrounding / idempotence unasserted). Per the task's non-goals no test or source file was written; the gaps were recorded as findings and left visible. ORIGINAL FINDING: The archived spec Traceability section is a pointer to a deleted file, not a rendered matrix, so 42 REQ ids have no test or implementation mapping in the permanent record. The section (lines 225-231) reads "Live map: .ai-work/wiki-mvp-core/traceability.yml ... The rendered matrix (Requirement / Test(s) / Implementation / Status) is baked into this section at feature-end archival, before .ai-work/ cleanup." The bake-in never happened and .ai-work/wiki-mvp-core/ no longer exists (present slugs: kb-dashboard-switcher, loop-consolidation, notes-overlay, retrieval-scorer-fix, test-topology-init), so all three .ai-work/ references in the spec dangle. EVIDENCE IS UNRECOVERABLE, not merely missing -- the yml was deleted at cleanup, so the mapping must be re-derived from the current suite. That is real work with a named owner, which is why this is debt rather than a fix-now defect. Sentinel SH04 (no UNTESTED entries) passes only VACUOUSLY: there are no entries at all. Note this is also a gate-liveness (GL03) instance -- the SH02 pass condition asserts the section is "present and non-empty" and the section does hold prose, so a structural check passes on an artifact with zero substance; SH02 upstream should gain a "contains at least 1 requirement row" clause. | 5337d2983eef |
| td-035 | suggested | drift | goals-to-code | .ai-state/calibration_log.md | code-quality |  | sentinel | 2026-08-04 | 2026-08-04 | implementer | resolved | 2026-08-05 orchestrator migrated the log to the canonical 7-column schema | RESOLVED 2026-08-05 (orchestrator, task `sentinel-remediation`): header migrated to the canonical `Timestamp / Task / Signals / Recommended Tier / Actual Tier / Source / Retrospective`; the gate now parses all 21 rows and immediately reported 50 uncalibrated pipeline commits since 2026-07-31 - a real finding it had been silently swallowing. Migration verified lossless: every original cell survives, the sole transformed cell being `Standard declared, **Full actually**`, deliberately split across the two tier columns. `Actual shape` was NOT folded into `Signals` - canonical Signals is at-intake scoring while `Actual shape` is realized-after, so conflating them would falsify the record; it is preserved at the head of each Retrospective. Recommended Tier and Source were derived, not blanked, for the 20 rows whose `Right call?` was Yes. NOTE: this fixes knotica's side only. The checker also fails open on the lowercase header `/onboard-project` itself seeds, because two Praxion documents declare conflicting canonical schemas - filed upstream as praxion#58. | 308f69809e2e |
| td-036 | important | correctness | code-to-goals | src/knotica/guillotine/report.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | implementer | resolved | 2026-08-05 implementer deleted the dead writer and widened the fitness scan codebase-wide | RESOLVED 2026-08-05 (implementer, task `sentinel-remediation`): `write_artifacts` deleted outright rather than routed through VaultTransaction -- it had zero references in src, tests or scripts, so the simplest correct change was removal. The fitness test no longer scans a package NAME-LIST: `test_adapters_do_not_call_mutating_store_methods` now enforces the DESIGN.md rule literally, codebase-wide, excluding only `core/transaction.py` (the sanctioned writer) and `store/` (which implements the mutation primitive, so its own use is implementation, not bypass). A name-list could only ever approximate the rule, and the next package added would start out uncovered -- which is exactly how this writer survived. One collision handled structurally: `delete` is both a VaultStore mutator and a VaultTransaction STAGING call, so the scan skips receivers bound by `with VaultTransaction(...) as <name>` -- rename-robust, and it fails CLOSED (a transaction obtained another way is flagged loudly). SIDE EFFECT: the deletion dropped `guillotine/report.py` to 799 lines, under the 800 ceiling, so the file-size ratchet correctly failed and its baseline exemption was removed -- a paid-down exemption is never kept. | guillotine-report-writes-outside-transaction |
| td-037 | suggested | test-coverage | goals-to-code | .ai-state/specs/SPEC_wiki-mvp-core_2026-07-03.md | spec-req | SPEC_wiki-mvp-core | orchestrator | 2026-08-05 | 2026-08-05 | test-engineer | open |  | The td-034 traceability re-derivation surfaced 8 genuine coverage gaps - REQs whose behavior is implemented but asserted by no test (1 UNTESTED, 7 PARTIAL where a named clause is unasserted). Filed as ONE row, not eight: they share a cause (the original .ai-work/ traceability was never baked in, so nothing ever compared REQ to test) and they resolve as one coverage pass. Sharpest three: REQ-PLUGIN-01 (.mcp.json launch line + absent `alwaysLoad` asserted by nothing - cheapest to close); REQ-REC-03 (nothing asserts root SCHEMA.md documents the five record formats, and the grammars are re-declared in tests/support/vault.py rather than read back, so doc and code can diverge silently); REQ-CLI-07 (curated / last-lint / unpushed counts implemented but unasserted). Full per-REQ status is the Status column of the spec's §Traceability matrix. | spec-wiki-mvp-core-uncovered-reqs |
